generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model mm_warehouses {
  warehouse_id        Int                       @id @default(autoincrement())
  warehouse_code      String?                   @db.VarChar(20)
  warehouse_name      String?                   @db.VarChar(200)
  warehouse_name_show String?                   @db.VarChar(200)
  label_name          String?                   @db.VarChar(200)
  is_actived          mm_warehouses_is_actived? @default(Y)
  is_deleted          mm_warehouses_is_deleted? @default(N)
  longitude           Float?                    @db.Float
  latitude            Float?                    @db.Float

  @@index([warehouse_id], map: "mm_warehouses_idx_warehouse_id")
}

model SLA {
  route_id        Int
  warehouse_id    Int?
  route_code      String? @db.VarChar(255)
  route_name      String? @db.VarChar(255)
  cost_oil        String? @db.VarChar(255)
  warehouse_name  String? @db.VarChar(255)
  tambon_id       String  @db.VarChar(6)
  tambon          String? @db.VarChar(255)
  ampur           String? @db.VarChar(255)
  province        String? @db.VarChar(255)
  Monday          String? @db.VarChar(255)
  Tuesday         String? @db.VarChar(255)
  Wednesday       String? @db.VarChar(255)
  Thursday        String? @db.VarChar(255)
  Friday          String? @db.VarChar(255)
  Saturday        String? @db.VarChar(255)
  Sunday          String? @db.VarChar(255)
  route_detail_id Int?

  @@id([route_id, tambon_id])
}

model acc_income_daily {
  id                    Int                               @default(autoincrement())
  date_receive          DateTime?                         @db.Timestamp(0)
  receive_date          DateTime                          @db.DateTime(0)
  create_date           DateTime                          @db.DateTime(0)
  doc_no                String                            @unique(map: "unq_doc_no") @db.VarChar(50)
  reference_no          String?                           @db.VarChar(200)
  serial_no             String                            @db.VarChar(100)
  package_name          String?                           @db.VarChar(255)
  cost                  Decimal?                          @db.Decimal(10, 2)
  customer_code         String                            @db.VarChar(7)
  customer_name         String                            @db.VarChar(255)
  from_warehouse_name   String?                           @db.VarChar(255)
  from_warehouse_id     Int?
  warehouse_name        String?                           @db.VarChar(255)
  to_warehouse_id       Int?
  recipient_code        String?                           @db.VarChar(255)
  recipient_name        String?                           @db.VarChar(255)
  address               String?                           @db.VarChar(255)
  tambon_name           String?                           @db.VarChar(255)
  ampur_name            String?                           @db.VarChar(255)
  province_name         String?                           @db.VarChar(255)
  zip_code              Int?
  tel                   String?                           @db.VarChar(12)
  del_retail            acc_income_daily_del_retail?      @default(N)
  del_bill              acc_income_daily_del_bill?        @default(N)
  payment_type_id       acc_income_daily_payment_type_id?
  payment_name          String?                           @db.VarChar(20)
  is_cod                acc_income_daily_is_cod?          @default(N)
  cod_baht              Decimal?                          @db.Decimal(5, 2)
  status_message        String?                           @db.VarChar(255)
  status_id             Int?
  note_del_bill         String?                           @db.VarChar(255)
  mounth                String                            @db.VarChar(255)
  year                  String?                           @db.VarChar(4)
  update_status_message String?                           @db.VarChar(255)
  update_status_id      String?                           @db.VarChar(255)
  bill_day              DateTime?                         @db.Date
  receives_money        acc_income_daily_receives_money?  @default(N)
  serial_id             String                            @db.VarChar(64)
  receive_id            Int?
  data_year             Int?
  data_yearmonth        Int?

  @@id([id, doc_no, serial_id])
  @@index([id], map: "id")
  @@index([data_year], map: "idx_data_year")
  @@index([data_yearmonth], map: "idx_data_yearmonth")
  @@index([serial_no], map: "idx_serial_no")
}

model at_accounts {
  account_id   Int                     @id @default(autoincrement())
  bank_id      Int?
  account_no   String?                 @db.VarChar(20)
  account_name String?                 @db.VarChar(200)
  bank_type_id Int?
  is_pay       at_accounts_is_pay?
  is_receipt   at_accounts_is_receipt?
  is_deleted   at_accounts_is_deleted? @default(N)
  is_actived   at_accounts_is_actived? @default(Y)
}

model at_billing_details {
  bill_detail_id Int  @id @default(autoincrement())
  bill_id        Int?
  inv_id         Int?
}

model at_billings {
  bill_id         Int                       @id @default(autoincrement())
  bill_no         String?                   @db.VarChar(30)
  bill_date       DateTime?                 @default(now()) @db.DateTime(0)
  customer_id     Int?
  create_user_id  Int?
  create_datetime DateTime?                 @default(now()) @db.DateTime(0)
  update_datetime DateTime?                 @db.DateTime(0)
  is_completed    at_billings_is_completed? @default(N)
  is_vat          at_billings_is_vat?
}

model at_co_details {
  co_detail_id          Int      @id @default(autoincrement())
  co_id                 Int?
  cost_id               Int?
  cost                  Decimal? @db.Decimal(32, 16)
  tax_with_holding      Int?
  rate_tax_with_holding Int?
}

model at_cos {
  co_id         Int                  @id @default(autoincrement())
  co_code       String?              @db.VarChar(30)
  creditor_id   Int?
  creditor_name String?              @db.VarChar(255)
  create_date   DateTime?            @default(now()) @db.DateTime(0)
  document_date DateTime?            @db.Date
  document_no   String?              @db.VarChar(200)
  address       String?              @db.VarChar(255)
  tax_no        String?              @db.VarChar(13)
  due_date      DateTime?            @db.Date
  inv_code      String?              @db.VarChar(255)
  inv_date      DateTime?            @db.Date
  inv_vat       Int?
  note          String?              @db.MediumText
  is_deleted    at_cos_is_deleted?   @default(N)
  is_completed  at_cos_is_completed? @default(N)
}

model at_costs {
  id         Int                  @id @default(autoincrement())
  code       String?              @db.VarChar(10)
  name       String               @db.VarChar(255)
  is_actived at_costs_is_actived? @default(Y)
  is_deleted at_costs_is_deleted? @default(N)
}

model at_creditors {
  id            Int                      @id @default(autoincrement())
  name          String                   @db.VarChar(255)
  is_actived    at_creditors_is_actived? @default(Y)
  is_deleted    at_creditors_is_deleted? @default(N)
  address       String?                  @db.VarChar(255)
  ampur_id      Int?
  tambon_id     Int?
  province_id   Int?
  zipcode       String?                  @db.VarChar(5)
  tax_no        Int?
  tax_type      at_creditors_tax_type?
  tax_branch    String?                  @db.VarChar(255)
  code          String?                  @db.VarChar(10)
  location_name String?                  @db.VarChar(255)
  room_no       String?                  @db.VarChar(255)
  floor         String?                  @db.VarChar(255)
  no            String?                  @db.VarChar(255)
  village       String?                  @db.VarChar(255)
  moo           String?                  @db.VarChar(255)
  soi           String?                  @db.VarChar(255)
  road          String?                  @db.VarChar(255)
}

model at_invoice_detail_items {
  inv_detail_item_id Int      @id @default(autoincrement())
  inv_detail_id      Int?
  serial_id          String?  @db.VarChar(64)
  cost               Decimal? @db.Decimal(32, 16)
}

model at_invoice_details {
  inv_detail_id       Int      @id @default(autoincrement())
  inv_id              Int?
  type                String?  @db.VarChar(2)
  receive_business_id Int?
  book_id             Int?
  total               Decimal? @db.Decimal(32, 16)
}

model at_invoices {
  inv_id          Int                     @id @default(autoincrement())
  inv_no          String?                 @db.VarChar(30)
  inv_date        DateTime?               @default(now()) @db.DateTime(0)
  customer_id     Int?
  create_user_id  Int?
  create_datetime DateTime?               @default(now()) @db.DateTime(0)
  update_datetime DateTime?               @db.DateTime(0)
  is_billing      at_invoices_is_billing? @default(N)
  is_vat          at_invoices_is_vat?
}

model at_receipt_co_details {
  receipt_detail_id Int      @id @default(autoincrement())
  receipt_id        Int?
  co_id             Int?
  cost              Decimal? @db.Decimal(32, 16)
}

model at_receipt_co_payments {
  receipt_payment_id Int                          @id @default(autoincrement())
  receipt_id         Int?
  type               at_receipt_co_payments_type?
  money              Decimal?                     @db.Decimal(32, 16)
  no                 String?                      @db.VarChar(50)
  bank               String?                      @db.VarChar(200)
  date               DateTime?                    @db.Date
  bank_id            Int?
  bank_name          String?                      @db.VarChar(255)
}

model at_receipt_cos {
  receipt_id       Int                      @id @default(autoincrement())
  receipt_code     String?                  @db.VarChar(30)
  creditor_id      Int?
  creditor_name    String?                  @db.VarChar(255)
  creditor_address String?                  @db.VarChar(255)
  receipt_date     DateTime?                @default(now()) @db.DateTime(0)
  create_user_id   Int?
  create_datetime  DateTime?                @default(now()) @db.DateTime(0)
  update_datetime  DateTime?                @db.DateTime(0)
  is_crash         at_receipt_cos_is_crash?
  creditor_tax_no  String?                  @db.VarChar(20)
}

model at_receipt_details {
  receipt_detail_id Int  @id @default(autoincrement())
  receipt_id        Int?
  bill_id           Int?
}

model at_receipt_payments {
  receipt_payment_id Int                       @id @default(autoincrement())
  receipt_id         Int?
  type               at_receipt_payments_type?
  money              Decimal?                  @db.Decimal(32, 16)
  no                 String?                   @db.VarChar(50)
  bank               String?                   @db.VarChar(200)
  date               DateTime?                 @db.Date
  bank_id            Int?
  bank_name          String?                   @db.VarChar(255)
}

model at_receipts {
  receipt_id      Int       @id @default(autoincrement())
  receipt_no      String?   @db.VarChar(30)
  receipt_date    DateTime? @default(now()) @db.DateTime(0)
  create_user_id  Int?
  create_datetime DateTime? @default(now()) @db.DateTime(0)
  update_datetime DateTime? @db.DateTime(0)
}

model audit_input_sn {
  id           Int       @default(autoincrement())
  create_date  DateTime  @db.Date
  create_time  DateTime  @db.Time(0)
  time_stamp   DateTime? @db.Timestamp(0)
  email_create String    @db.VarChar(319)
  serial_no    String    @db.VarChar(100)
  mac_address  String?   @db.VarChar(255)
  pic1         Bytes?    @db.MediumBlob
  pic2         Bytes?    @db.MediumBlob
  pic3         Bytes?    @db.MediumBlob

  @@id([id, serial_no])
  @@index([id], map: "id")
  @@index([serial_no], map: "series")
}

model audit_report_tk {
  id               Int                             @id @default(autoincrement())
  truck_load_id    Int?
  truck_code       String?                         @db.VarChar(20)
  date             DateTime?                       @db.Date
  truck_detail_id  Int?
  receive_code     String?                         @db.VarChar(50)
  serial_no        String?                         @db.VarChar(100)
  status_message   audit_report_tk_status_message?
  is_close         String?                         @db.VarChar(255)
  is_completed     String?                         @db.VarChar(255)
  customer_id      Int?
  user_truck_id    Int?
  status           String?                         @db.VarChar(255)
  warehouse_id     Int?
  to_warehouse_id  Int?
  job_id           Int?
  is_go            String?                         @db.VarChar(255)
  close_datetime   DateTime?                       @db.DateTime(0)
  go_datetime      DateTime?                       @db.DateTime(0)
  close_by         String?                         @db.VarChar(255)
  go_by            String?                         @db.VarChar(255)
  is_arrived       String?                         @db.VarChar(255)
  arrived_by       String?                         @db.VarChar(255)
  arrived_datetime DateTime?                       @db.DateTime(0)
  shipper_id       Int?
  recipient_id     Int?
  is_deleted       String?                         @db.VarChar(255)
  mw1              String?                         @db.VarChar(255)
  mw2              String?                         @db.VarChar(255)
  license_plate    String?                         @db.VarChar(255)
  name_th          String?                         @db.VarChar(255)
  customer_name    String?                         @db.VarChar(255)
  count4            String?                         @map("4_count") @db.VarChar(3)
  count5           String?                         @map("5_count") @db.VarChar(3)
  count11            String?                         @map("11_count") @db.VarChar(3)
  count12            String?                         @map("12_count") @db.VarChar(3)
  serial_id        Int
  cost             Decimal?                        @db.Decimal(32, 2)

  @@index([id], map: "id")
  @@index([serial_id], map: "serial_id")
}

model ci_sessions {
  id             String @db.VarChar(128)
  ip_address     String @db.VarChar(45)
  timestamp      Int    @default(0) @db.UnsignedInt
  data           Bytes  @db.Blob
  data_year      Int?
  data_yearmonth Int?

  @@id([id, ip_address])
  @@index([timestamp], map: "ci_sessions_timestamp")
  @@index([data_year], map: "idx_data_year")
  @@index([data_yearmonth], map: "idx_data_yearmonth")
}

model company {
  companyid Int         @id @default(autoincrement())
  fld_name  String?     @db.VarChar(200)
  resources resources[]
}

model contacts {
  contactid    Int     @id @default(autoincrement())
  fld_name     String  @default("") @db.VarChar(200)
  company      Int?
  title        String? @db.VarChar(200)
  department   Int?
  phone        String? @db.VarChar(30)
  mobile       String? @db.VarChar(30)
  fax          String? @db.VarChar(30)
  email        String? @db.VarChar(200)
  recordowner  Int?
  lastmodifyby Int?
}

model dc_schedule {
  id             Int     @id @default(autoincrement())
  tambon_id      Int?
  zip_code       Int?
  tambon         String? @db.VarChar(100)
  ampur          String? @db.VarChar(100)
  province       String? @db.VarChar(100)
  warehouse_id   Int?
  warehouse_name String? @db.VarChar(100)
  Monday         String? @db.VarChar(20)
  Tuesday        String? @db.VarChar(20)
  Wednesday      String? @db.VarChar(20)
  Thursday       String? @db.VarChar(20)
  Friday         String? @db.VarChar(20)
  Saturday       String? @db.VarChar(20)
  Sunday         String? @db.VarChar(20)

  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // เข้ากี่วัน Int? @map("เข้ากี่วัน")
  @@index([province], map: "idx_province")
  @@index([tambon_id], map: "idx_tambon_id")
  @@index([warehouse_id], map: "idx_warehouse_id")
  @@index([zip_code], map: "idx_zip_code")
}

model debug_log {
  id         Int       @id @default(autoincrement())
  message    String?   @db.Text
  created_at DateTime? @default(now()) @db.Timestamp(0)

  @@index([id], map: "id")
}

model department {
  departmentid Int         @id @default(autoincrement())
  fld_name     String?     @db.VarChar(200)
  resources    resources[]
}

model dm_documents {
  document_id   String    @id @db.VarChar(64)
  document_code String?   @db.VarChar(64)
  file_name     String?   @db.VarChar(150)
  file_path     String?   @db.VarChar(200)
  uploaded_at   String?   @db.VarChar(20)
  mime_type     String?   @db.VarChar(50)
  file_size     Int?
  type          String?   @db.VarChar(100)
  create_date   DateTime? @default(now()) @db.DateTime(0)

  @@index([document_code], map: "idx_document_code")
}

model documenthistory {
  documenthistoryid Int             @id @default(autoincrement())
  documentlibraryid Int
  revision          Int?
  datecreated       DateTime?       @db.Date
  fileformat        String?         @db.VarChar(100)
  attachment        Bytes?          @db.Blob
  recordowner       Int?
  documentcurrent   String?         @db.Char(1)
  documentlibrary   documentlibrary @relation(fields: [documentlibraryid], references: [documentlibraryid], onDelete: NoAction, onUpdate: NoAction, map: "documenthistory_ibfk_1")

  @@index([documentlibraryid], map: "documentlibraryid")
}

model documentlibrary {
  documentlibraryid Int               @id @default(autoincrement())
  projectid         Int?
  title             String?           @db.VarChar(200)
  description       String?           @db.Text
  attachment        Bytes?            @db.Blob
  filetype          String?           @db.VarChar(30)
  fileformat        String?           @db.VarChar(100)
  datecreated       DateTime?         @db.Date
  datemodified      DateTime?         @db.Date
  recordowner       Int?
  lastmodifiedby    Int?
  documenthistory   documenthistory[]
  projects          projects?         @relation(fields: [projectid], references: [projectid], onDelete: NoAction, onUpdate: NoAction, map: "documentlibrary_ibfk_1")

  @@index([projectid], map: "projectid")
}

model filetype {
  filetypeid  String  @id @db.VarChar(30)
  description String? @db.VarChar(200)
}

model issuepriority {
  priorityid                                 String   @id @db.VarChar(100)
  description                                String?  @db.VarChar(200)
  issues_issues_issuepriorityToissuepriority issues[] @relation("issues_issuepriorityToissuepriority")
}

model issues {
  issueid                                           Int            @id @default(autoincrement())
  issuename                                         String?        @db.VarChar(200)
  description                                       String?        @db.Text
  assignedto                                        Int?
  duedate                                           DateTime?      @db.Date
  issuestatus                                       String?        @db.VarChar(100)
  projectid                                         Int?
  updates                                           String?        @db.Text
  resolution                                        String?        @db.Text
  issuepriority                                     String?        @db.VarChar(100)
  dateidentified                                    DateTime?      @db.Date
  daysoverdue                                       Int?
  dateresolved                                      DateTime?      @db.Date
  datecreated                                       DateTime?      @db.Date
  datemodified                                      DateTime?      @db.Date
  recordowner                                       Int?
  lastmodifiedby                                    Int?
  issuestatus_issues_issuestatusToissuestatus       issuestatus?   @relation("issues_issuestatusToissuestatus", fields: [issuestatus], references: [statusid], onDelete: NoAction, onUpdate: NoAction, map: "issues_ibfk_1")
  projects                                          projects?      @relation(fields: [projectid], references: [projectid], onDelete: NoAction, onUpdate: NoAction, map: "issues_ibfk_2")
  issuepriority_issues_issuepriorityToissuepriority issuepriority? @relation("issues_issuepriorityToissuepriority", fields: [issuepriority], references: [priorityid], onDelete: NoAction, onUpdate: NoAction, map: "issues_ibfk_3")

  @@index([issuepriority], map: "issuepriority")
  @@index([issuestatus], map: "issuestatus")
  @@index([projectid], map: "projectid")
}

model issuestatus {
  statusid                               String   @id @db.VarChar(100)
  description                            String?  @db.VarChar(200)
  issues_issues_issuestatusToissuestatus issues[] @relation("issues_issuestatusToissuestatus")
}

model l_edit_mobile {
  id                   Int                                 @id @default(autoincrement())
  value_old            String?                             @db.VarChar(255)
  value_new            String?                             @db.VarChar(1000)
  user_id              Int?
  pk_id                Int?
  create_date          DateTime?                           @default(now()) @db.DateTime(0)
  concat               String?                             @unique(map: "concat") @db.VarChar(255)
  update2_l_edit_table l_edit_mobile_update2_l_edit_table? @default(N)
  timestamp            DateTime?                           @db.DateTime(0)
  table                String?                             @db.VarChar(255)
  column               String?                             @db.VarChar(255)
  data_year            Int?
  data_yearmonth       Int?

  @@index([id], map: "id")
  @@index([data_year], map: "idx_data_year")
  @@index([data_yearmonth], map: "idx_data_yearmonth")
}

model l_edit_table {
  id          Int       @id @default(autoincrement())
  table       String?   @db.VarChar(255)
  column      String?   @db.VarChar(255)
  value_old   String?   @db.VarChar(255)
  value_new   String?   @db.VarChar(1000)
  user_id     Int?
  pk_id       Int?
  create_date DateTime? @default(now()) @db.DateTime(0)

  @@index([id], map: "id")
  @@index([pk_id], map: "pk")
}

model l_edit_table_it {
  id          Int       @id @default(autoincrement())
  table       String?   @db.VarChar(255)
  column      String?   @db.VarChar(255)
  value_old   String?   @db.VarChar(255)
  value_new   String?   @db.VarChar(255)
  user_id     Int?
  pk_id       Int?
  create_date DateTime? @default(now()) @db.DateTime(0)
}

model l_edit_table_tmp {
  id          Int       @id @default(autoincrement())
  table       String?   @db.VarChar(255)
  column      String?   @db.VarChar(255)
  value_old   String?   @db.VarChar(255)
  value_new   String?   @db.VarChar(255)
  user_id     Int?
  pk_id       Int?
  create_date DateTime? @default(now()) @db.DateTime(0)
}

model log_tm_do_not_send_images {
  id             Int
  do_not_send_id Int?
  document_id    String?   @db.VarChar(200)
  log_type       String?   @db.VarChar(255)
  log_datetime   DateTime? @default(now()) @db.DateTime(0)
  log_id         Int       @id @default(autoincrement())
}

model log_tm_resend_images {
  id           Int
  resend_id    String?   @db.VarChar(64)
  document_id  String?   @db.VarChar(200)
  log_type     String?   @db.VarChar(255)
  log_datetime DateTime? @default(now()) @db.DateTime(0)
  log_id       Int       @id @default(autoincrement())
}

model log_tm_send_images {
  id           Int
  send_id      Int?
  document_id  String?   @db.VarChar(200)
  log_type     String?   @db.VarChar(255)
  log_datetime DateTime? @default(now()) @db.DateTime(0)
  log_id       Int       @id @default(autoincrement())

  @@index([document_id], map: "log_tm_send_images_document_id_IDX")
  @@index([log_id], map: "log_tm_send_images_log_id_IDX")
  @@index([log_type], map: "log_tm_send_images_log_type_IDX")
  @@index([send_id], map: "log_tm_send_images_send_id_IDX")
}

model lookup {
  code        String  @default("") @db.VarChar(100)
  xkey        String  @default("") @db.VarChar(100)
  description String? @db.VarChar(200)
  pm_type     String? @db.VarChar(100)

  @@id([code, xkey])
}

model measurements {
  id           BigInt    @id @unique(map: "measure_id") @default(autoincrement()) @db.UnsignedBigInt
  username     String?   @db.VarChar(50)
  width        Float?
  length       Float?
  height       Float?
  weight       Float?
  amount       Float?
  serial_no    String?   @db.VarChar(50)
  measure_date DateTime? @db.DateTime(0)

  @@index([serial_no], map: "ix_serial_no")
  @@index([username], map: "ix_username")
}

model mm_accident_types {
  id         Int                           @id @default(autoincrement())
  name       String?                       @db.VarChar(255)
  is_deleted mm_accident_types_is_deleted? @default(N)
}

model mm_accidents {
  accident_id   Int                      @id @default(autoincrement())
  accident_date DateTime?                @db.Date
  accident_code String?                  @db.VarChar(50)
  is_check      mm_accidents_is_check?   @default(N)
  user_id       Int?
  subject       String?                  @db.VarChar(200)
  detail        String?                  @db.VarChar(1000)
  is_deleted    mm_accidents_is_deleted? @default(N)
}

model mm_ampur {
  id          Int                  @id @default(autoincrement())
  code        String               @db.VarChar(4)
  name_th     String               @db.VarChar(150)
  name_en     String               @db.VarChar(150)
  province_id Int                  @default(0)
  is_deleted  mm_ampur_is_deleted? @default(N)
  is_actived  mm_ampur_is_actived? @default(Y)
}

model mm_banks {
  id         Int                  @id @default(autoincrement())
  code       String?              @db.VarChar(20)
  name       String?              @db.VarChar(200)
  is_deleted mm_banks_is_deleted? @default(N)
  is_actived mm_banks_is_actived? @default(Y)
}

model mm_banks_account_types {
  id         Int                                @id @default(autoincrement())
  name       String?                            @db.VarChar(200)
  is_deleted mm_banks_account_types_is_deleted? @default(N)
  is_actived mm_banks_account_types_is_actived? @default(Y)
}

model mm_complaint_types {
  id         Int                            @id @default(autoincrement())
  name       String?                        @db.VarChar(255)
  is_deleted mm_complaint_types_is_deleted? @default(N)
}

model mm_cost_other {
  id         Int                       @id @default(autoincrement())
  name       String?                   @db.VarChar(255)
  is_deleted mm_cost_other_is_deleted? @default(N)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model mm_cost_truck {
  cost_truck_id Int      @id @default(autoincrement())
  customer_id   Int?
  truck_type_id Int?
  cost          Decimal? @db.Decimal(32, 16)
  qty           Int?
}

model mm_delivery_status {
  id   Int     @id
  name String? @db.VarChar(255)
}

model mm_delivery_types {
  id         Int                           @id @default(autoincrement())
  name       String?                       @db.VarChar(255)
  is_deleted mm_delivery_types_is_deleted? @default(N)
  name_thai  String?                       @db.VarChar(255)
}

model mm_driver_truck {
  driver_id   Int
  truck_id    Int
  is_deleted  mm_driver_truck_is_deleted? @default(N)
  create_date DateTime                    @default(now()) @db.Timestamp(0)
  update_date DateTime                    @default(dbgenerated("'0000-00-00 00:00:00'")) @db.Timestamp(0)

  @@id([truck_id, driver_id])
}

model mm_drivers {
  id         Int                    @id @default(autoincrement())
  title_id   Int?
  first_name String?                @db.VarChar(100)
  last_name  String?                @db.VarChar(100)
  telephone  String?                @db.VarChar(100)
  truck_id   Int?
  is_deleted mm_drivers_is_deleted? @default(N)
  is_actived mm_drivers_is_actived? @default(Y)
}

model mm_ex_curier {
  curier_id      Int                     @default(autoincrement())
  key_curier     String?                 @db.VarChar(30)
  curier_name    String                  @db.VarChar(200)
  color          String?                 @db.VarChar(10)
  is_delete      mm_ex_curier_is_delete? @default(Y)
  is_active      mm_ex_curier_is_active? @default(Y)
  create_date    DateTime?               @db.DateTime(0)
  timestamp      DateTime?               @db.Timestamp(0)
  imageURL       String?                 @db.VarChar(255)
  url_image      String?                 @db.VarChar(255)
  data_year      Int?
  data_yearmonth Int?

  @@id([curier_id, curier_name])
  @@index([curier_name], map: "curier_name")
  @@index([curier_id], map: "id")
  @@index([data_year], map: "idx_data_year")
  @@index([data_yearmonth], map: "idx_data_yearmonth")
  @@index([key_curier], map: "key_curier")
}

model mm_ex_curier_reason {
  curier_reason_id Int                            @id
  reason           String                         @db.VarChar(255)
  is_delete        mm_ex_curier_reason_is_delete? @default(N)
  is_active        mm_ex_curier_reason_is_active? @default(Y)
  create_date      DateTime?                      @db.DateTime(0)
  timestamp        DateTime?                      @db.Timestamp(0)
  note             String?                        @db.LongText

  @@index([reason], map: "curier_name")
  @@index([curier_reason_id], map: "id")
}

model mm_geographies {
  id         Int                        @id @default(autoincrement())
  name       String                     @db.VarChar(255)
  is_actived mm_geographies_is_actived? @default(Y)
  is_deleted mm_geographies_is_deleted? @default(N)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model mm_holiday {
  id           Int                   @id @default(autoincrement())
  fisyear      String                @db.VarChar(4)
  date         DateTime              @db.Date
  description  String                @db.VarChar(255)
  status       String?               @db.VarChar(255)
  warehouse_id Int                   @default(0)
  is_deleted   mm_holiday_is_deleted @default(N)
  is_actived   mm_holiday_is_actived @default(Y)
  remask       String?               @db.VarChar(255)

  @@index([id, fisyear, description], map: "id")
}

model mm_image_document_return_details {
  detail_id  Int                                          @id @default(autoincrement())
  image_url  String?                                      @db.VarChar(200)
  id         Int?
  is_deleted mm_image_document_return_details_is_deleted? @default(N)
  status     mm_image_document_return_details_status?
}

model mm_image_document_returns {
  id          Int                                   @id @default(autoincrement())
  name        String?                               @db.VarChar(200)
  customer_id Int?
  is_deleted  mm_image_document_returns_is_deleted? @default(N)
}

model mm_ispickup_customer {
  id                    Int
  is_pickup_customer    mm_ispickup_customer_is_pickup_customer @default(N)
  pickup_shipper_retail String?                                 @db.VarChar(255)

  @@id([id, is_pickup_customer])
  @@index([id], map: "id")
  @@index([is_pickup_customer], map: "is_pickup_customer")
}

model mm_ispickup_shipper {
  id                    Int
  is_pickup_shipper     mm_ispickup_shipper_is_pickup_shipper @default(N)
  pickup_shipper_retail String?                               @db.VarChar(255)

  @@id([id, is_pickup_shipper])
  @@index([id], map: "id")
  @@index([is_pickup_shipper], map: "is_pickup_shipper")
}

model mm_l_edit_table_type {
  id          Int                               @id
  column_name String?                           @db.VarChar(255)
  name        String?                           @db.VarChar(200)
  is_actived  mm_l_edit_table_type_is_actived?  @default(Y)
  is_deleted  mm_l_edit_table_type_is_deleted?  @default(N)
  is_business mm_l_edit_table_type_is_business? @default(N)
  is_express  mm_l_edit_table_type_is_express?  @default(N)
}

model mm_line_id {
  user_id      Int                    @default(autoincrement())
  phone_number String                 @db.VarChar(15)
  line_uid     String                 @db.VarChar(50)
  email        String?                @db.VarChar(255)
  is_actived   mm_line_id_is_actived? @default(Y)
  is_deleted   mm_line_id_is_deleted? @default(Y)
  updated_date DateTime?              @db.DateTime(0)

  @@id([user_id, phone_number])
  @@unique([line_uid, email], map: "line_uid")
  @@index([user_id], map: "user_id")
}

model mm_line_message {
  user_id      Int                         @default(autoincrement())
  phone_number String                      @db.VarChar(15)
  line_uid     String                      @db.VarChar(50)
  email        String?                     @db.VarChar(255)
  is_actived   mm_line_message_is_actived? @default(Y)
  is_deleted   mm_line_message_is_deleted? @default(Y)
  updated_date DateTime?                   @db.DateTime(0)

  @@id([user_id, phone_number])
  @@unique([line_uid, email], map: "line_uid")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model mm_package_business {
  id                        Int                                     @id @default(autoincrement())
  package_detail_code       String?                                 @db.VarChar(255)
  package_detail_name       String?                                 @db.VarChar(255)
  unit_id                   Int?
  is_deleted                mm_package_business_is_deleted?         @default(N)
  size_min                  Int?
  size_max                  Int?
  weight_min                Decimal?                                @db.Decimal(32, 2)
  weight_max                Decimal?                                @db.Decimal(32, 2)
  cost                      Decimal?                                @db.Decimal(32, 16)
  is_actived                mm_package_business_is_actived?         @default(N)
  cost_difference_warehouse Decimal?                                @db.Decimal(32, 16)
  create_user_id            Int?
  update_user_id            Int?
  created_date              DateTime?                               @default(now()) @db.DateTime(0)
  updated_date              DateTime?                               @default(now()) @db.DateTime(0)
  is_document_return        mm_package_business_is_document_return? @default(N)
  cost_go                   Decimal?                                @db.Decimal(32, 16)
  cost_return               Decimal?                                @db.Decimal(32, 16)
  is_weight_fix             mm_package_business_is_weight_fix?      @default(N)
  is_vat                    mm_package_business_is_vat?             @default(N)
  package_id                Int?

  @@index([package_id], map: "package_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model mm_package_businessxxx {
  id                        Int                                        @id @default(autoincrement())
  package_detail_code       String?                                    @db.VarChar(255)
  package_detail_name       String?                                    @db.VarChar(255)
  unit_id                   Int?
  is_deleted                mm_package_businessxxx_is_deleted?         @default(N)
  size_min                  Int?
  size_max                  Int?
  weight_min                Int?
  weight_max                Int?
  cost                      Decimal?                                   @db.Decimal(32, 16)
  is_actived                mm_package_businessxxx_is_actived?         @default(N)
  cost_difference_warehouse Decimal?                                   @db.Decimal(32, 16)
  create_user_id            Int?
  update_user_id            Int?
  created_date              DateTime?                                  @default(now()) @db.DateTime(0)
  updated_date              DateTime?                                  @default(now()) @db.DateTime(0)
  is_document_return        mm_package_businessxxx_is_document_return? @default(N)
  cost_go                   Decimal?                                   @db.Decimal(32, 16)
  cost_return               Decimal?                                   @db.Decimal(32, 16)
  is_weight_fix             mm_package_businessxxx_is_weight_fix?      @default(N)
  is_vat                    mm_package_businessxxx_is_vat?             @default(N)
  package_id                Int?

  @@index([package_id], map: "package_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model mm_package_details {
  package_detail_id  Int                                    @id @default(autoincrement())
  package_id         Int?
  size_name          String?                                @db.VarChar(255)
  unit_id            Int?
  cost               Decimal?                               @db.Decimal(32, 16)
  size_height        Int?
  size_width         Int?
  size_length        Int?
  weight             Decimal?                               @db.Decimal(32, 16)
  is_deleted         mm_package_details_is_deleted?         @default(N)
  create_user_id     Int?
  update_user_id     Int?
  create_date        DateTime?                              @default(now()) @db.DateTime(0)
  update_date        DateTime?                              @default(now()) @db.DateTime(0)
  is_document_return mm_package_details_is_document_return? @default(N)
  cost_go            Decimal?                               @db.Decimal(32, 16)
  cost_return        Decimal?                               @db.Decimal(32, 16)
  is_weight_fix      mm_package_details_is_weight_fix?      @default(N)
  is_vat             mm_package_details_is_vat?             @default(N)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model mm_package_express {
  id                        Int                            @id @default(autoincrement())
  package_detail_code       String?                        @db.VarChar(255)
  package_detail_name       String?                        @db.VarChar(255)
  unit_id                   Int?
  is_deleted                mm_package_express_is_deleted? @default(N)
  size_min                  Int?
  size_max                  Int?
  weight_min                Int?
  weight_max                Int?
  cost                      Decimal?                       @db.Decimal(32, 16)
  is_actived                mm_package_express_is_actived? @default(N)
  cost_difference_warehouse Decimal?                       @db.Decimal(32, 16)
  package_id                Int?
  package_setting_id        Int?
  cost_go                   Int?
  cost_return               Int?
  create_user_id            Int?
  is_document_return        String?                        @db.VarChar(1)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model mm_package_express_settings {
  id                        Int                                     @id @default(autoincrement())
  code                      String?                                 @db.VarChar(255)
  name                      String?                                 @db.VarChar(255)
  unit_id                   Int?
  is_deleted                mm_package_express_settings_is_deleted? @default(N)
  size_min                  Int?
  size_max                  Int?
  weight_min                Int?
  weight_max                Int?
  cost                      Decimal?                                @db.Decimal(32, 16)
  is_actived                mm_package_express_settings_is_actived? @default(N)
  cost_difference_warehouse Decimal?                                @db.Decimal(32, 16)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model mm_packages {
  package_id         Int                             @id @default(autoincrement())
  package_code       String?                         @db.VarChar(30)
  package_name       String?                         @db.VarChar(255)
  customer_id        Int?
  is_deleted         mm_packages_is_deleted?         @default(N)
  create_user_id     Int?
  update_user_id     Int?
  create_date        DateTime?                       @default(now()) @db.DateTime(0)
  update_date        DateTime?                       @default(now()) @db.DateTime(0)
  is_document_return mm_packages_is_document_return? @default(N)
  type               mm_packages_type?               @default(BUSINESS)
  is_actived         mm_packages_is_actived?         @default(Y)
  pay_commission     String?                         @db.VarChar(2)
}

model mm_payments {
  id          Int                      @id
  name        String?                  @db.VarChar(200)
  is_actived  mm_payments_is_actived?  @default(Y)
  is_deleted  mm_payments_is_deleted?  @default(N)
  is_business mm_payments_is_business? @default(N)
  is_express  mm_payments_is_express?  @default(N)
}

model mm_price_fast12 {
  id         Int                         @id @default(autoincrement())
  name       String                      @db.VarChar(255)
  cost_bill  Decimal?                    @db.Decimal(32, 2)
  is_actived mm_price_fast12_is_actived? @default(Y)
  is_deleted mm_price_fast12_is_deleted? @default(N)
}

model mm_product_group_status {
  id   Int     @id
  name String? @db.VarChar(255)
}

model mm_product_status {
  id              Int     @id
  name            String? @db.VarChar(255)
  group_status_id Int?
}

model mm_province {
  id           Int                     @id @default(autoincrement())
  code         String                  @db.VarChar(2)
  name_th      String                  @db.VarChar(150)
  name_en      String                  @db.VarChar(150)
  geography_id Int                     @default(0)
  is_deleted   mm_province_is_deleted? @default(N)
  is_actived   mm_province_is_actived? @default(Y)
}

model mm_province_zone {
  id           Int                          @id @default(autoincrement())
  code         String                       @db.VarChar(2)
  name_th      String                       @db.VarChar(150)
  name_en      String                       @db.VarChar(150)
  geography_id Int                          @default(0)
  is_deleted   mm_province_zone_is_deleted? @default(N)
  is_actived   mm_province_zone_is_actived? @default(Y)
  zone         String?                      @db.VarChar(2)
}

model mm_reason_send_problems {
  reason_send_problem_id     Int     @id @default(autoincrement())
  reason_send_problem_detail String? @db.VarChar(255)
}

model mm_reason_sends {
  reason_id  Int                         @id @default(autoincrement())
  detail     String?                     @db.VarChar(200)
  is_deleted mm_reason_sends_is_deleted? @default(N)
  resends    tm_resends[]  @relation("ReasonToResends")
}

model mm_receive_types {
  id         Int                          @id @default(autoincrement())
  name       String?                      @db.VarChar(255)
  is_deleted mm_receive_types_is_deleted? @default(N)
}

model mm_recipient_details {
  recipient_detail_id   Int                              @id @default(autoincrement())
  recipient_id          Int?
  recipient_detail_name String?                          @db.VarChar(255)
  address               String?                          @db.VarChar(1000)
  tambon_id             Int?
  ampur_id              Int?
  province_id           Int?
  zip_code              String?                          @db.VarChar(255)
  tel1                  String?                          @db.VarChar(10)
  tel1_ext              String?                          @db.VarChar(10)
  tel2                  String?                          @db.VarChar(10)
  tel2_ext              String?                          @db.VarChar(10)
  is_deleted            mm_recipient_details_is_deleted? @default(N)
  is_default            mm_recipient_details_is_default? @default(N)
  line_id               String?                          @db.VarChar(100)
  longitude             String?                          @db.VarChar(100)
  latitude              String?                          @db.VarChar(100)

  @@index([ampur_id], map: "ampur_id")
  @@index([is_default], map: "is_default")
  @@index([is_deleted], map: "is_deleted")
  @@index([province_id], map: "province_id")
  @@index([recipient_id], map: "recipient_id")
  @@index([tambon_id], map: "tambon_id")
}

model mm_recipient_types {
  id         Int                            @id @default(autoincrement())
  name       String?                        @db.VarChar(255)
  is_deleted mm_recipient_types_is_deleted? @default(N)
}

model mm_recipient_walkins {
  recipient_id   Int     @id @default(autoincrement())
  recipient_name String? @db.VarChar(255)
  customer_id    Int?
  address        String? @db.VarChar(255)
  tambon_id      Int?
  ampur_id       Int?
  province_id    Int?
  zip_code       String? @db.VarChar(10)
  tel            String? @db.VarChar(50)

  @@unique([recipient_name, customer_id], map: "duplicateRecipientName")
  @@index([tel], map: "idx-tel")
}

model mm_recipients {
  recipient_id      Int                       @id @default(autoincrement())
  recipient_code    String?                   @db.VarChar(50)
  recipient_type_id Int?
  recipient_name    String?                   @db.VarChar(255)
  customer_id       Int?
  is_deleted        mm_recipients_is_deleted? @default(N)

  @@unique([recipient_code, customer_id], map: "duplicateRecipientCode")
  @@index([customer_id], map: "idx-customerId")
  @@index([is_deleted], map: "idx-is_deleted")
  @@index([recipient_name], map: "idx-recipient_name")
}

model mm_report_customers {
  id          Int                             @id @default(autoincrement())
  name        String?                         @db.VarChar(1000)
  url         String?                         @db.VarChar(1000)
  detail      String?                         @db.Text
  is_deleted  mm_report_customers_is_deleted? @default(N)
  customer_id Int?
}

model mm_route_detail_days {
  id              Int     @id @default(autoincrement())
  route_detail_id Int?
  day             String? @default("N") @db.VarChar(255)
}

model mm_route_details {
  route_detail_id Int  @id @default(autoincrement())
  route_id        Int?
  tambon_id       Int?

  @@index([route_id], map: "mm_route_details_route_id_IDX")
  @@index([tambon_id], map: "mm_route_details_tambon_id_IDX")
}

model mm_route_details_old {
  route_detail_id Int     @id @default(autoincrement())
  route_id        Int?
  zip_code        String? @db.VarChar(50)
  tambon_id       Int?
}

model mm_route_detailsxxx {
  route_detail_id Int  @id @default(autoincrement())
  route_id        Int?
  tambon_id       Int?

  @@index([route_id], map: "mm_route_details_route_id_IDX")
  @@index([tambon_id], map: "mm_route_details_tambon_id_IDX")
}

model mm_routes {
  route_id     Int                   @id @default(autoincrement())
  warehouse_id Int?
  route_code   String?               @db.VarChar(10)
  is_deleted   mm_routes_is_deleted? @default(N)
  route_name   String?               @db.VarChar(150)
  cost_oil     Decimal?              @db.Decimal(32, 2)

  @@index([route_code], map: "mm_routes_idx_route_code")
  @@index([is_deleted], map: "mm_routes_is_deleted_IDX")
  @@index([route_code], map: "mm_routes_route_code_IDX")
}

model mm_routes_2024_11_19 {
  route_id     Int                              @id @default(autoincrement())
  warehouse_id Int?
  route_code   String?                          @db.VarChar(10)
  is_deleted   mm_routes_2024_11_19_is_deleted? @default(N)
  route_name   String?                          @db.VarChar(150)
  cost_oil     Decimal?                         @db.Decimal(32, 2)

  @@index([route_code], map: "mm_routes_idx_route_code")
  @@index([is_deleted], map: "mm_routes_is_deleted_IDX")
  @@index([route_code], map: "mm_routes_route_code_IDX")
  @@map("mm_routes_2024-11-19")
}

model mm_routes_dc_to_dc {
  route_id         Int                            @id @default(autoincrement())
  src_warehouse_id Int?
  is_deleted       mm_routes_dc_to_dc_is_deleted? @default(N)
  dst_warehouse_id Int?
  num_days         Int?
}

model mm_routes_old {
  route_id     Int                       @id @default(autoincrement())
  warehouse_id Int?
  route_code   String?                   @db.VarChar(10)
  is_deleted   mm_routes_old_is_deleted? @default(N)
  route_name   String?                   @db.VarChar(50)
}

model mm_shipper_types {
  id         Int                          @id @default(autoincrement())
  name       String?                      @db.VarChar(255)
  is_deleted mm_shipper_types_is_deleted? @default(N)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model mm_shippers {
  shipper_id      Int                     @id @default(autoincrement())
  shipper_code    String?                 @db.VarChar(50)
  shipper_type_id Int?
  shipper_name    String?                 @db.VarChar(255)
  address         String?                 @db.Text
  tambon_id       Int?
  ampur_id        Int?
  province_id     Int?
  zip_code        String?                 @db.VarChar(255)
  tel             String?                 @db.VarChar(10)
  fax             String?                 @db.VarChar(255)
  customer_id     Int?
  is_deleted      mm_shippers_is_deleted? @default(N)
  longitude       String?                 @db.VarChar(20)
  latitude        String?                 @db.VarChar(20)
  priority        Int?                    @default(0)

  @@unique([shipper_code, customer_id], map: "duplicateShipperCode")
}

model mm_tambon {
  id          Int                    @id @default(autoincrement())
  zip_code    Int
  name_th     String                 @db.VarChar(150)
  name_en     String                 @db.VarChar(150)
  ampur_id    Int                    @default(0)
  is_deleted  mm_tambon_is_deleted?  @default(N)
  is_actived  mm_tambon_is_actived?  @default(Y)
  tambon_paid mm_tambon_tambon_paid? @default(N)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model mm_tambon_days {
  id            Int                        @default(autoincrement())
  tambon_id     Int
  day           String?                    @default("N") @db.VarChar(255)
  dayNo         String?                    @db.VarChar(10)
  lastupdate    DateTime?                  @db.Timestamp(0)
  count         mm_tambon_days_count?      @default(dbgenerated("1"))
  tambon_id_day String                     @unique(map: "tambon_id_day") @db.VarChar(255)
  zip_code      Int?
  name_th       String?                    @db.VarChar(150)
  is_deleted    mm_tambon_days_is_deleted?
  is_actived    mm_tambon_days_is_actived?

  @@id([id, tambon_id_day, tambon_id])
  @@index([id], map: "id")
  @@index([tambon_id], map: "tambon_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model mm_tambon_days_2024_11_07 {
  id            Int                                   @default(autoincrement())
  tambon_id     Int
  day           String?                               @default("N") @db.VarChar(255)
  dayNo         String?                               @db.VarChar(10)
  lastupdate    DateTime?                             @db.Timestamp(0)
  count         mm_tambon_days_2024_11_07_count?      @default(dbgenerated("1"))
  tambon_id_day String                                @unique(map: "tambon_id_day") @db.VarChar(255)
  zip_code      Int?
  name_th       String?                               @db.VarChar(150)
  is_deleted    mm_tambon_days_2024_11_07_is_deleted?
  is_actived    mm_tambon_days_2024_11_07_is_actived?

  @@id([id, tambon_id_day, tambon_id])
  @@index([id], map: "id")
  @@index([tambon_id], map: "tambon_id")
  @@map("mm_tambon_days_2024-11-07")
}

model mm_tambon_days_bak {
  id        Int     @id @default(autoincrement())
  tambon_id Int?
  day       String? @default("N") @db.VarChar(255)
  dayNo     String? @db.VarChar(10)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model mm_tambon_days_old_2024_08_06_ {
  id            Int                                        @default(autoincrement())
  tambon_id     Int
  day           String?                                    @default("N") @db.VarChar(255)
  dayNo         String?                                    @db.VarChar(10)
  lastupdate    DateTime?                                  @db.Timestamp(0)
  count         mm_tambon_days_old_2024_08_06__count?      @default(dbgenerated("1"))
  tambon_id_day String                                     @unique(map: "tambon_id_day") @db.VarChar(255)
  zip_code      Int?
  name_th       String?                                    @db.VarChar(150)
  is_deleted    mm_tambon_days_old_2024_08_06__is_deleted?
  is_actived    mm_tambon_days_old_2024_08_06__is_actived?

  @@id([id, tambon_id_day, tambon_id])
  @@index([id], map: "id")
  @@index([tambon_id], map: "tambon_id")
  @@map("mm_tambon_days_old(2024-08-06)")
}

model mm_truck_status {
  id              Int    @id @default(autoincrement())
  truck_status    String @db.VarChar(255)
  truck_status_th String @db.VarChar(255)

  @@index([truck_status], map: "truck_status")
}

model mm_truck_types {
  id         Int                        @id @default(autoincrement())
  name       String?                    @db.VarChar(200)
  is_deleted mm_truck_types_is_deleted? @default(N)

  @@index([id], map: "mm_truck_types_idx_id")
  @@index([is_deleted], map: "mm_truck_types_is_deleted_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model mm_trucks {
  id                            Int                   @id @default(autoincrement())
  truck_number                  String?               @db.VarChar(200)
  license_plate                 String?               @db.VarChar(20)
  province_id                   Int?
  brand                         String?               @db.VarChar(200)
  truck_type_id                 Int?
  model                         String?               @db.VarChar(200)
  color                         String?               @db.VarChar(200)
  warehouse_id                  Int?
  is_deleted                    mm_trucks_is_deleted? @default(N)
  is_actived                    mm_trucks_is_actived? @default(Y)
  type                          mm_trucks_type?       @default(DELIVERY)
  fleet_card                    String?               @db.VarChar(200)
  driver_picture                String?               @db.VarChar(255)
  qr_payments                   String?               @db.VarChar(255)
  year                          String?               @db.VarChar(200)
  vehicle_identification_number String?               @db.VarChar(200)
  referent_number               String?               @db.VarChar(200)
  date_of_purchase              String?               @db.VarChar(200)
  remark                        String?               @db.VarChar(255)
  loading_weight                String?               @db.VarChar(200)
  maximum_loading_volume        String?               @db.VarChar(200)
  pallet_quantity               String?               @db.VarChar(200)
  transportation_service        String?               @db.VarChar(200)
  contract_period               String?               @db.VarChar(200)
  condition_details             String?               @db.VarChar(255)

  @@index([id], map: "mm_trucks_idx_id")
  @@index([province_id], map: "province_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model mm_units {
  unit_id        Int                  @id @default(autoincrement())
  unit_name      String?              @db.VarChar(255)
  is_deleted     mm_units_is_deleted? @default(N)
  create_user_id Int?
  update_user_id Int?
  create_date    DateTime?            @default(now()) @db.DateTime(0)
  update_date    DateTime?            @default(now()) @db.DateTime(0)
}

model mm_warehouse_details {
  warehouse_detail_id Int  @id @default(autoincrement())
  warehouse_id        Int?
  tambon_id           Int? @unique(map: "tambon_id")

  @@index([warehouse_detail_id], map: "id")
}

model mm_warehouse_mapping {
  id                   Int                              @id @default(autoincrement())
  warehouse_id         Int?
  zip_code_            String?                          @db.VarChar(10)
  is_deleted           mm_warehouse_mapping_is_deleted? @default(N)
  tambon_id            Int?
  cost_island          Int?
  business_cost_island Int?

  @@index([id], map: "id")
  @@index([tambon_id], map: "idx-tambon_id")
}

model mm_warehouse_mapping_old {
  id           Int                                  @id @default(autoincrement())
  warehouse_id Int?
  zip_code     String?                              @db.VarChar(10)
  is_deleted   mm_warehouse_mapping_old_is_deleted? @default(N)
  tambon_id    Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model mm_warehouse_num_days {
  id               Int                               @id @default(autoincrement())
  src_warehouse_id Int?
  dst_warehouse_id Int?
  num_days         Int?
  is_deleted       mm_warehouse_num_days_is_deleted? @default(N)
  warehouse_id     Int?
}

model mm_warehouse_oil {
  id           BigInt    @id @unique(map: "id") @default(autoincrement()) @db.UnsignedBigInt
  warehouse_id Int?
  oil_date     DateTime? @db.Date
  oil_price    Float?
}

model mm_warehouse_subs {
  id           Int                           @id @default(autoincrement())
  warehouse_id Int?
  name         String?                       @db.VarChar(255)
  is_deleted   mm_warehouse_subs_is_deleted? @default(N)
}

model mm_warehouses_to {
  warehouse_id        Int                          @id @default(autoincrement())
  warehouse_code      String?                      @db.VarChar(20)
  warehouse_name      String?                      @db.VarChar(200)
  warehouse_name_show String?                      @db.VarChar(200)
  label_name          String?                      @db.VarChar(200)
  is_actived          mm_warehouses_to_is_actived? @default(Y)
  is_deleted          mm_warehouses_to_is_deleted? @default(N)
  longitude           Float?                       @db.Float
  latitude            Float?                       @db.Float

  @@index([warehouse_id], map: "mm_warehouses_idx_warehouse_id")
}

model mm_warehouses_too {
  warehouse_id        Int                           @id @default(autoincrement())
  warehouse_code      String?                       @db.VarChar(20)
  warehouse_name      String?                       @db.VarChar(200)
  warehouse_name_show String?                       @db.VarChar(200)
  label_name          String?                       @db.VarChar(200)
  is_actived          mm_warehouses_too_is_actived? @default(Y)
  is_deleted          mm_warehouses_too_is_deleted? @default(N)
  longitude           Float?                        @db.Float
  latitude            Float?                        @db.Float

  @@index([warehouse_id], map: "mm_warehouses_idx_warehouse_id")
}

model nm_damage_products {
  damage_id       Int                           @id @default(autoincrement())
  damage_date     DateTime?                     @db.Date
  damage_subject  String?                       @db.VarChar(255)
  damage_detail   String?                       @db.VarChar(1000)
  damage_location String?                       @db.VarChar(1000)
  is_deleted      nm_damage_products_is_deleted @default(N)
  create_date     DateTime?                     @default(now()) @db.Timestamp(0)
  update_date     DateTime?                     @db.Timestamp(0)
  create_user_id  Int
  serial_id       String?                       @db.VarChar(64)
}

model nm_damage_products_details {
  receive_id  Int
  serial_no   String    @db.VarChar(50)
  damage_id   Int
  create_date DateTime? @default(now()) @db.Timestamp(0)

  @@id([receive_id, serial_no])
}

model nm_gasolines {
  gasoline_id Int                       @id @default(autoincrement())
  user_id     Int?
  receive_id  Int?
  is_deleted  nm_gasolines_is_deleted?  @default(N)
  is_approved nm_gasolines_is_approved? @default(N)
}

model nm_lost_product {
  lost_id        Int                        @id @default(autoincrement())
  lost_date      DateTime?                  @db.Date
  lost_subject   String?                    @db.VarChar(255)
  lost_detail    String?                    @db.VarChar(1000)
  lost_location  String?                    @db.VarChar(1000)
  receive_id     Int?
  qty            Int?
  is_deleted     nm_lost_product_is_deleted @default(N)
  create_date    DateTime?                  @default(now()) @db.Timestamp(0)
  update_date    DateTime?                  @db.Timestamp(0)
  create_user_id Int
  serial_id      String?                    @db.VarChar(64)
}

model pm_posts {
  post_id          Int                 @id @default(autoincrement())
  customer_id      Int?
  user_customer_id Int?
  admin_id         Int?
  text             String?             @db.Text
  image_url        String?             @db.VarChar(200)
  datetime         DateTime?           @default(now()) @db.DateTime(0)
  user_type        pm_posts_user_type?
  document_id      String?             @db.VarChar(64)
  data_year        Int?
  data_yearmonth   Int?

  @@index([data_year], map: "idx_data_year")
  @@index([data_yearmonth], map: "idx_data_yearmonth")
}

model projectphase {
  projectphaseid                         String  @id @db.VarChar(100)
  description                            String? @db.VarChar(200)
  tasks_tasks_projectphaseToprojectphase tasks[] @relation("tasks_projectphaseToprojectphase")
}

model projectpriority {
  priorityid                                         String     @id @db.VarChar(100)
  description                                        String?    @db.VarChar(200)
  projects_projects_projectpriorityToprojectpriority projects[] @relation("projects_projectpriorityToprojectpriority")
}

model projects {
  projectid                                                 Int               @id @default(autoincrement())
  recordowner                                               String            @default("") @db.VarChar(200)
  projectlead                                               Int
  lastmodifiedby                                            Int
  projectname                                               String?           @db.VarChar(255)
  company                                                   Int?
  contactname                                               String?           @db.VarChar(200)
  summary                                                   String?           @db.Text
  estimatedprojectstart                                     DateTime?         @db.Date
  estimatedprojectend                                       DateTime?         @db.Date
  projectpriority                                           String?           @db.VarChar(100)
  projectstatus                                             String?           @db.VarChar(100)
  notes                                                     String?           @db.Text
  startdate                                                 DateTime?         @db.Date
  plannedfinishdate                                         DateTime?         @db.Date
  percentecomplete                                          Int?
  customercontact                                           Int?
  actualcost                                                Decimal?          @db.Decimal(20, 3)
  estimatedcost                                             Decimal?          @db.Decimal(20, 3)
  actualhours                                               Int?
  estimetedhours                                            Int?
  percentoverbudget                                         Int?
  datecreated                                               DateTime?         @db.DateTime(0)
  datemodified                                              DateTime?         @db.DateTime(0)
  documentlibrary                                           documentlibrary[]
  issues                                                    issues[]
  projectpriority_projects_projectpriorityToprojectpriority projectpriority?  @relation("projects_projectpriorityToprojectpriority", fields: [projectpriority], references: [priorityid], onDelete: NoAction, onUpdate: NoAction, map: "projects_ibfk_1")
  projectstatus_projects_projectstatusToprojectstatus       projectstatus?    @relation("projects_projectstatusToprojectstatus", fields: [projectstatus], references: [statusid], onDelete: NoAction, onUpdate: NoAction, map: "projects_ibfk_2")
  tasks                                                     tasks[]
  timecard                                                  timecard[]

  @@index([customercontact], map: "customercontact")
  @@index([projectpriority], map: "projectpriority")
  @@index([projectstatus], map: "projectstatus")
}

model projectstatus {
  statusid                                       String     @id @db.VarChar(100)
  description                                    String?    @db.VarChar(200)
  projects_projects_projectstatusToprojectstatus projects[] @relation("projects_projectstatusToprojectstatus")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model r_from_customer {
  id                 Int       @id @default(autoincrement())
  from_customer_code String?   @db.VarChar(20)
  customer_id        Int?
  warehouse_id       Int?
  license_plate      String?   @db.VarChar(50)
  create_date        DateTime? @default(now()) @db.DateTime(0)
  truck_tel          String?   @db.VarChar(30)
  truck_name         String?   @db.VarChar(200)
  document1          String?   @db.VarChar(200)
  document2          String?   @db.VarChar(200)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model r_from_customer_details {
  id               Int     @id @default(autoincrement())
  from_customer_id Int?
  serial_id        String? @db.VarChar(64)
  receive_id       Int?

  @@index([from_customer_id], map: "r_from_customer_details_from_customer_id_IDX")
  @@index([receive_id], map: "r_from_customer_details_receive_id_IDX")
  @@index([serial_id], map: "r_from_customer_details_serial_id_IDX")
}

model report_toll_dally {
  id             Int       @default(autoincrement())
  receive_date   DateTime? @db.DateTime(0)
  receive_code   String    @db.VarChar(50)
  reference_no   String?   @db.VarChar(200)
  serial_no      String?   @db.VarChar(50)
  weight         Decimal?  @db.Decimal(32, 2)
  width          Decimal?  @db.Decimal(32, 2)
  length         Decimal?  @db.Decimal(32, 2)
  height         Decimal?  @db.Decimal(32, 2)
  package_name   String?   @db.VarChar(255)
  recipient_name String?   @db.VarChar(255)
  tambon_name    String?   @db.VarChar(150)
  ampur_name     String?   @db.VarChar(150)
  province_name  String?   @db.VarChar(150)
  zip_code       String?   @db.VarChar(5)
  tel            String?   @db.VarChar(255)
  status_id      Int?
  status_message String?   @db.VarChar(255)
  datetime       DateTime? @db.DateTime(0)
  customer_id    Int?
  longitude      String?   @db.VarChar(20)
  latitude       String?   @db.VarChar(20)
  send_id        String?   @db.VarChar(255)
  sign_name      String?   @db.VarChar(1000)
  image_doc      String?   @db.VarChar(1000)
  document_id    String?   @db.VarChar(255)
  pic1           String?   @db.VarChar(255)
  pic2           String?   @db.VarChar(255)
  pic3           String?   @db.VarChar(255)
  pic4           String?   @db.VarChar(255)
  pic5           String?   @db.VarChar(255)
  data_year      Int?
  data_yearmonth Int?

  @@id([id, receive_code])
  @@index([id], map: "id")
  @@index([data_year], map: "idx_data_year")
  @@index([data_yearmonth], map: "idx_data_yearmonth")
  @@index([receive_code, serial_no], map: "sn")
}

model resources {
  resourceid     Int         @id @default(autoincrement())
  fld_name       String?     @db.VarChar(100)
  bussinesphone  String?     @db.VarChar(30)
  skillset       String?     @db.Text
  rate           Decimal?    @db.Decimal(20, 3)
  notes          String?     @db.Text
  jobtitle       String?     @db.VarChar(100)
  reportsto      String?     @db.VarChar(100)
  departmentid   Int?
  companyid      Int?
  bussinessfax   String?     @db.VarChar(30)
  photo          Bytes?      @db.Blob
  datecreated    DateTime?   @db.Date
  datemodified   DateTime?   @db.Date
  recordowner    Int?
  lastmodifiedby Int?
  login          String?     @db.VarChar(255)
  pwd            String?     @db.VarChar(255)
  department     department? @relation(fields: [departmentid], references: [departmentid], onDelete: NoAction, onUpdate: NoAction, map: "resources_ibfk_1")
  company        company?    @relation(fields: [companyid], references: [companyid], onDelete: NoAction, onUpdate: NoAction, map: "resources_ibfk_2")
  timecard       timecard[]

  @@index([companyid], map: "companyid")
  @@index([departmentid], map: "departmentid")
}

model sys_login_logs {
  id              Int                        @id @default(autoincrement())
  username        String?                    @db.VarChar(100)
  user_id         Int?
  login_time      DateTime?                  @default(now()) @db.Timestamp(0)
  status          sys_login_logs_status?
  is_mobile       sys_login_logs_is_mobile?  @default(N)
  is_tablet       sys_login_logs_is_tablet?  @default(N)
  is_desktop      sys_login_logs_is_desktop? @default(N)
  public_ip       String?                    @db.VarChar(16)
  browser         String?                    @db.VarChar(100)
  browser_version String?                    @db.VarChar(250)
  host            String?                    @db.VarChar(200)
  os              String?                    @db.VarChar(200)
  os_version      String?                    @db.VarChar(200)
  userAgent       String?                    @db.VarChar(1000)

  @@index([status], map: "sys_login_logs_status_IDX")
  @@index([user_id], map: "sys_login_logs_user_id_IDX")
  @@index([username], map: "sys_login_logs_username_IDX")
}

model sys_post_customer_time {
  id         Int       @id @default(autoincrement())
  day        String    @db.VarChar(20)
  day_th     String?   @db.VarChar(255)
  time_open  DateTime? @db.Time(0)
  time_close DateTime? @db.Time(0)
  note       String?   @db.Text
}

model sys_serial_receives {
  customer_id Int
  no          Int?
  year        String @db.VarChar(255)

  @@id([customer_id, year])
}

model sys_serials {
  serial_id Int     @id @default(autoincrement())
  serial_no Int?
  type      String? @db.VarChar(10)
  format    String? @db.VarChar(50)
  prefix    String? @db.VarChar(20)
  length    Int?

  @@index([type], map: "type-idx")
}

model sys_token {
  token_id    String                @db.Text
  token       String                @db.Text
  is_actived  sys_token_is_actived? @default(Y)
  user_id     Int?
  create_time DateTime?             @default(now()) @db.DateTime(0)
  update_time DateTime?             @db.DateTime(0)
  id          Int                   @id @default(autoincrement())

  @@index([is_actived], map: "sys_token_is_actived_IDX")
  @@index([user_id], map: "sys_token_user_id_IDX")
}

model tambon_latlng {
  AD_LEVEL   String? @db.VarChar(255)
  TA_ID      String? @db.VarChar(255)
  TAMBON_T   String? @db.VarChar(255)
  TAMBON_E   String? @db.VarChar(255)
  AM_ID      String? @db.VarChar(255)
  AMPHOE_T   String? @db.VarChar(255)
  AMPHOE_E   String? @db.VarChar(255)
  CH_ID      String? @db.VarChar(255)
  CHANGWAT_T String? @db.VarChar(255)
  CHANGWAT_E String? @db.VarChar(255)
  LAT        String? @db.VarChar(255)
  LNG        String? @db.VarChar(255)
  ID         Int     @id @default(autoincrement())
}

model taskpriority {
  priorityid                             String  @id @db.VarChar(100)
  description                            String? @db.VarChar(200)
  tasks_tasks_taskpriorityTotaskpriority tasks[] @relation("tasks_taskpriorityTotaskpriority")
}

model tasks {
  taskid                                        Int           @id @default(autoincrement())
  projectid                                     Int?
  projectphase                                  String?       @db.VarChar(100)
  taskname                                      String?       @db.VarChar(200)
  milestone                                     String?       @db.Char(1)
  description                                   String?       @db.Text
  duration                                      Int?
  startdate                                     DateTime?     @db.Date
  finishdate                                    DateTime?     @db.Date
  daysoverdue                                   Int?
  taskstatus                                    String?       @db.VarChar(100)
  percentcomplete                               Int?
  taskpriority                                  String?       @db.VarChar(100)
  assignedto                                    Int?
  historynote                                   String?       @db.Text
  datecreated                                   DateTime?     @db.Date
  datemodified                                  DateTime?     @db.Date
  recordowner                                   Int?
  lastmodifiedby                                Int?
  taskpredecessor                               String?       @db.VarChar(200)
  actualfinishdate                              DateTime?     @db.Date
  taskstatus_tasks_taskstatusTotaskstatus       taskstatus?   @relation("tasks_taskstatusTotaskstatus", fields: [taskstatus], references: [statusid], onDelete: NoAction, onUpdate: NoAction, map: "tasks_ibfk_1")
  taskpriority_tasks_taskpriorityTotaskpriority taskpriority? @relation("tasks_taskpriorityTotaskpriority", fields: [taskpriority], references: [priorityid], onDelete: NoAction, onUpdate: NoAction, map: "tasks_ibfk_2")
  projects                                      projects?     @relation(fields: [projectid], references: [projectid], onDelete: NoAction, onUpdate: NoAction, map: "tasks_ibfk_3")
  projectphase_tasks_projectphaseToprojectphase projectphase? @relation("tasks_projectphaseToprojectphase", fields: [projectphase], references: [projectphaseid], onDelete: NoAction, onUpdate: NoAction, map: "tasks_ibfk_4")

  @@index([projectid], map: "projectid")
  @@index([projectphase], map: "projectphase")
  @@index([taskpriority], map: "taskpriority")
  @@index([taskstatus], map: "taskstatus")
}

model taskstatus {
  statusid                           String  @id @db.VarChar(100)
  description                        String? @db.VarChar(200)
  tasks_tasks_taskstatusTotaskstatus tasks[] @relation("tasks_taskstatusTotaskstatus")
}

model temp_receive_last {
  delivery_note_id Int       @id
  receive_date     DateTime? @db.Date
  receive_by       Int?
  data_year        Int?
  data_yearmonth   Int?

  @@index([data_year], map: "idx_data_year")
  @@index([data_yearmonth], map: "idx_data_yearmonth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_test {
  receive_code        String? @db.VarChar(50)
  receive_business_id String? @db.VarChar(50)
  receive_date        String? @db.VarChar(50)
  receive_walkin_id   String? @db.VarChar(50)
  serial_id           String? @db.VarChar(50)
  serial_no           String? @db.VarChar(50)
  package_id          String? @db.VarChar(50)
  package_name        String? @db.VarChar(50)
  package_detail_id   String? @db.VarChar(50)
  package_detail_name String? @db.VarChar(50)
  customer_id         String  @id @db.VarChar(50)
  cost                String? @db.VarChar(50)
  status_id           String? @db.VarChar(50)
  to_warehouse_id     String? @db.VarChar(50)
  recipient_name      String? @db.VarChar(50)
  address             String? @db.VarChar(50)
  ampur_id            String? @db.VarChar(50)
  ampur_name          String? @db.VarChar(50)
  tambon_id           String? @db.VarChar(50)
  tambon_name         String? @db.VarChar(50)
  province_id         String? @db.VarChar(50)
  province_name       String? @db.VarChar(50)
  zip_code            String? @db.VarChar(50)
  tel                 String? @db.VarChar(50)
}

model temp_unique_api {
  send_id      String    @id @unique(map: "send_id") @db.VarChar(100)
  reference_no String?   @db.VarChar(100)
  create_date  DateTime? @default(now()) @db.DateTime(0)
}

model timecard {
  timecardid     Int        @id @default(autoincrement())
  projectid      Int?
  resourceid     Int?
  weekstartingon DateTime?  @db.Date
  sun            Int?
  mon            Int?
  tue            Int?
  wed            Int?
  thu            Int?
  fri            Int?
  sat            Int?
  datecreated    DateTime?  @db.Date
  datemodified   DateTime?  @db.Date
  recordowner    Int?
  lastmodifiedby Int?
  projects       projects?  @relation(fields: [projectid], references: [projectid], onDelete: NoAction, onUpdate: NoAction, map: "timecard_ibfk_1")
  resources      resources? @relation(fields: [resourceid], references: [resourceid], onDelete: NoAction, onUpdate: NoAction, map: "timecard_ibfk_2")

  @@index([projectid], map: "projectid")
  @@index([resourceid], map: "resourceid")
}

model tm_advice_no_sn {
  id           Int       @id @default(autoincrement())
  serial_no    String?   @db.VarChar(100)
  reference_no String?   @db.VarChar(200)
  create_date  DateTime? @db.DateTime(0)
}

model tm_avi_ampor_pay {
  id                Int       @default(autoincrement())
  receive_detail_id Int?
  receive_id        Int
  package_id        Int?
  cost_island       Decimal?  @db.Decimal(32, 2)
  cost_difference   Decimal?  @db.Decimal(32, 2)
  receive_date      DateTime? @db.DateTime(0)
  receive_code      String?   @db.VarChar(50)
  serial_no         String?   @db.VarChar(100)
  serial_id         String    @unique(map: "sn_id") @db.VarChar(64)
  recipient_code    String?   @db.VarChar(50)
  recipient_name    String?   @db.VarChar(255)
  customer_id       Int?
  timestamp         DateTime? @db.DateTime(0)

  @@id([id, receive_id, serial_id])
  @@index([id, receive_detail_id, receive_id], map: "id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_bill_details {
  bill_detail_id Int                     @id @default(autoincrement())
  bill_id        Int?
  receive_id     Int?
  is_pay         tm_bill_details_is_pay? @default(N)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_bills {
  bill_id      Int                    @id @default(autoincrement())
  bill_code    String?                @db.VarChar(100)
  bill_date    DateTime?              @db.Date
  customer_id  Int?
  is_completed tm_bills_is_completed? @default(N)
  is_deleted   tm_bills_is_deleted?   @default(N)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_book_details {
  book_detail_id Int                     @id @default(autoincrement())
  truck_id       Int?
  date_time      DateTime?               @db.DateTime(0)
  book_id        Int?
  status         tm_book_details_status? @default(PENDING)
  user_truck_id  Int?
  job_id         Int?

  @@index([book_id], map: "tm_book_details_book_id_IDX")
  @@index([date_time], map: "tm_book_details_date_time_IDX")
  @@index([status], map: "tm_book_details_status_IDX")
  @@index([truck_id], map: "tm_book_details_truck_id_IDX")
  @@index([user_truck_id], map: "tm_book_details_user_truck_id_IDX")
}

model tm_book_receives {
  id         Int  @id @default(autoincrement())
  receive_id Int?
  book_id    Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_books {
  book_id            Int                          @id @default(autoincrement())
  book_code          String?                      @db.VarChar(20)
  book_date          DateTime?                    @db.Date
  book_time          DateTime?                    @db.Time(0)
  qty                Int                          @default(1)
  truck_type_id      Int?
  create_date        DateTime?                    @default(now()) @db.DateTime(0)
  customer_id        Int?
  is_deleted         tm_books_is_deleted?         @default(N)
  is_approved        tm_books_is_approved?        @default(N)
  approve_date       DateTime?                    @db.DateTime(0)
  approve_people_id  Int?
  book_status        tm_books_book_status?
  cost               Int?
  confirm_cost       Int?
  warehouse_id       Int?
  address            String?                      @db.Text
  province_id        Int?
  ampur_id           Int?
  tambon_id          Int?
  is_pickup_customer tm_books_is_pickup_customer? @default(N)
  is_pickup_shipper  tm_books_is_pickup_shipper?  @default(N)
  is_invoices        tm_books_is_invoices?        @default(N)
  name               String?                      @db.VarChar(255)
  ampur_name         String?                      @db.VarChar(255)
  tambon_name        String?                      @db.VarChar(255)
  province_name      String?                      @db.VarChar(255)
  zipcode            String?                      @db.VarChar(10)
  tel                String?                      @db.VarChar(20)
  shipper_id         Int?
  receive_id         Int?
  updated_date       DateTime?                    @default(now()) @db.DateTime(0)

  @@index([shipper_id], map: "idx_shipper_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_budget_transaction_customers {
  transaction_id   Int                                         @id @default(autoincrement())
  transaction_type String?                                     @db.VarChar(200)
  amount           Decimal?                                    @db.Decimal(32, 16)
  status           String?                                     @db.VarChar(200)
  customer_id      Int?
  document_ref_id  Int?
  is_receive       tm_budget_transaction_customers_is_receive? @default(N)
  is_pay           tm_budget_transaction_customers_is_pay?     @default(N)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_budget_transaction_warehouses {
  transaction_id   Int      @id
  transaction_type String?  @db.VarChar(200)
  amount           Decimal? @db.Decimal(32, 16)
  status           String?  @db.VarChar(200)
  warehouse_id     Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_budget_transactions {
  transaction_id   Int      @id
  transaction_type String?  @db.VarChar(200)
  amount           Decimal? @db.Decimal(32, 16)
  status           String?  @db.VarChar(200)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_delivery_details {
  delivery_detail_id Int     @id @default(autoincrement())
  delivery_id        Int?
  receive_item_id    Int?
  tracking_no        String? @db.VarChar(50)
  serial_no          String? @db.VarChar(50)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_delivery_logs {
  id                 Int       @id @default(autoincrement())
  tracking_no        String?   @db.VarChar(0)
  receive_item_id    Int?
  serial_no          String?   @db.VarChar(0)
  warehouse_id       Int?
  delivery_status_id Int?
  datetime           DateTime? @default(now()) @db.DateTime(0)
  update_date        DateTime? @db.DateTime(0)
  data_year          Int?
  data_yearmonth     Int?

  @@index([data_year], map: "idx_data_year")
  @@index([data_yearmonth], map: "idx_data_yearmonth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_delivery_note_details {
  id               Int  @id @default(autoincrement())
  delivery_note_id Int?
  receive_id       Int?

  @@index([delivery_note_id], map: "idx-delivery_note_id")
  @@index([receive_id], map: "idx-receive_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_delivery_note_express {
  id             Int                                   @default(autoincrement())
  code           String?                               @db.VarChar(20)
  customer_id    Int
  warehouse_id   Int
  license_plate  String?                               @db.VarChar(50)
  create_date    DateTime?                             @default(now()) @db.DateTime(0)
  truck_tel      String?                               @db.VarChar(30)
  truck_name     String?                               @db.VarChar(200)
  is_deleted     tm_delivery_note_express_is_deleted?  @default(N)
  is_approved    tm_delivery_note_express_is_approved? @default(N)
  document1      String?                               @db.VarChar(200)
  document2      String?                               @db.VarChar(200)
  receive_count  Int?
  serial_count   Int?
  created_by     Int?
  receive_date   DateTime?                             @db.DateTime(0)
  receive_by     Int?
  updated_by     Int?
  updated_date   DateTime?                             @db.DateTime(0)
  status         String?                               @db.VarChar(20)
  data_year      Int?
  data_yearmonth Int?

  @@id([id, customer_id, warehouse_id])
  @@index([customer_id], map: "customer_id")
  @@index([customer_id], map: "idx-customer_id")
  @@index([warehouse_id], map: "idx-warehouse_id")
  @@index([data_year], map: "idx_data_year")
  @@index([data_yearmonth], map: "idx_data_yearmonth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_delivery_note_express_details {
  id                Int  @id @default(autoincrement())
  delivery_note_id  Int?
  receive_walkin_id Int?

  @@index([id], map: "id")
  @@index([delivery_note_id], map: "idx-delivery_note_id")
  @@index([receive_walkin_id], map: "idx-receive_id")
}

model tm_delivery_note_express_serials {
  id                Int                                          @default(autoincrement())
  delivery_note_id  Int
  serial_id         String                                       @db.VarChar(64)
  is_receive        tm_delivery_note_express_serials_is_receive? @default(N)
  created_date      DateTime?                                    @default(now()) @db.DateTime(0)
  receive_date      DateTime?                                    @db.DateTime(0)
  receive_walkin_id Int?
  receive_by        Int?
  customer_id       Int?

  @@id([delivery_note_id, id, serial_id])
  @@index([delivery_note_id], map: "deliver_note_id")
  @@index([id], map: "id")
  @@index([receive_walkin_id], map: "receice_id")
  @@index([serial_id], map: "serial_id")
}

model tm_delivery_note_serials {
  id               Int                                  @id @default(autoincrement())
  delivery_note_id Int?
  serial_id        String?                              @db.VarChar(64)
  is_receive       tm_delivery_note_serials_is_receive? @default(N)
  created_date     DateTime?                            @default(now()) @db.DateTime(0)
  receive_date     DateTime?                            @db.DateTime(0)
  receive_id       Int?
  receive_by       Int?
  data_year        Int?
  data_yearmonth   Int?

  @@index([delivery_note_id], map: "delivery_note_id")
  @@index([data_year], map: "idx_data_year")
  @@index([data_yearmonth], map: "idx_data_yearmonth")
  @@index([receive_id], map: "receive_id")
  @@index([serial_id], map: "serial_id")
  @@index([is_receive], map: "tm_delivery_note_serials_is_receive_IDX")
  @@index([receive_by], map: "tm_delivery_note_serials_receive_by_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_delivery_notes {
  id            Int                            @id @default(autoincrement())
  code          String?                        @db.VarChar(20)
  customer_id   Int?
  warehouse_id  Int?
  license_plate String?                        @db.VarChar(50)
  create_date   DateTime?                      @default(now()) @db.DateTime(0)
  truck_tel     String?                        @db.VarChar(30)
  truck_name    String?                        @db.VarChar(200)
  is_deleted    tm_delivery_notes_is_deleted?  @default(N)
  is_approved   tm_delivery_notes_is_approved? @default(N)
  document1     String?                        @db.VarChar(200)
  document2     String?                        @db.VarChar(200)
  receive_count Int?
  serial_count  Int?
  created_by    Int?
  receive_date  DateTime?                      @db.DateTime(0)
  receive_by    Int?
  updated_by    Int?
  updated_date  DateTime?                      @db.DateTime(0)
  status        String?                        @db.VarChar(20)

  @@index([customer_id], map: "customer_id")
  @@index([customer_id], map: "idx-customer_id")
  @@index([warehouse_id], map: "idx-warehouse_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_deliverys {
  delivery_id       Int                       @id @default(autoincrement())
  delivery_code     String?                   @db.VarChar(20)
  delivery_date     DateTime?                 @db.Date
  is_close          tm_deliverys_is_close?    @default(N)
  is_approved       tm_deliverys_is_approved? @default(N)
  user_id           Int?
  from_warehouse_id Int?
  to_warehouse_id   Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_do_not_send_images {
  id             Int     @id @default(autoincrement())
  do_not_send_id Int?
  document_id    String? @db.VarChar(200)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_do_not_sends {
  do_not_send_id Int                      @id @default(autoincrement())
  reason_id      Int?
  serial_no      String?                  @db.VarChar(50)
  serial_id      String?                  @db.VarChar(64)
  create_date    DateTime?                @default(now()) @db.DateTime(0)
  user_truck_id  Int?
  tel_count      Int?
  is_send        tm_do_not_sends_is_send? @default(N)
  longitude      String?                  @db.VarChar(20)
  latitude       String?                  @db.VarChar(20)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_document_returns {
  serial_id   String    @id @unique(map: "serial_id-idx") @db.VarChar(64)
  create_date DateTime? @db.Date
}

model tm_ex_transit {
  id                     Int                               @default(autoincrement())
  date_procress          DateTime?                         @db.DateTime(0)
  transactions_last_id   String                            @db.VarChar(50)
  receive_business_id    String                            @db.VarChar(50)
  receive_walkin_id      String                            @db.VarChar(50)
  receive_code           String                            @db.VarChar(50)
  serial_id              String                            @unique(map: "serial_id") @db.VarChar(64)
  serial_no              String                            @db.VarChar(100)
  status_id              Int
  datetime               DateTime?                         @db.DateTime(0)
  status_message         String?                           @db.VarChar(200)
  warehouse_id           Int
  warehouse_name         String?                           @db.VarChar(255)
  receive_date           DateTime?                         @db.DateTime(0)
  customer_id            Int
  delivery_date          DateTime?                         @db.Date
  weight                 Decimal?                          @db.Decimal(32, 2)
  width                  Decimal?                          @db.Decimal(32, 2)
  length                 Decimal?                          @db.Decimal(32, 2)
  height                 Decimal?                          @db.Decimal(32, 2)
  q                      Decimal?                          @db.Decimal(32, 2)
  item_is_deleted        String?                           @db.VarChar(1)
  tambon_name            String?                           @db.VarChar(150)
  ampur_name             String?                           @db.VarChar(150)
  province_name          String?                           @db.VarChar(150)
  zip_code               String?                           @db.VarChar(5)
  tel                    String?                           @db.VarChar(10)
  address                String?                           @db.VarChar(1000)
  recipient_code         String?                           @db.VarChar(50)
  recipient_name         String?                           @db.VarChar(255)
  cod                    Decimal?                          @db.Decimal(32, 2)
  is_cod                 tm_ex_transit_is_cod?             @default(N)
  reference_no           String                            @db.VarChar(200)
  is_pickup_customer     tm_ex_transit_is_pickup_customer? @default(N)
  is_pickup_shipper      tm_ex_transit_is_pickup_shipper?  @default(N)
  cost                   Decimal?                          @db.Decimal(10, 2)
  cost_other             Decimal?                          @db.Decimal(10, 2)
  deleted_date           DateTime?                         @db.DateTime(0)
  deleted_by_user_id     Int
  arrival_date           DateTime?                         @db.DateTime(0)
  deleted_by_user        String?                           @db.VarChar(70)
  truck_load_id          String?                           @db.VarChar(50)
  truck_code             String?                           @db.VarChar(20)
  receive_code_serial_no String?                           @db.VarChar(150)

  @@id([id, serial_id, warehouse_id, customer_id, reference_no, deleted_by_user_id, receive_code, transactions_last_id, receive_business_id, receive_walkin_id])
  @@index([id], map: "id")
  @@index([receive_code_serial_no], map: "receive_code_serial_no")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_import_receive_details {
  detail_id         Int     @id @default(autoincrement())
  receive_id        Int?
  package_id        Int?
  serial_no         String? @db.VarChar(50)
  package_detail_id Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_import_receives {
  receive_id          Int                                    @id @default(autoincrement())
  delivery_type_id    Int?
  reference           String?                                @db.VarChar(255)
  customer_id         Int?
  shipper_id          Int?
  recipient_detail_id Int?
  is_cod              tm_import_receives_is_cod?             @default(N)
  cod                 Decimal?                               @db.Decimal(32, 16)
  is_document_return  tm_import_receives_is_document_return?
  document_return     String?                                @db.VarChar(200)
  payment_type_id     Int?
  is_deleted          tm_import_receives_is_deleted?         @default(N)
  create_date         DateTime?                              @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_job_cus_to_dc {
  job_id          Int                            @id @default(autoincrement())
  create_date     DateTime?                      @default(now()) @db.DateTime(0)
  job_date        DateTime?                      @db.Date
  job_time        DateTime?                      @db.Time(0)
  truck_id        Int?
  customer_id     Int?
  warehouse_id    Int?
  document_ref    String?                        @db.VarChar(50)
  document_ref_id Int?
  user_truck_id   Int?
  is_completed    tm_job_cus_to_dc_is_completed? @default(N)
  is_deleted      tm_job_cus_to_dc_is_deleted?   @default(N)
  is_pickup       tm_job_cus_to_dc_is_pickup?    @default(CUSTOMER)
  shipper_id      Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_job_dc_to_cus {
  job_id          Int                            @id @default(autoincrement())
  create_date     DateTime?                      @default(now()) @db.DateTime(0)
  job_date        DateTime?                      @db.Date
  job_time        DateTime?                      @db.Time(0)
  truck_id        Int?
  warehouse_id    Int?
  document_ref    String?                        @db.VarChar(50)
  document_ref_id Int?
  user_truck_id   Int?
  is_completed    tm_job_dc_to_cus_is_completed? @default(N)
  is_deleted      tm_job_dc_to_cus_is_deleted?   @default(N)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_job_dc_to_dc {
  job_id          Int                           @id @default(autoincrement())
  create_date     DateTime?                     @default(now()) @db.DateTime(0)
  job_date        DateTime?                     @db.Date
  job_time        DateTime?                     @db.Time(0)
  warehouse_id    Int?
  to_warehouse_id Int?
  user_truck_id   Int?
  is_completed    tm_job_dc_to_dc_is_completed? @default(N)
  is_deleted      tm_job_dc_to_dc_is_deleted?   @default(N)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_job_shipper_to_dc {
  job_id              Int                                @id @default(autoincrement())
  create_date         DateTime?                          @default(now()) @db.DateTime(0)
  job_date            DateTime?                          @db.Date
  job_time            DateTime?                          @db.Time(0)
  truck_id            Int?
  shipper_id          Int?
  warehouse_id        Int?
  recipient_id        Int?
  document_ref        String?                            @db.VarChar(50)
  document_ref_id     Int?
  user_truck_id       Int?
  is_completed        tm_job_shipper_to_dc_is_completed? @default(N)
  is_deleted          tm_job_shipper_to_dc_is_deleted?   @default(N)
  is_pickup           tm_job_shipper_to_dc_is_pickup?    @default(CUSTOMER)
  type                tm_job_shipper_to_dc_type?
  document_return_id  Int?
  recipient_detail_id Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_jobs {
  job_id           Int                   @id @default(autoincrement())
  create_date      DateTime?             @default(now()) @db.DateTime(0)
  job_date         DateTime?             @db.Date
  job_time         DateTime?             @db.Time(0)
  truck_id         Int?
  customer_id      Int?
  src_warehouse_id Int?
  dst_warehouse_id Int?
  status           tm_jobs_status?
  document_ref     String?               @db.VarChar(50)
  document_ref_id  Int?
  user_truck_id    Int?
  is_completed     tm_jobs_is_completed? @default(N)
  is_deleted       tm_jobs_is_deleted?   @default(N)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_line_transactions {
  id                  Int                        @id @default(autoincrement())
  receive_business_id Int?
  receive_walkin_id   Int?
  receive_code        String?                    @db.VarChar(50)
  serial_id           String?                    @db.VarChar(64)
  serial_no           String?                    @db.VarChar(50)
  status_message      String?                    @db.VarChar(200)
  status_id           Int?
  datetime            DateTime?                  @default(now()) @db.DateTime(0)
  update_date         DateTime?                  @db.DateTime(0)
  type                tm_line_transactions_type?
  warehouse_id        Int?
  created_by          Int?
  latitude            String?                    @db.VarChar(20)
  longitude           String?                    @db.VarChar(20)
  warehouse_name      String?                    @db.VarChar(255)
  address             String?                    @db.VarChar(255)
  province_name       String?                    @db.VarChar(255)
  ampur_name          String?                    @db.VarChar(255)
  tambon_name         String?                    @db.VarChar(255)
  zip_code            String?                    @db.VarChar(5)
  created_name        String?                    @db.VarChar(255)
  username            String?                    @db.VarChar(50)
  truck_license_plate String?                    @db.VarChar(20)
  user_truck_id       Int?
  truck_name          String?                    @db.VarChar(255)
  truck_id            Int?
  truck_province      String?                    @db.VarChar(50)
  note                String?                    @db.VarChar(255)
  data_year           Int?
  data_yearmonth      Int?

  @@index([receive_business_id], map: "idx-receive-business-id")
  @@index([receive_walkin_id], map: "idx-receive-id")
  @@index([serial_id], map: "idx-serial_id")
  @@index([status_id], map: "idx-status_id")
  @@index([warehouse_id], map: "idx-warehouse_id")
  @@index([data_year], map: "idx_data_year")
  @@index([data_yearmonth], map: "idx_data_yearmonth")
  @@index([created_by], map: "tm_product_transactions_created_by_IDX")
  @@index([datetime], map: "tm_product_transactions_datetime_IDX")
  @@index([receive_code], map: "tm_product_transactions_receive_code_IDX")
  @@index([serial_no], map: "tm_product_transactions_serial_no_IDX")
  @@index([truck_id], map: "tm_product_transactions_truck_id_IDX")
  @@index([type], map: "tm_product_transactions_type_IDX")
  @@index([user_truck_id], map: "tm_product_transactions_user_truck_id_IDX")
  @@index([username], map: "tm_product_transactions_username_IDX")
}

model tm_log_activity {
  id              Int                   @id @default(autoincrement())
  DATE            DateTime?             @db.Date
  time            DateTime?             @db.Time(0)
  value_old       String?               @db.VarChar(255)
  value_new       String?               @db.VarChar(255)
  username        String?               @db.VarChar(255)
  name_surname    String?               @db.VarChar(255)
  Department      String?               @db.VarChar(255)
  position        String?               @db.VarChar(255)
  license_plate   String?               @db.VarChar(255)
  serial_no       String?               @db.VarChar(255)
  receive_code    String?               @db.VarChar(50)
  l_table         String?               @db.VarChar(255)
  l_column        String?               @db.VarChar(255)
  user_id         Int?
  pk_id           Int?
  l_edit_table_id Int?                  @unique(map: "l_edit_table_id")
  create_date     DateTime?             @default(now()) @db.Timestamp(0)
  type            tm_log_activity_type? @default(PUBLIC)
  customer_id     Int?

  @@index([receive_code], map: "do")
  @@index([id], map: "id")
}

model tm_log_activity_tel {
  id            Int                            @id @default(autoincrement())
  DATE          DateTime?                      @db.Date
  time          DateTime?                      @db.Time(0)
  receive_code  String?                        @db.VarChar(50)
  reference_no  String?                        @db.VarChar(70)
  serial_no     String?                        @db.VarChar(255)
  customer_id   Int?
  reason_id     Int?
  resend_id     Int?
  resend_date   DateTime?                      @db.DateTime(0)
  longitude     String?                        @db.VarChar(255)
  latitude      String?                        @db.VarChar(255)
  username      String?                        @db.VarChar(255)
  name_surname  String?                        @db.VarChar(255)
  position      String?                        @db.VarChar(255)
  Department    String?                        @db.VarChar(255)
  call_type     tm_log_activity_tel_call_type? @default(dbgenerated("1"))
  call_datetime DateTime?                      @db.DateTime(0)
  detail        String?                        @db.VarChar(255)
  phone_number  String?                        @db.VarChar(10)
  duration      String?                        @db.VarChar(10)
  user_truck_id Int?
  license_plate String?                        @db.VarChar(255)
  receive_id    String                         @unique(map: "l_edit_table_id") @db.VarChar(255)
  type          tm_log_activity_tel_type?      @default(PUBLIC)
  create_date   DateTime?                      @default(now()) @db.Timestamp(0)

  @@index([receive_code], map: "do")
  @@index([id], map: "id")
}

model tm_log_activity_transection {
  id              Int                               @id @default(autoincrement())
  DATE            DateTime?                         @db.Date
  time            DateTime?                         @db.Time(0)
  value_old       String?                           @db.VarChar(255)
  value_new       String?                           @db.VarChar(255)
  username        String?                           @db.VarChar(255)
  name_surname    String?                           @db.VarChar(255)
  Department      String?                           @db.VarChar(255)
  position        String?                           @db.VarChar(255)
  license_plate   String?                           @db.VarChar(255)
  serial_no       String?                           @db.VarChar(255)
  receive_code    String?                           @db.VarChar(50)
  l_table         String?                           @db.VarChar(255)
  l_column        String?                           @db.VarChar(255)
  user_id         Int?
  pk_id           Int?
  l_edit_table_id Int?                              @unique(map: "l_edit_table_id")
  create_date     DateTime?                         @default(now()) @db.Timestamp(0)
  type            tm_log_activity_transection_type? @default(PUBLIC)
  customer_id     Int?
  reference_no    String?                           @db.VarChar(70)
  call_datetime   DateTime?                         @db.DateTime(0)
  detail          String?                           @db.VarChar(255)
  phone_number    String?                           @db.VarChar(10)
  receive_id      Int?

  @@index([receive_code], map: "do")
  @@index([id], map: "id")
  @@index([receive_id], map: "receive_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_log_serials {
  id           Int       @id @default(autoincrement())
  user_id      Int?
  warehouse_id Int?
  serial_id    String?   @db.VarChar(64)
  serial_no    String?   @db.VarChar(50)
  comment      String?   @db.VarChar(255)
  time         DateTime? @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_logs_api {
  id          Int       @id @default(autoincrement())
  body        String?   @db.Text
  res         String?   @db.Text
  create_date DateTime? @default(now()) @db.DateTime(0)
}

model tm_manager_tk {
  truck_code       String    @db.VarChar(50)
  create_date      DateTime? @db.DateTime(0)
  user_truck_id    BigInt?   @db.UnsignedBigInt
  dc_______        String?   @map("dc_ต้นทาง") @db.VarChar(150)
  dc________       String?   @map("dc_ปลายทาง") @db.VarChar(150)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // ชื่อพนักงานขับรถ String? @map("ชื่อพนักงานขับรถ") @db.VarChar(100)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // นามสุกล String? @map("นามสุกล") @db.VarChar(100)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // ทะเบียนรถ String? @map("ทะเบียนรถ") @db.VarChar(50)
  dc               String?   @map("รถขึ้นอยู่_dc") @db.VarChar(150)
  status           String?   @db.VarChar(50)
  is_completed     String?   @default("N") @db.Char(1)
  customer_id      BigInt?   @db.UnsignedBigInt
  is_go            String?   @default("N") @db.Char(1)
  close_datetime   DateTime? @db.DateTime(0)
  go_datetime      DateTime? @db.DateTime(0)
  is_arrived       String?   @default("N") @db.Char(1)
  arrived_by       BigInt?   @db.UnsignedBigInt
  arrived_datetime DateTime? @db.DateTime(0)
  is_deleted       String?   @default("N") @db.Char(1)
  truck_load_id    BigInt    @unique(map: "idx_truck_id") @db.UnsignedBigInt

  @@id([truck_code, truck_load_id])
  @@index([arrived_by], map: "idx_arrived_by")
  @@index([close_datetime], map: "idx_close_datetime")
  @@index([create_date], map: "idx_create_date")
  @@index([customer_id], map: "idx_customer_id")
  @@index([dc_______], map: "idx_dc_ต้นทาง")
  @@index([dc________], map: "idx_dc_ปลายทาง")
  @@index([is_arrived], map: "idx_is_arrived")
  @@index([is_completed], map: "idx_is_completed")
  @@index([is_deleted], map: "idx_is_deleted")
  @@index([is_go], map: "idx_is_go")
  @@index([status], map: "idx_status")
  @@index([user_truck_id], map: "idx_user_truck_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_palettes {
  palette_id     Int                     @id @default(autoincrement())
  palette_serial String?                 @db.VarChar(255)
  is_actived     tm_palettes_is_actived? @default(Y)
  is_deleted     tm_palettes_is_deleted? @default(N)
  create_date    DateTime?               @default(now()) @db.Timestamp(0)
  is_success     tm_palettes_is_success? @default(N)
  warehouse_id   Int?
  reference      String?                 @db.VarChar(255)
}

model tm_product_actived {
  id           Int       @default(autoincrement())
  serial_id    String    @db.VarChar(64)
  serial_no    String    @unique(map: "serial_no") @db.VarChar(50)
  created_date DateTime? @default(now()) @db.DateTime(0)
  updated_date DateTime? @db.DateTime(0)
  remark       String?   @db.VarChar(255)

  @@id([id, serial_no, serial_id])
  @@index([id], map: "id")
  @@index([serial_id], map: "serial_id")
}

model tm_product_actived_tan {
  serial_id String  @id @db.VarChar(64)
  serial_no String? @db.VarChar(64)
}

model tm_product_actived_temp {
  id                 Int       @id @default(autoincrement())
  actived_temp_id    Int?
  last_serial_id     String?   @unique(map: "serial_id") @db.VarChar(64)
  last_serial_no     String?   @db.VarChar(50)
  created_date       DateTime? @default(now()) @db.DateTime(0)
  receive_date       DateTime? @db.DateTime(0)
  is_pickup_shipper  String?   @db.VarChar(1)
  is_pickup_customer String?   @db.VarChar(1)
  status_id          Int?
  status_message     String?   @db.VarChar(255)
  is_deleted         String?   @db.VarChar(1)
  item_is_deleted    String?   @db.VarChar(1)
  actived_id         Int?
  serial_id          String?   @db.VarChar(64)
  serial_no          String?   @db.VarChar(50)

  @@index([last_serial_no], map: "serial_no")
}

model tm_product_activedxx {
  id           Int       @id @default(autoincrement())
  serial_id    String?   @db.VarChar(64)
  serial_no    String?   @unique(map: "serial_no") @db.VarChar(50)
  created_date DateTime? @default(now()) @db.DateTime(0)
  updated_date DateTime? @db.DateTime(0)

  @@index([serial_id], map: "serial_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_product_customers {
  product_id  Int     @id @default(autoincrement())
  serial_id   String? @unique(map: "idx-uniuq") @db.VarChar(64)
  customer_id Int?
  shipper_id  Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_product_exchange_derivery {
  product_id       Int       @id @default(autoincrement())
  serial_id        String?   @unique(map: "serial_id-idx") @db.VarChar(64)
  warehouse_id     Int?
  dst_warehouse_id Int?
  palette_id       Int?
  route_id         Int?
  resend_date      DateTime? @db.Date
  created_by       Int?
  timestamp        DateTime? @default(now()) @db.Timestamp(0)
  exchange_id      Int?

  @@index([exchange_id], map: "exchange_id")
  @@index([warehouse_id], map: "warehouse_id-idx")
  @@map("tm_product_exchange derivery")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_product_recipients {
  product_id          Int     @id @default(autoincrement())
  serial_id           String? @unique(map: "idx-duplicate") @db.VarChar(64)
  recipient_id        Int?
  shipper_id          Int?
  recipient_detail_id Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_product_resentdate_transection {
  id                   Int       @default(autoincrement())
  product_id           Int
  serial_id            String?   @unique(map: "serial_id-idx") @db.VarChar(64)
  warehouse_id         Int?
  dst_warehouse_id     Int?
  palette_id           Int?
  route_id             Int?
  resend_date          DateTime? @db.Date
  created_by           Int?
  timestamp            DateTime? @default(now()) @db.DateTime(0)
  resentdate_timestamp DateTime? @db.Timestamp(0)

  @@id([product_id, id])
  @@index([id], map: "id_idx")
  @@index([warehouse_id], map: "warehouse_id-idx")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_product_transactions {
  id                  Int                           @id @default(autoincrement())
  receive_business_id Int?
  receive_walkin_id   Int?
  receive_code        String?                       @db.VarChar(50)
  serial_id           String?                       @db.VarChar(64)
  serial_no           String?                       @db.VarChar(50)
  status_message      String?                       @db.VarChar(200)
  status_id           Int?
  datetime            DateTime?                     @default(now()) @db.DateTime(0)
  update_date         DateTime?                     @db.DateTime(0)
  type                tm_product_transactions_type? @default(PUBLIC)
  warehouse_id        Int?
  created_by          Int?
  latitude            String?                       @db.VarChar(20)
  longitude           String?                       @db.VarChar(20)
  warehouse_name      String?                       @db.VarChar(255)
  address             String?                       @db.VarChar(255)
  province_name       String?                       @db.VarChar(255)
  ampur_name          String?                       @db.VarChar(255)
  tambon_name         String?                       @db.VarChar(255)
  zip_code            String?                       @db.VarChar(5)
  created_name        String?                       @db.VarChar(255)
  username            String?                       @db.VarChar(50)
  truck_license_plate String?                       @db.VarChar(20)
  user_truck_id       Int?
  truck_name          String?                       @db.VarChar(255)
  truck_id            Int?
  truck_province      String?                       @db.VarChar(50)
  note                String?                       @db.VarChar(255)
  data_year           Int?
  data_yearmonth      Int?

  @@index([receive_business_id], map: "idx-receive-business-id")
  @@index([receive_walkin_id], map: "idx-receive-id")
  @@index([serial_id], map: "idx-serial_id")
  @@index([status_id], map: "idx-status_id")
  @@index([warehouse_id], map: "idx-warehouse_id")
  @@index([created_by], map: "tm_product_transactions_created_by_IDX")
  @@index([datetime], map: "tm_product_transactions_datetime_IDX")
  @@index([receive_code], map: "tm_product_transactions_receive_code_IDX")
  @@index([serial_no], map: "tm_product_transactions_serial_no_IDX")
  @@index([truck_id], map: "tm_product_transactions_truck_id_IDX")
  @@index([type], map: "tm_product_transactions_type_IDX")
  @@index([user_truck_id], map: "tm_product_transactions_user_truck_id_IDX")
  @@index([username], map: "tm_product_transactions_username_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_product_transactions_cencel {
  id                  Int                                  @id
  receive_business_id Int?
  receive_walkin_id   Int?
  receive_code        String?                              @db.VarChar(50)
  serial_id           String?                              @db.VarChar(64)
  serial_no           String?                              @db.VarChar(50)
  status_message      String?                              @db.VarChar(200)
  status_id           Int?
  datetime            DateTime?                            @default(now()) @db.DateTime(0)
  update_date         DateTime?                            @db.DateTime(0)
  type                tm_product_transactions_cencel_type? @default(PUBLIC)
  warehouse_id        Int?
  created_by          Int?
  latitude            String?                              @db.VarChar(20)
  longitude           String?                              @db.VarChar(20)
  warehouse_name      String?                              @db.VarChar(255)
  address             String?                              @db.VarChar(255)
  province_name       String?                              @db.VarChar(255)
  ampur_name          String?                              @db.VarChar(255)
  tambon_name         String?                              @db.VarChar(255)
  zip_code            String?                              @db.VarChar(5)
  created_name        String?                              @db.VarChar(255)
  username            String?                              @db.VarChar(50)
  truck_license_plate String?                              @db.VarChar(20)
  user_truck_id       Int?
  truck_name          String?                              @db.VarChar(255)
  truck_id            Int?
  truck_province      String?                              @db.VarChar(50)
  note                String?                              @db.VarChar(255)

  @@index([receive_business_id], map: "idx-receive-business-id")
  @@index([receive_walkin_id], map: "idx-receive-id")
  @@index([serial_id], map: "idx-serial_id")
  @@index([status_id], map: "idx-status_id")
  @@index([warehouse_id], map: "idx-warehouse_id")
  @@index([created_by], map: "tm_product_transactions_created_by_IDX")
  @@index([receive_code], map: "tm_product_transactions_receive_code_IDX")
  @@index([serial_no], map: "tm_product_transactions_serial_no_IDX")
  @@index([truck_id], map: "tm_product_transactions_truck_id_IDX")
  @@index([type], map: "tm_product_transactions_type_IDX")
  @@index([user_truck_id], map: "tm_product_transactions_user_truck_id_IDX")
  @@index([username], map: "tm_product_transactions_username_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_product_transactions_ex {
  id                  Int                              @id @default(autoincrement())
  receive_business_id Int?
  receive_walkin_id   Int?
  receive_code        String?                          @db.VarChar(50)
  serial_id           String?                          @db.VarChar(64)
  serial_no           String?                          @db.VarChar(50)
  status_message      String?                          @db.VarChar(200)
  status_id           Int?
  datetime            DateTime?                        @default(now()) @db.DateTime(0)
  update_date         DateTime?                        @db.DateTime(0)
  type                tm_product_transactions_ex_type? @default(PUBLIC)
  warehouse_id        Int?
  created_by          Int?
  latitude            String?                          @db.VarChar(20)
  longitude           String?                          @db.VarChar(20)
  warehouse_name      String?                          @db.VarChar(255)
  address             String?                          @db.VarChar(255)
  province_name       String?                          @db.VarChar(255)
  ampur_name          String?                          @db.VarChar(255)
  tambon_name         String?                          @db.VarChar(255)
  zip_code            String?                          @db.VarChar(5)
  created_name        String?                          @db.VarChar(255)
  username            String?                          @db.VarChar(50)
  truck_license_plate String?                          @db.VarChar(20)
  user_truck_id       Int?
  truck_name          String?                          @db.VarChar(255)
  truck_id            Int?
  truck_province      String?                          @db.VarChar(50)
  note                String?                          @db.VarChar(255)

  @@index([receive_business_id], map: "idx-receive-business-id")
  @@index([receive_walkin_id], map: "idx-receive-id")
  @@index([serial_id], map: "idx-serial_id")
  @@index([status_id], map: "idx-status_id")
  @@index([warehouse_id], map: "idx-warehouse_id")
  @@index([created_by], map: "tm_product_transactions_created_by_IDX")
  @@index([datetime], map: "tm_product_transactions_datetime_IDX")
  @@index([receive_code], map: "tm_product_transactions_receive_code_IDX")
  @@index([serial_no], map: "tm_product_transactions_serial_no_IDX")
  @@index([truck_id], map: "tm_product_transactions_truck_id_IDX")
  @@index([type], map: "tm_product_transactions_type_IDX")
  @@index([user_truck_id], map: "tm_product_transactions_user_truck_id_IDX")
  @@index([username], map: "tm_product_transactions_username_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_product_transactions_last {
  id                  Int                                @id @default(autoincrement())
  receive_business_id Int?
  receive_walkin_id   Int?
  receive_code        String?                            @db.VarChar(50)
  serial_id           String?                            @unique(map: "idx-serial_id") @db.VarChar(64)
  serial_no           String?                            @db.VarChar(50)
  status_message      String?                            @db.VarChar(200)
  status_id           Int?
  datetime            DateTime?                          @default(now()) @db.DateTime(0)
  update_date         DateTime?                          @db.DateTime(0)
  type                tm_product_transactions_last_type? @default(PUBLIC)
  warehouse_id        Int?
  created_by          Int?
  latitude            String?                            @db.VarChar(20)
  longitude           String?                            @db.VarChar(20)
  warehouse_name      String?                            @db.VarChar(255)
  address             String?                            @db.VarChar(255)
  province_name       String?                            @db.VarChar(255)
  ampur_name          String?                            @db.VarChar(255)
  tambon_name         String?                            @db.VarChar(255)
  zip_code            String?                            @db.VarChar(5)
  created_name        String?                            @db.VarChar(255)
  username            String?                            @db.VarChar(50)
  truck_license_plate String?                            @db.VarChar(20)
  user_truck_id       Int?
  truck_name          String?                            @db.VarChar(255)
  truck_id            Int?
  truck_province      String?                            @db.VarChar(50)
  note                String?                            @db.VarChar(255)
  timestamp           DateTime?                          @default(now()) @db.DateTime(0)

  @@index([receive_business_id], map: "idx-receive-business-id")
  @@index([receive_walkin_id], map: "idx-receive-id")
  @@index([status_id], map: "idx-status_id")
  @@index([timestamp], map: "idx-timestamp")
  @@index([warehouse_id], map: "idx-warehouse_id")
  @@index([receive_code], map: "idx_receive_code")
  @@index([created_by], map: "tm_product_transactions_last_created_by_IDX")
  @@index([serial_no], map: "tm_product_transactions_last_serial_no_IDX")
  @@index([truck_id], map: "tm_product_transactions_last_truck_id_IDX")
  @@index([user_truck_id], map: "tm_product_transactions_last_user_truck_id_IDX")
  @@index([username], map: "tm_product_transactions_last_username_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_product_transactions_status8 {
  id                  Int                                   @id @default(autoincrement())
  receive_business_id Int?
  receive_walkin_id   Int?
  receive_code        String?                               @db.VarChar(50)
  serial_id           String?                               @db.VarChar(64)
  serial_no           String?                               @db.VarChar(50)
  status_message      String?                               @db.VarChar(200)
  status_id           Int?
  datetime            DateTime?                             @default(now()) @db.DateTime(0)
  update_date         DateTime?                             @db.DateTime(0)
  type                tm_product_transactions_status8_type? @default(PUBLIC)
  warehouse_id        Int?
  created_by          Int?
  latitude            String?                               @db.VarChar(20)
  longitude           String?                               @db.VarChar(20)
  warehouse_name      String?                               @db.VarChar(255)
  address             String?                               @db.VarChar(255)
  province_name       String?                               @db.VarChar(255)
  ampur_name          String?                               @db.VarChar(255)
  tambon_name         String?                               @db.VarChar(255)
  zip_code            String?                               @db.VarChar(5)
  created_name        String?                               @db.VarChar(255)
  username            String?                               @db.VarChar(50)
  truck_license_plate String?                               @db.VarChar(20)
  user_truck_id       Int?
  truck_name          String?                               @db.VarChar(255)
  truck_id            Int?
  truck_province      String?                               @db.VarChar(50)
  note                String?                               @db.VarChar(255)

  @@index([receive_business_id], map: "idx-receive-business-id")
  @@index([receive_walkin_id], map: "idx-receive-id")
  @@index([serial_id], map: "idx-serial_id")
  @@index([status_id], map: "idx-status_id")
  @@index([warehouse_id], map: "idx-warehouse_id")
  @@index([created_by], map: "tm_product_transactions_created_by_IDX")
  @@index([datetime], map: "tm_product_transactions_datetime_IDX")
  @@index([receive_code], map: "tm_product_transactions_receive_code_IDX")
  @@index([serial_no], map: "tm_product_transactions_serial_no_IDX")
  @@index([truck_id], map: "tm_product_transactions_truck_id_IDX")
  @@index([type], map: "tm_product_transactions_type_IDX")
  @@index([user_truck_id], map: "tm_product_transactions_user_truck_id_IDX")
  @@index([username], map: "tm_product_transactions_username_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_product_trucks {
  product_id    Int                       @id @default(autoincrement())
  serial_id     String?                   @unique(map: "serial_id-idx") @db.VarChar(64)
  user_truck_id Int?
  truck_id      Int?
  status        tm_product_trucks_status? @default(PENDDING)
  resend_date   DateTime?                 @db.Date
  truck_load_id Int?
  created_date  DateTime?                 @default(now()) @db.DateTime(0)

  @@index([status], map: "tm_product_trucks_status_IDX")
  @@index([truck_id], map: "tm_product_trucks_truck_id_IDX")
  @@index([truck_load_id], map: "tm_product_trucks_truck_load_id_IDX")
  @@index([user_truck_id], map: "tm_product_trucks_user_truck_id_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_product_trucks_temp {
  product_id    Int                            @id @default(autoincrement())
  serial_id     String?                        @unique(map: "serial_id-idx") @db.VarChar(64)
  user_truck_id Int?
  truck_id      Int?
  status        tm_product_trucks_temp_status? @default(PENDDING)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_product_warehouses {
  product_id       Int       @id @default(autoincrement())
  serial_id        String?   @unique(map: "serial_id-idx") @db.VarChar(64)
  warehouse_id     Int?
  dst_warehouse_id Int?
  palette_id       Int?
  route_id         Int?
  resend_date      DateTime? @db.Date
  created_by       Int?
  timestamp        DateTime? @default(now()) @db.Timestamp(0)

  @@index([warehouse_id], map: "warehouse_id-idx")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_products {
  product_id  Int       @id @default(autoincrement())
  serial_id   String?   @db.VarChar(64)
  status_id   Int?
  create_date DateTime? @default(now()) @db.DateTime(0)
  update_date DateTime? @db.DateTime(0)

  @@index([serial_id], map: "idx-serial_id")
  @@index([status_id], map: "tm_products_status_id_IDX")
}

model tm_pull_serial_dc {
  id                Int       @id @default(autoincrement())
  serial_id         String?   @db.VarChar(64)
  serial_no         String?   @db.VarChar(50)
  created_by        Int?
  warehouse_id      Int?
  created_date      DateTime? @default(now()) @db.DateTime(0)
  from_warehouse_id Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receive_advice_fast12 {
  receive_code        String?                         @db.VarChar(50)
  receive_date        DateTime?                       @db.DateTime(0)
  delivery_date       DateTime?                       @db.Date
  reference_no        String?                         @db.VarChar(200)
  serial_no           String?                         @db.VarChar(100)
  payments_name       String?                         @db.VarChar(200)
  package_name        String?                         @db.VarChar(255)
  customer_name       String?                         @db.VarChar(255)
  cost_serials        Decimal?                        @db.Decimal(32, 2)
  warehouse_name      String?                         @db.VarChar(60)
  to_warehouse_name   String?                         @db.VarChar(60)
  shipper_name        String?                         @db.VarChar(255)
  recipient_code      String?                         @db.VarChar(50)
  recipient_name      String?                         @db.VarChar(255)
  address             String?                         @db.VarChar(1000)
  tambon_name         String?                         @db.VarChar(150)
  ampur_name          String?                         @db.VarChar(150)
  province_name       String?                         @db.VarChar(150)
  zip_code            String?                         @db.VarChar(255)
  tel                 String?                         @db.VarChar(13)
  shipper_id          Int?
  serial_id           String                          @db.VarChar(64)
  cost                Decimal?                        @db.Decimal(10, 2)
  receive_id          Int
  receive_business_id Int
  is_deleted          String?                         @default("N") @db.VarChar(1)
  item_is_deleted     String?                         @db.VarChar(1)
  q                   Decimal?                        @db.Decimal(32, 2)
  status_id           Int?
  status_message      String?                         @db.VarChar(255)
  datetime_status     DateTime?                       @db.DateTime(0)
  timestamp           DateTime?                       @db.Timestamp(0)
  count               tm_receive_advice_fast12_count? @default(dbgenerated("1"))

  @@id([serial_id, receive_business_id, receive_id])
  @@index([receive_business_id], map: "idx-receive_business_id")
  @@index([serial_no], map: "idx-serial_no")
  @@index([receive_code], map: "receive_code")
  @@index([item_is_deleted], map: "tm_receive_serials_item_is_deleted_IDX")
  @@index([receive_date], map: "tm_receive_serials_receive_date_IDX")
  @@index([recipient_code], map: "tm_receive_serials_recipient_code_IDX")
  @@index([serial_id], map: "tm_receive_serials_serial_id_IDX")
  @@index([shipper_id], map: "tm_receive_serials_shipper_id_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receive_bill_print_logs {
  id                  Int       @id @default(autoincrement())
  receive_walkin_id   Int?
  receive_business_id Int?
  datetime            DateTime? @default(now()) @db.DateTime(0)
  note                String?   @db.VarChar(1000)
  user_id             Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receive_cost_other {
  id                  Int      @id @default(autoincrement())
  receive_business_id Int?
  cost_other_id       Int?
  cost                Decimal? @db.Decimal(32, 16)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receive_detail_items {
  receive_item_id   Int                                 @id @default(autoincrement())
  receive_detail_id Int?
  serial_id         String?                             @db.VarChar(64)
  serial_no         String?                             @db.VarChar(50)
  is_deleted        tm_receive_detail_items_is_deleted? @default(N)
  deleted_by        Int?
  deleted_time      DateTime?                           @db.DateTime(0)

  @@index([receive_detail_id], map: "idx-receive_detail_id")
  @@index([serial_id], map: "idx-serial_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([serial_no], map: "serial_no")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receive_details {
  receive_detail_id Int                           @id @default(autoincrement())
  receive_id        Int?
  package_id        Int?
  package_detail_id Int?
  package_name      String?                       @db.VarChar(255)
  unit_name         String?                       @db.VarChar(255)
  qty               Int?
  cost              Decimal?                      @db.Decimal(32, 2)
  weight            Decimal?                      @db.Decimal(32, 2)
  cost_difference   Decimal?                      @db.Decimal(32, 0)
  remark            String?                       @db.VarChar(255)
  width             Decimal?                      @db.Decimal(32, 2)
  height            Decimal?                      @db.Decimal(32, 2)
  length            Decimal?                      @db.Decimal(32, 2)
  cost_island       Decimal?                      @db.Decimal(32, 2)
  cost_other        Decimal?                      @db.Decimal(32, 2)
  q                 Decimal?                      @db.Decimal(32, 2)
  size_type         tm_receive_details_size_type?
  vol               Decimal?                      @db.Decimal(32, 2)

  @@index([receive_id], map: "idx-receive_id")
  @@index([package_detail_id], map: "package_detail_id")
  @@index([package_detail_id], map: "tm_receive_details_package_detail_id_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receive_drivers {
  receive_driver_id Int  @id @default(autoincrement())
  receive_id        Int?
  user_id           Int?
}

model tm_receive_files {
  id          Int     @id @default(autoincrement())
  document_id String? @db.VarChar(64)
  receive_id  Int?

  @@index([document_id], map: "idx-document_id")
  @@index([receive_id], map: "idx-receive_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receive_import_detail_items {
  receive_item_id   Int                                        @id @default(autoincrement())
  receive_detail_id Int?
  serial_id         String?                                    @db.VarChar(64)
  serial_no         String?                                    @db.VarChar(50)
  is_deleted        tm_receive_import_detail_items_is_deleted? @default(N)

  @@index([receive_detail_id], map: "idx-receive_detail_id")
  @@index([serial_id], map: "idx-serial_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receive_import_details {
  receive_detail_id  Int                                           @id @default(autoincrement())
  receive_id         Int?
  package_id         Int?
  package_detail_id  Int?
  package_name       String?                                       @db.VarChar(255)
  unit_name          String?                                       @db.VarChar(255)
  qty                Int?
  cost               Decimal?                                      @db.Decimal(32, 16)
  weight             Decimal?                                      @db.Decimal(32, 16)
  cost_difference    Decimal?                                      @db.Decimal(32, 0)
  remark             String?                                       @db.VarChar(255)
  width              Decimal?                                      @db.Decimal(32, 16)
  height             Decimal?                                      @db.Decimal(32, 16)
  length             Decimal?                                      @db.Decimal(32, 16)
  cost_island        Decimal?                                      @db.Decimal(32, 16)
  cost_other         Decimal?                                      @db.Decimal(32, 16)
  q                  Decimal?                                      @db.Decimal(32, 16)
  size_type          tm_receive_import_details_size_type?
  is_document_return tm_receive_import_details_is_document_return? @default(N)

  @@index([receive_id], map: "idx-receive_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receive_import_head {
  receive_id          Int                                        @id @default(autoincrement())
  receive_date        DateTime?                                  @db.DateTime(0)
  receive_code        String?                                    @db.VarChar(50)
  tracking_no         String?                                    @db.VarChar(20)
  delivery_type_id    Int?
  job_type_id         Int?
  customer_id         Int?
  shipper_id          Int?
  recipient_id        Int?
  recipient_detail_id Int?
  recipient_name      String?                                    @db.VarChar(255)
  address             String?                                    @db.VarChar(1000)
  province_id         Int?
  ampur_id            Int?
  tambon_id           Int?
  zip_code            String?                                    @db.VarChar(10)
  tel                 String?                                    @db.VarChar(50)
  fax                 String?                                    @db.VarChar(50)
  contact             String?                                    @db.VarChar(200)
  delivery_date       DateTime?                                  @db.Date
  is_cod              tm_receive_import_head_is_cod?             @default(N)
  cod                 Decimal?                                   @db.Decimal(32, 16)
  is_document_return  tm_receive_import_head_is_document_return?
  document_return     String?                                    @db.VarChar(200)
  payment_type_id     Int?
  payment_status      String?                                    @db.VarChar(100)
  is_deleted          tm_receive_import_head_is_deleted?         @default(N)
  is_approved         tm_receive_import_head_is_approved?        @default(N)
  approve_date        DateTime?                                  @db.Date
  approve_people_id   Int?
  from_warehouse_id   Int?
  to_warehouse_id     Int?
  status              String?                                    @db.VarChar(200)
  create_date         DateTime?                                  @default(now()) @db.Timestamp(0)
  is_pickup_customer  tm_receive_import_head_is_pickup_customer? @default(N)
  is_pickup_shipper   tm_receive_import_head_is_pickup_shipper?  @default(N)
  is_invoices         tm_receive_import_head_is_invoices?        @default(N)
  app_create          tm_receive_import_head_app_create?         @default(WEB)
  reference_no        String?                                    @db.VarChar(200)
  is_returned         tm_receive_import_head_is_returned?        @default(N)
  url                 String?                                    @db.Text
  remark              String?                                    @db.Text
  import_id           Int?
  arrival_date        DateTime?                                  @db.Date

  @@index([ampur_id], map: "idx-ampur_id")
  @@index([customer_id], map: "idx-customer_id")
  @@index([from_warehouse_id], map: "idx-from_warehouse_id")
  @@index([province_id], map: "idx-province_id")
  @@index([receive_code], map: "idx-receive_code")
  @@index([recipient_id], map: "idx-recipient_id")
  @@index([shipper_id], map: "idx-shipper_id")
  @@index([tambon_id], map: "idx-tambon_id")
  @@index([to_warehouse_id], map: "idx-to_warehouse_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receive_imports {
  id           Int                            @id @default(autoincrement())
  created_date DateTime?                      @default(now()) @db.DateTime(0)
  status       tm_receive_imports_status?     @default(PENDING)
  is_deleted   tm_receive_imports_is_deleted? @default(N)
  created_by   Int?
  customer_id  Int?
  updated_date DateTime?                      @db.DateTime(0)
  updated_by   Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receive_invoice {
  id           Int                             @id @default(autoincrement())
  invoice_code String?                         @db.VarChar(50)
  receive_id   Int?
  price        Decimal?                        @db.Decimal(16, 10)
  vat_rate     Decimal?                        @db.Decimal(16, 10)
  vat          Decimal?                        @db.Decimal(16, 10)
  net          Decimal?                        @db.Decimal(16, 10)
  is_complete  tm_receive_invoice_is_complete? @default(N)
}

model tm_receive_reference_group {
  reference_no String? @db.Text
  receive_id   Int     @id
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receive_references {
  reference_id Int     @id @default(autoincrement())
  reference_no String? @db.VarChar(200)
  receive_id   Int?

  @@index([receive_id], map: "receive_id")
  @@index([reference_no], map: "reference_no")
}

model tm_receive_serial_files {
  id          Int     @id @default(autoincrement())
  document_id String? @db.VarChar(64)
  receive_id  Int?
  serial_id   String? @db.VarChar(64)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receive_serials {
  receive_code          String?                       @db.VarChar(50)
  receive_business_id   Int?
  receive_date          DateTime?                     @db.DateTime(0)
  receive_walkin_id     Int?
  delivery_date         DateTime?                     @db.Date
  serial_id             String                        @id @db.VarChar(64)
  serial_no             String?                       @db.VarChar(100)
  package_id            Int?
  package_name          String?                       @db.VarChar(255)
  package_detail_id     Int?
  package_detail_name   String?                       @db.VarChar(255)
  customer_id           Int?
  cost                  Decimal?                      @db.Decimal(32, 16)
  from_warehouse_id     Int?
  to_warehouse_id       Int?
  recipient_name        String?                       @db.VarChar(255)
  recipient_code        String?                       @db.VarChar(50)
  address               String?                       @db.VarChar(1000)
  ampur_id              Int?
  ampur_name            String?                       @db.VarChar(150)
  tambon_id             Int?
  tambon_name           String?                       @db.VarChar(150)
  province_id           Int?
  province_name         String?                       @db.VarChar(150)
  zip_code              String?                       @db.VarChar(255)
  tel                   String?                       @db.VarChar(50)
  item_is_deleted       String?                       @db.VarChar(1)
  recipient_id          Int?
  shipper_id            Int?
  shipper_name          String?                       @db.VarChar(255)
  document_return       String?                       @db.VarChar(200)
  remark                String?                       @db.MediumText
  url                   String?                       @db.MediumText
  cod                   Decimal?                      @db.Decimal(32, 2)
  weight                Decimal?                      @db.Decimal(32, 2)
  width                 Decimal?                      @db.Decimal(32, 2)
  length                Decimal?                      @db.Decimal(32, 2)
  height                Decimal?                      @db.Decimal(32, 2)
  q                     Decimal?                      @db.Decimal(32, 2)
  is_returned           String?                       @db.VarChar(1)
  payment_type_id       Int?
  recipient_detail_id   Int?
  recipient_detail_name String?                       @db.VarChar(255)
  deleted_date          DateTime?                     @db.DateTime(0)
  deleted_by_user       String?                       @db.VarChar(70)
  vol                   Decimal?                      @db.Decimal(32, 2)
  size_type             tm_receive_serials_size_type?

  @@index([customer_id], map: "customer_id")
  @@index([receive_business_id], map: "idx-receive_business_id")
  @@index([serial_no], map: "idx-serial_no")
  @@index([receive_code], map: "receive_code")
  @@index([ampur_id], map: "tm_receive_serials_ampur_id_IDX")
  @@index([from_warehouse_id], map: "tm_receive_serials_from_warehouse_id_IDX")
  @@index([is_returned], map: "tm_receive_serials_is_returned_IDX")
  @@index([item_is_deleted], map: "tm_receive_serials_item_is_deleted_IDX")
  @@index([package_id], map: "tm_receive_serials_package_id_IDX")
  @@index([payment_type_id], map: "tm_receive_serials_payment_type_id_IDX")
  @@index([province_id], map: "tm_receive_serials_province_id_IDX")
  @@index([receive_date], map: "tm_receive_serials_receive_date_IDX")
  @@index([recipient_code], map: "tm_receive_serials_recipient_code_IDX")
  @@index([recipient_detail_id], map: "tm_receive_serials_recipient_detail_id_IDX")
  @@index([recipient_id], map: "tm_receive_serials_recipient_id_IDX")
  @@index([serial_id], map: "tm_receive_serials_serial_id_IDX")
  @@index([shipper_id], map: "tm_receive_serials_shipper_id_IDX")
  @@index([tambon_id], map: "tm_receive_serials_tambon_id_IDX")
  @@index([to_warehouse_id], map: "tm_receive_serials_to_warehouse_id_IDX")
}

model tm_receive_status {
  receive_walkin_id   Int?      @unique(map: "uniuq_walkin_id")
  receive_business_id Int?      @unique(map: "uniuq_business_id")
  receive_code        String?   @unique(map: "tm_receive_status_receive_code_IDX") @db.VarChar(50)
  status_id           Int?
  datetime            DateTime? @db.DateTime(0)
  status              String?   @db.VarChar(255)
  id                  Int       @id @default(autoincrement())

  @@index([id], map: "id")
  @@index([status], map: "tm_receive_status_status_IDX")
}

model tm_receive_status_copy1 {
  receive_walkin_id   Int?      @unique(map: "uniuq_walkin_id")
  receive_business_id Int?      @unique(map: "uniuq_business_id")
  receive_code        String?   @unique(map: "tm_receive_status_receive_code_IDX") @db.VarChar(50)
  status_id           Int?
  datetime            DateTime? @db.DateTime(0)
  status              String?   @db.VarChar(255)
  id                  Int       @id @default(autoincrement())

  @@index([id], map: "id")
  @@index([status], map: "tm_receive_status_status_IDX")
}

model tm_receive_status_crm {
  receive_walkin_id   Int?      @unique(map: "uniuq_walkin_id")
  receive_business_id Int?      @unique(map: "uniuq_business_id")
  receive_code        String?   @db.VarChar(50)
  status_id           Int?
  datetime            DateTime? @db.DateTime(0)
  status              String?   @db.VarChar(255)
  id                  Int       @id @default(autoincrement())

  @@index([receive_code], map: "tm_receive_status_receive_code_IDX")
  @@index([status], map: "tm_receive_status_status_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receive_sticker_print_logs {
  id                  Int       @id @default(autoincrement())
  receive_walkin_id   Int?
  receive_business_id Int?
  datetime            DateTime? @default(now()) @db.DateTime(0)
  note                String?   @db.VarChar(1000)
  user_id             Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receive_walkin_details {
  receive_detail_id     Int                                        @id @default(autoincrement())
  receive_id            Int?
  package_id            Int?
  package_detail_id     Int?
  package_name          String?                                    @db.VarChar(255)
  unit_name             String?                                    @db.VarChar(255)
  cod                   Decimal?                                   @db.Decimal(32, 16)
  is_cod                tm_receive_walkin_details_is_cod?          @default(N)
  weight                Decimal?                                   @db.Decimal(32, 16)
  cost                  Decimal?                                   @db.Decimal(32, 16)
  serial_id             String?                                    @db.VarChar(64)
  serial_no             String?                                    @db.VarChar(100)
  recipient_name        String?                                    @db.VarChar(200)
  recipient_address     String?                                    @db.VarChar(1000)
  recipient_ampur_id    Int?
  recipient_tambon_id   Int?
  recipient_province_id Int?
  recipient_zip_code    String?                                    @db.VarChar(6)
  recipient_tel         String?                                    @db.VarChar(20)
  to_warehouse_id       Int?
  width                 Decimal?                                   @db.Decimal(32, 16)
  height                Decimal?                                   @db.Decimal(32, 16)
  length                Decimal?                                   @db.Decimal(32, 16)
  cost_island           Decimal?                                   @db.Decimal(32, 16)
  cost_other            Decimal?                                   @db.Decimal(32, 16)
  delivery_date         DateTime?                                  @db.Date
  q                     Decimal?                                   @db.Decimal(32, 16)
  size_type             tm_receive_walkin_details_size_type?
  arrival_date          DateTime?                                  @db.DateTime(0)
  item_is_deleted       tm_receive_walkin_details_item_is_deleted? @default(N)
  remask                String?                                    @db.VarChar(255)

  @@index([receive_id], map: "idx-receive_id")
  @@index([serial_id], map: "idx-serial_id")
  @@index([package_detail_id], map: "package_detail_id")
  @@index([package_id], map: "package_id")
  @@index([recipient_ampur_id], map: "recipient_ampur_id")
  @@index([recipient_province_id], map: "recipient_province_id")
  @@index([recipient_tambon_id], map: "recipient_tambon_id")
  @@index([to_warehouse_id], map: "to_warehouse_id")
}

model tm_receive_walkin_files {
  id          Int     @id @default(autoincrement())
  document_id String? @db.VarChar(64)
  receive_id  Int?
}

model tm_receive_walkin_serial_files {
  id          Int     @id @default(autoincrement())
  document_id String? @db.VarChar(64)
  receive_id  Int?
  serial_id   String? @db.VarChar(64)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receive_walkins {
  receive_id          Int                             @id @default(autoincrement())
  receive_date        DateTime?                       @db.Date
  receive_code        String?                         @db.VarChar(50)
  delivery_type_id    Int?
  job_type_id         Int?
  customer_id         Int?
  shipper_cid         String?                         @db.VarChar(20)
  shipper_name        String?                         @db.VarChar(200)
  shipper_address     String?                         @db.VarChar(1000)
  shipper_province_id Int?
  shipper_ampur_id    Int?
  shipper_tambon_id   Int?
  shipper_zip_code    String?                         @db.VarChar(10)
  shipper_tel         String?                         @db.VarChar(50)
  delivery_date       DateTime?                       @db.Date
  payment_type_id     Int?
  payment_status      String?                         @db.VarChar(100)
  is_deleted          tm_receive_walkins_is_deleted?  @default(N)
  is_approved         tm_receive_walkins_is_approved? @default(N)
  approve_date        DateTime?                       @db.Date
  approve_people_id   Int?
  from_warehouse_id   Int?
  status              String?                         @db.VarChar(200)
  create_date         DateTime?                       @default(now()) @db.Timestamp(0)
  create_user_id      Int?
  is_invoices         tm_receive_walkins_is_invoices? @default(N)
  deleted_by_user_id  Int?
  deleted_date        DateTime?                       @db.DateTime(0)

  @@index([is_deleted], map: "is_deleted")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receives {
  receive_id          Int                             @id @default(autoincrement())
  receive_date        DateTime?                       @db.DateTime(0)
  receive_code        String?                         @db.VarChar(50)
  tracking_no         String?                         @db.VarChar(20)
  delivery_type_id    Int?
  job_type_id         Int?
  customer_id         Int?
  shipper_id          Int?
  recipient_id        Int?
  recipient_detail_id Int?
  recipient_name      String?                         @db.VarChar(255)
  address             String?                         @db.VarChar(1000)
  province_id         Int?
  ampur_id            Int?
  tambon_id           Int?
  zip_code            String?                         @db.VarChar(10)
  tel                 String?                         @db.VarChar(50)
  fax                 String?                         @db.VarChar(50)
  contact             String?                         @db.VarChar(200)
  delivery_date       DateTime?                       @db.Date
  is_cod              tm_receives_is_cod?             @default(N)
  cod                 Decimal?                        @db.Decimal(32, 2)
  is_document_return  tm_receives_is_document_return?
  document_return     String?                         @db.VarChar(200)
  payment_type_id     Int?
  payment_status      String?                         @db.VarChar(100)
  is_deleted          tm_receives_is_deleted?         @default(N)
  is_approved         tm_receives_is_approved?        @default(N)
  approve_date        DateTime?                       @db.Date
  approve_people_id   Int?
  from_warehouse_id   Int?
  to_warehouse_id     Int?
  status              String?                         @db.VarChar(200)
  create_date         DateTime?                       @default(now()) @db.Timestamp(0)
  is_pickup_customer  tm_receives_is_pickup_customer? @default(N)
  is_pickup_shipper   tm_receives_is_pickup_shipper?  @default(N)
  is_invoices         tm_receives_is_invoices?        @default(N)
  app_create          tm_receives_app_create?         @default(WEB)
  reference_no        String?                         @db.VarChar(200)
  is_returned         tm_receives_is_returned?        @default(N)
  url                 String?                         @db.Text
  remark              String?                         @db.Text
  cost                Decimal?                        @db.Decimal(32, 2)
  cost_other          Decimal?                        @db.Decimal(32, 2)
  discount            Decimal?                        @db.Decimal(32, 2)
  discount_coupon     Decimal?                        @db.Decimal(32, 2)
  net                 Decimal?                        @db.Decimal(32, 2)
  reason_remove       String?                         @db.VarChar(255)
  deleted_by_user_id  Int?
  deleted_date        DateTime?                       @db.DateTime(0)
  arrival_date        DateTime?                       @db.DateTime(0)
  deleted_by_user     String?                         @db.VarChar(70)

  @@index([receive_id], map: "id")
  @@index([ampur_id], map: "idx-ampur_id")
  @@index([customer_id], map: "idx-customer_id")
  @@index([from_warehouse_id], map: "idx-from_warehouse_id")
  @@index([province_id], map: "idx-province_id")
  @@index([receive_code], map: "idx-receive_code")
  @@index([recipient_id], map: "idx-recipient_id")
  @@index([shipper_id], map: "idx-shipper_id")
  @@index([tambon_id], map: "idx-tambon_id")
  @@index([to_warehouse_id], map: "idx-to_warehouse_id")
  @@index([receive_date], map: "idx_receive_date")
  @@index([is_deleted], map: "is_deleted")
  @@index([receive_date], map: "receive_date")
  @@index([reference_no], map: "reference_no")
  @@index([receive_code], map: "tm_receives_idx_receive_code")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receives_backup {
  receive_id          Int                                    @id
  receive_date        DateTime?                              @db.DateTime(0)
  receive_code        String?                                @db.VarChar(50)
  tracking_no         String?                                @db.VarChar(20)
  delivery_type_id    Int?
  job_type_id         Int?
  customer_id         Int?
  shipper_id          Int?
  recipient_id        Int?
  recipient_detail_id Int?
  recipient_name      String?                                @db.VarChar(255)
  address             String?                                @db.VarChar(1000)
  province_id         Int?
  ampur_id            Int?
  tambon_id           Int?
  zip_code            String?                                @db.VarChar(10)
  tel                 String?                                @db.VarChar(50)
  fax                 String?                                @db.VarChar(50)
  contact             String?                                @db.VarChar(200)
  delivery_date       DateTime?                              @db.Date
  is_cod              tm_receives_backup_is_cod?             @default(N)
  cod                 Decimal?                               @db.Decimal(32, 16)
  is_document_return  tm_receives_backup_is_document_return?
  document_return     String?                                @db.VarChar(200)
  payment_type_id     Int?
  payment_status      String?                                @db.VarChar(100)
  is_deleted          tm_receives_backup_is_deleted?         @default(N)
  is_approved         tm_receives_backup_is_approved?        @default(N)
  approve_date        DateTime?                              @db.Date
  approve_people_id   Int?
  from_warehouse_id   Int?
  to_warehouse_id     Int?
  status              String?                                @db.VarChar(200)
  create_date         DateTime?                              @default(now()) @db.Timestamp(0)
  is_pickup_customer  tm_receives_backup_is_pickup_customer? @default(N)
  is_pickup_shipper   tm_receives_backup_is_pickup_shipper?  @default(N)
  is_invoices         tm_receives_backup_is_invoices?        @default(N)
  app_create          tm_receives_backup_app_create?         @default(WEB)
  reference_no        String?                                @db.VarChar(200)
  is_returned         tm_receives_backup_is_returned?        @default(N)
  url                 String?                                @db.Text
  remark              String?                                @db.Text
  cost                Decimal?                               @db.Decimal(32, 16)
  cost_other          Decimal?                               @db.Decimal(32, 16)
  discount            Decimal?                               @db.Decimal(32, 16)
  discount_coupon     Decimal?                               @db.Decimal(32, 16)
  net                 Decimal?                               @db.Decimal(32, 16)
  reason_remove       String?                                @db.VarChar(255)
  deleted_by_user_id  Int?
  deleted_date        DateTime?                              @db.DateTime(0)
  arrival_date        DateTime?                              @db.DateTime(0)
  deleted_by_user     String?                                @db.VarChar(70)

  @@index([ampur_id], map: "idx-ampur_id")
  @@index([customer_id], map: "idx-customer_id")
  @@index([from_warehouse_id], map: "idx-from_warehouse_id")
  @@index([province_id], map: "idx-province_id")
  @@index([receive_code], map: "idx-receive_code")
  @@index([recipient_id], map: "idx-recipient_id")
  @@index([shipper_id], map: "idx-shipper_id")
  @@index([tambon_id], map: "idx-tambon_id")
  @@index([to_warehouse_id], map: "idx-to_warehouse_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([receive_date], map: "receive_date")
  @@index([receive_code], map: "tm_receives_idx_receive_code")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_receives_copy2 {
  receive_id          Int                                   @id @default(autoincrement())
  receive_date        DateTime?                             @db.DateTime(0)
  receive_code        String?                               @db.VarChar(50)
  tracking_no         String?                               @db.VarChar(20)
  delivery_type_id    Int?
  job_type_id         Int?
  customer_id         Int?
  shipper_id          Int?
  recipient_id        Int?
  recipient_detail_id Int?
  recipient_name      String?                               @db.VarChar(255)
  address             String?                               @db.VarChar(1000)
  province_id         Int?
  ampur_id            Int?
  tambon_id           Int?
  zip_code            String?                               @db.VarChar(10)
  tel                 String?                               @db.VarChar(50)
  fax                 String?                               @db.VarChar(50)
  contact             String?                               @db.VarChar(200)
  delivery_date       DateTime?                             @db.Date
  is_cod              tm_receives_copy2_is_cod?             @default(N)
  cod                 Decimal?                              @db.Decimal(32, 2)
  is_document_return  tm_receives_copy2_is_document_return?
  document_return     String?                               @db.VarChar(200)
  payment_type_id     Int?
  payment_status      String?                               @db.VarChar(100)
  is_deleted          tm_receives_copy2_is_deleted?         @default(N)
  is_approved         tm_receives_copy2_is_approved?        @default(N)
  approve_date        DateTime?                             @db.Date
  approve_people_id   Int?
  from_warehouse_id   Int?
  to_warehouse_id     Int?
  status              String?                               @db.VarChar(200)
  create_date         DateTime?                             @default(now()) @db.Timestamp(0)
  is_pickup_customer  tm_receives_copy2_is_pickup_customer? @default(N)
  is_pickup_shipper   tm_receives_copy2_is_pickup_shipper?  @default(N)
  is_invoices         tm_receives_copy2_is_invoices?        @default(N)
  app_create          tm_receives_copy2_app_create?         @default(WEB)
  reference_no        String?                               @db.VarChar(200)
  is_returned         tm_receives_copy2_is_returned?        @default(N)
  url                 String?                               @db.Text
  remark              String?                               @db.Text
  cost                Decimal?                              @db.Decimal(32, 2)
  cost_other          Decimal?                              @db.Decimal(32, 2)
  discount            Decimal?                              @db.Decimal(32, 2)
  discount_coupon     Decimal?                              @db.Decimal(32, 2)
  net                 Decimal?                              @db.Decimal(32, 2)
  reason_remove       String?                               @db.VarChar(255)
  deleted_by_user_id  Int?
  deleted_date        DateTime?                             @db.DateTime(0)
  arrival_date        DateTime?                             @db.DateTime(0)
  deleted_by_user     String?                               @db.VarChar(70)

  @@index([ampur_id], map: "idx-ampur_id")
  @@index([customer_id], map: "idx-customer_id")
  @@index([from_warehouse_id], map: "idx-from_warehouse_id")
  @@index([province_id], map: "idx-province_id")
  @@index([receive_code], map: "idx-receive_code")
  @@index([recipient_id], map: "idx-recipient_id")
  @@index([shipper_id], map: "idx-shipper_id")
  @@index([tambon_id], map: "idx-tambon_id")
  @@index([to_warehouse_id], map: "idx-to_warehouse_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([receive_date], map: "receive_date")
  @@index([reference_no], map: "reference_no")
  @@index([receive_code], map: "tm_receives_idx_receive_code")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_resend_images {
  id          Int     @id @default(autoincrement())
  resend_id   String? @db.VarChar(64)
  document_id String? @db.VarChar(200)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_resends {
  resend_id     Int       @id @default(autoincrement())
  reason_id     Int?
  reason        mm_reason_sends? @relation("ReasonToResends", fields: [reason_id], references: [reason_id])
  serial_no     String?   @db.VarChar(30)
  serial_id     String?   @db.VarChar(64)
  create_date   DateTime? @default(now()) @db.DateTime(0)
  user_truck_id Int?
  resend_date   DateTime? @db.Date
  success       String?   @default("N") @db.Char(1)
  longitude     String?   @db.VarChar(20)
  latitude      String?   @db.VarChar(20)
  success_18_14 String?   @db.VarChar(1)

  @@index([resend_id], map: "id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_resends_not_in_14_18 {
  resend_id                  Int?
  reason_id                  Int?
  serial_no                  String?                                @db.VarChar(30)
  serial_id                  String                                 @db.VarChar(64)
  create_date                DateTime?                              @default(now()) @db.DateTime(0)
  user_truck_id              Int?
  resend_date                DateTime?                              @db.Date
  success                    String?                                @default("N") @db.Char(1)
  longitude                  String?                                @db.VarChar(20)
  latitude                   String?                                @db.VarChar(20)
  success_18_14              tm_resends_not_in_14_18_success_18_14? @default(Y)
  status_id                  Int?
  datetime                   DateTime?                              @db.DateTime(0)
  send_reason                Int?
  sends_detail               String?                                @db.VarChar(200)
  is_deleted                 tm_resends_not_in_14_18_is_deleted?    @default(N)
  reason_send_problem_detail String?                                @db.VarChar(255)
  updatetime                 DateTime?                              @db.DateTime(0)
  resends_14_18_id           Int                                    @default(autoincrement())

  @@id([resends_14_18_id, serial_id])
  @@index([resends_14_18_id], map: "id")
  @@index([serial_id], map: "serial_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_route_trucks {
  id       Int  @id @default(autoincrement())
  route_id Int?
  truck_id Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_screen_details {
  id           Int                           @id @default(autoincrement())
  screen_id    Int?
  serial_id    String?                       @db.VarChar(64)
  is_receive   tm_screen_details_is_receive? @default(N)
  receive_id   Int?
  receive_date DateTime?                     @db.DateTime(0)
  receive_by   Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_screen_warehouse {
  id           Int  @id @default(autoincrement())
  screen_id    Int
  warehouse_id Int?

  @@index([screen_id], map: "idx-screen_id")
  @@index([warehouse_id], map: "idx-warehouse_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_screens {
  id              Int                    @id @default(autoincrement())
  code            String?                @db.VarChar(255)
  is_deleted      tm_screens_is_deleted? @default(N)
  created_date    DateTime?              @default(now()) @db.DateTime(0)
  created_by      Int?
  warehouse_id    Int?
  to_warehouse_id Int?
  user_truck_id   Int?
  is_move         tm_screens_is_move?    @default(N)
  is_go           tm_screens_is_go?      @default(N)
  datetime_go     DateTime?              @db.DateTime(0)
  user_receiver   Int?
  time_receive    DateTime?              @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_send_images {
  id          Int     @id @default(autoincrement())
  send_id     Int?
  document_id String? @db.VarChar(200)

  @@index([document_id], map: "tm_send_images_document_id_IDX")
  @@index([send_id], map: "tm_send_images_send_id_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_send_problem_reasons {
  send_problem_reason_id Int       @id @default(autoincrement())
  reason_send_problem_id Int?
  serial_id              String?   @db.VarChar(64)
  user_truck_id          Int?
  create_date            DateTime? @default(now()) @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_sends {
  send_id        Int       @id @default(autoincrement())
  recipient_name String?   @db.VarChar(255)
  sign_name      String?   @db.VarChar(255)
  serial_no      String?   @db.VarChar(50)
  serial_id      String?   @db.VarChar(64)
  create_date    DateTime? @default(now()) @db.DateTime(0)
  user_truck_id  Int?
  longitude      String?   @db.VarChar(20)
  latitude       String?   @db.VarChar(20)
  send_image_id  String?   @db.VarChar(255)

  @@index([serial_id], map: "idx-serial_id")
  @@index([send_image_id], map: "tm_sends_send_image_id_IDX")
  @@index([serial_no], map: "tm_sends_serial_no_IDX")
  @@index([user_truck_id], map: "tm_sends_user_truck_id_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_tel_historys {
  id            Int       @unique(map: "idx_id") @default(autoincrement())
  serial_id     String    @db.VarChar(64)
  call_type     String    @db.VarChar(20)
  call_datetime DateTime  @db.DateTime(0)
  phone_number  String    @db.VarChar(20)
  duration      Int?
  user_truck_id Int
  created_date  DateTime? @default(now()) @db.DateTime(0)
  updated_date  DateTime? @db.DateTime(0)

  @@id([serial_id, call_type, call_datetime, user_truck_id, phone_number])
}

model tm_tmp_product_actived {
  id           Int       @id
  serial_id    String?   @db.VarChar(64)
  serial_no    String?   @unique(map: "serial_no") @db.VarChar(50)
  created_date DateTime? @default(now()) @db.DateTime(0)
  updated_date DateTime? @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_truck_checks {
  id           Int                     @id @default(autoincrement())
  created_date DateTime?               @default(now()) @db.DateTime(0)
  user_id      Int?
  latitude     String?                 @db.VarChar(255)
  longitude    Bytes?                  @db.VarBinary(255)
  warehouse_id Int?
  status       tm_truck_checks_status?
}

model tm_truck_count {
  id            Int     @id @default(autoincrement())
  truck_load_id Int
  count_box     String? @db.VarChar(3)
  vol_count     String? @db.VarChar(5)

  @@index([id], map: "id")
  @@index([truck_load_id], map: "truck_load_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_truck_detail_groups {
  truck_detail_group_id Int  @id @default(autoincrement())
  truck_group_code      Int?
  truck_detail_id       Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_truck_details {
  truck_detail_id Int                            @id @default(autoincrement())
  truck_load_id   Int?
  serial_id       String?                        @db.VarChar(64)
  create_date     DateTime?                      @default(now()) @db.DateTime(0)
  is_receive      tm_truck_details_is_receive?   @default(N)
  receive_by      Int?
  receive_date    DateTime?                      @db.DateTime(0)
  manul_recive    tm_truck_details_manul_recive? @default(N)
  note            String?                        @db.VarChar(255)
  cost            Decimal?                       @db.Decimal(10, 2)

  @@unique([truck_load_id, serial_id], map: "idx-dupli")
  @@index([serial_id], map: "serial_id")
  @@index([serial_id], map: "tm_truck_details_idx_serial_id")
  @@index([truck_load_id], map: "truck_load_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_truck_info {
  id             Int      @id @default(autoincrement())
  truck_load_id  Int?
  document       String?  @db.VarChar(255)
  truck_name     String?  @db.VarChar(255)
  truck_no       String?  @db.VarChar(255)
  truck_province String?  @db.VarChar(255)
  card_oil       Decimal? @db.Decimal(32, 16)
  cost           Decimal? @db.Decimal(32, 16)
  truck_type     Int?
}

model tm_truck_summary {
  truck_load_id    BigInt    @unique(map: "track_load") @db.UnsignedBigInt
  total_items      Int       @default(0) @db.UnsignedInt
  returned_items   Int       @default(0) @db.UnsignedInt
  total_cost       Decimal   @default(0.00) @db.Decimal(18, 2)
  recipient_count  Int       @default(0) @db.UnsignedInt
  truck_sammary_id Int       @default(autoincrement())
  create_date      DateTime? @db.Date

  @@id([truck_load_id, truck_sammary_id])
  @@index([create_date], map: "create_date")
  @@index([truck_sammary_id], map: "id")
  @@index([recipient_count], map: "idx_recipient_count")
  @@index([returned_items], map: "idx_returned_items")
  @@index([total_cost], map: "idx_total_cost")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tm_trucks {
  truck_load_id       Int                     @id @default(autoincrement())
  truck_code          String?                 @unique(map: "truck_code") @db.VarChar(20)
  create_date         DateTime?               @default(now()) @db.DateTime(0)
  is_close            tm_trucks_is_close?     @default(N)
  is_completed        tm_trucks_is_completed? @default(N)
  customer_id         Int?
  user_truck_id       Int?
  status              tm_trucks_status?
  warehouse_id        Int?
  to_warehouse_id     Int?
  job_id              Int?
  is_go               tm_trucks_is_go?        @default(N)
  close_datetime      DateTime?               @db.DateTime(0)
  go_datetime         DateTime?               @db.DateTime(0)
  close_by            Int?
  go_by               Int?
  is_arrived          tm_trucks_is_arrived?   @default(N)
  arrived_by          Int?
  arrived_datetime    DateTime?               @db.DateTime(0)
  shipper_id          Int?
  recipient_id        Int?
  is_deleted          tm_trucks_is_deleted?   @default(N)
  deleted_by          Int?
  is_go_app           tm_trucks_is_go_app?    @default(N)
  go_app_datetime     DateTime?               @db.DateTime(0)
  go_app_by           Int?
  recipient_detail_id Int?
  note                String?                 @db.VarChar(255)

  @@index([customer_id], map: "customer_id")
  @@index([to_warehouse_id], map: "idx-to_warehouse_id")
  @@index([truck_code], map: "idx-truck_code")
  @@index([warehouse_id], map: "idx-warehouse_id")
  @@index([is_deleted], map: "is_deleted")
  @@index([arrived_by], map: "tm_trucks_arrived_by_IDX")
  @@index([close_by], map: "tm_trucks_close_by_IDX")
  @@index([create_date], map: "tm_trucks_create_date_IDX")
  @@index([go_by], map: "tm_trucks_go_by_IDX")
  @@index([truck_load_id], map: "tm_trucks_idx_truck_id")
  @@index([is_arrived], map: "tm_trucks_is_arrived_IDX")
  @@index([is_deleted], map: "tm_trucks_is_deleted_IDX")
  @@index([is_go], map: "tm_trucks_is_go_IDX")
  @@index([job_id], map: "tm_trucks_job_id_IDX")
  @@index([recipient_detail_id], map: "tm_trucks_recipient_detail_id_IDX")
  @@index([recipient_id], map: "tm_trucks_recipient_id_IDX")
  @@index([shipper_id], map: "tm_trucks_shipper_id_IDX")
  @@index([status], map: "tm_trucks_status_IDX")
  @@index([user_truck_id], map: "tm_trucks_user_truck_id_IDX")
}

model tmp_del_product {
  id         Int
  product_id Int
  serial_id  String @db.VarChar(64)

  @@id([id, serial_id])
  @@index([product_id, serial_id], map: "product_id")
}

model tmp_del_serial {
  id              Int       @default(autoincrement())
  product_id      Int
  serial_id       String    @unique(map: "serial_id") @db.VarChar(64)
  item_is_deleted String?   @db.VarChar(1)
  customer_id     Int?
  shipper_id      Int?
  status_id       Int?
  datetime        DateTime? @db.DateTime(0)
  status_message  String?   @db.VarChar(200)
  diff_date       Int?

  @@id([id, product_id, serial_id])
  @@index([item_is_deleted], map: "item_is_deleted")
  @@index([product_id], map: "product_id")
  @@index([id], map: "tmp_remove_id")
}

model tmp_do_sn {
  id                     Int     @id @default(autoincrement())
  receive_code           String? @db.VarChar(255)
  serial_no              String? @db.VarChar(50)
  serial_id              String? @db.VarChar(64)
  receive_code_serial_no String? @db.VarChar(150)

  @@index([id], map: "id")
}

model tmp_latest_reference {
  receive_id   Int     @id
  reference_no String? @db.VarChar(255)

  @@index([reference_no], map: "idx_reference_no")
}

model tmp_product_del {
  id           Int       @default(autoincrement())
  serial_id    String    @unique(map: "serial_id") @db.VarChar(64)
  status_id    Int
  warehouse_id Int?
  datetime     DateTime? @db.DateTime(0)

  @@id([id, status_id])
  @@index([id], map: "id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tmp_product_transactions {
  id                  Int?
  receive_business_id Int?
  receive_walkin_id   Int?
  receive_code        String?                        @db.VarChar(50)
  serial_id           String                         @id @db.VarChar(64)
  serial_no           String?                        @db.VarChar(50)
  status_message      String?                        @db.VarChar(200)
  status_id           Int?
  datetime            DateTime?                      @default(now()) @db.DateTime(0)
  update_date         DateTime?                      @db.DateTime(0)
  type                tmp_product_transactions_type? @default(PUBLIC)
  warehouse_id        Int?
  created_by          Int?
  latitude            String?                        @db.VarChar(20)
  longitude           String?                        @db.VarChar(20)
  warehouse_name      String?                        @db.VarChar(255)
  address             String?                        @db.VarChar(255)
  province_name       String?                        @db.VarChar(255)
  ampur_name          String?                        @db.VarChar(255)
  tambon_name         String?                        @db.VarChar(255)
  zip_code            String?                        @db.VarChar(5)
  created_name        String?                        @db.VarChar(255)
  username            String?                        @db.VarChar(50)
  truck_license_plate String?                        @db.VarChar(20)
  user_truck_id       Int?
  truck_name          String?                        @db.VarChar(255)
  truck_id            Int?
  truck_province      String?                        @db.VarChar(50)
  note                String?                        @db.VarChar(255)

  @@index([receive_business_id], map: "idx-receive-business-id")
  @@index([receive_walkin_id], map: "idx-receive-id")
  @@index([serial_id], map: "idx-serial_id")
  @@index([status_id], map: "idx-status_id")
  @@index([warehouse_id], map: "idx-warehouse_id")
  @@index([created_by], map: "tm_product_transactions_created_by_IDX")
  @@index([datetime], map: "tm_product_transactions_datetime_IDX")
  @@index([receive_code], map: "tm_product_transactions_receive_code_IDX")
  @@index([serial_no], map: "tm_product_transactions_serial_no_IDX")
  @@index([truck_id], map: "tm_product_transactions_truck_id_IDX")
  @@index([type], map: "tm_product_transactions_type_IDX")
  @@index([user_truck_id], map: "tm_product_transactions_user_truck_id_IDX")
  @@index([username], map: "tm_product_transactions_username_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tmp_product_transactions_ro {
  id                  Int?
  receive_business_id Int?
  receive_walkin_id   Int?
  receive_code        String?                           @db.VarChar(50)
  serial_id           String                            @id @db.VarChar(64)
  serial_no           String?                           @db.VarChar(50)
  status_message      String?                           @db.VarChar(200)
  status_id           Int?
  datetime            DateTime?                         @default(now()) @db.DateTime(0)
  update_date         DateTime?                         @db.DateTime(0)
  type                tmp_product_transactions_ro_type? @default(PUBLIC)
  warehouse_id        Int?
  created_by          Int?
  latitude            String?                           @db.VarChar(20)
  longitude           String?                           @db.VarChar(20)
  warehouse_name      String?                           @db.VarChar(255)
  address             String?                           @db.VarChar(255)
  province_name       String?                           @db.VarChar(255)
  ampur_name          String?                           @db.VarChar(255)
  tambon_name         String?                           @db.VarChar(255)
  zip_code            String?                           @db.VarChar(5)
  created_name        String?                           @db.VarChar(255)
  username            String?                           @db.VarChar(50)
  truck_license_plate String?                           @db.VarChar(20)
  user_truck_id       Int?
  truck_name          String?                           @db.VarChar(255)
  truck_id            Int?
  truck_province      String?                           @db.VarChar(50)
  note                String?                           @db.VarChar(255)

  @@index([receive_business_id], map: "idx-receive-business-id")
  @@index([receive_walkin_id], map: "idx-receive-id")
  @@index([serial_id], map: "idx-serial_id")
  @@index([status_id], map: "idx-status_id")
  @@index([warehouse_id], map: "idx-warehouse_id")
  @@index([created_by], map: "tm_product_transactions_created_by_IDX")
  @@index([datetime], map: "tm_product_transactions_datetime_IDX")
  @@index([receive_code], map: "tm_product_transactions_receive_code_IDX")
  @@index([serial_no], map: "tm_product_transactions_serial_no_IDX")
  @@index([truck_id], map: "tm_product_transactions_truck_id_IDX")
  @@index([type], map: "tm_product_transactions_type_IDX")
  @@index([user_truck_id], map: "tm_product_transactions_user_truck_id_IDX")
  @@index([username], map: "tm_product_transactions_username_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tmp_product_warehouses {
  id           Int       @default(autoincrement())
  serial_id    String    @unique(map: "serial_id-idx") @db.VarChar(64)
  warehouse_id Int?
  created_by   Int?
  timestamp    DateTime? @default(now()) @db.Timestamp(0)

  @@id([id, serial_id])
  @@index([id], map: "id")
  @@index([warehouse_id], map: "warehouse_id-idx")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tmp_product_warehouses_teststatus {
  id           Int       @default(autoincrement())
  serial_id    String    @unique(map: "serial_id-idx") @db.VarChar(64)
  warehouse_id Int?
  created_by   Int?
  timestamp    DateTime? @default(now()) @db.Timestamp(0)

  @@id([id, serial_id])
  @@index([id], map: "id")
  @@index([warehouse_id], map: "warehouse_id-idx")
}

model tmp_pt_status9 {
  id              Int     @default(autoincrement())
  transactions_id Int?
  serial_id       String  @db.VarChar(64)
  note1           String? @db.VarChar(255)
  note            String? @db.VarChar(255)

  @@id([serial_id, id])
  @@index([id], map: "id")
  @@index([serial_id], map: "idx-serial_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tmp_receive_cod {
  receive_code          String?                    @db.VarChar(50)
  receive_business_id   Int?
  receive_date          DateTime?                  @db.DateTime(0)
  receive_walkin_id     Int?
  delivery_date         DateTime?                  @db.Date
  serial_id             String                     @id @db.VarChar(64)
  serial_no             String?                    @db.VarChar(100)
  package_id            Int?
  package_name          String?                    @db.VarChar(255)
  package_detail_id     Int?
  package_detail_name   String?                    @db.VarChar(255)
  customer_id           Int?
  cost                  Decimal?                   @db.Decimal(32, 16)
  from_warehouse_id     Int?
  to_warehouse_id       Int?
  recipient_name        String?                    @db.VarChar(255)
  recipient_code        String?                    @db.VarChar(50)
  address               String?                    @db.VarChar(1000)
  ampur_id              Int?
  ampur_name            String?                    @db.VarChar(150)
  tambon_id             Int?
  tambon_name           String?                    @db.VarChar(150)
  province_id           Int?
  province_name         String?                    @db.VarChar(150)
  zip_code              String?                    @db.VarChar(255)
  tel                   String?                    @db.VarChar(255)
  item_is_deleted       String?                    @db.VarChar(1)
  recipient_id          Int?
  shipper_id            Int?
  shipper_name          String?                    @db.VarChar(255)
  document_return       String?                    @db.VarChar(200)
  remark                String?                    @db.MediumText
  url                   String?                    @db.MediumText
  cod                   Decimal?                   @db.Decimal(32, 2)
  weight                Decimal?                   @db.Decimal(32, 2)
  width                 Decimal?                   @db.Decimal(32, 2)
  length                Decimal?                   @db.Decimal(32, 2)
  height                Decimal?                   @db.Decimal(32, 2)
  q                     Decimal?                   @db.Decimal(32, 2)
  is_returned           String?                    @db.VarChar(1)
  payment_type_id       Int?
  recipient_detail_id   Int?
  recipient_detail_name String?                    @db.VarChar(255)
  deleted_date          DateTime?                  @db.DateTime(0)
  deleted_by_user       String?                    @db.VarChar(70)
  vol                   Decimal?                   @db.Decimal(32, 2)
  size_type             tmp_receive_cod_size_type?

  @@index([customer_id], map: "customer_id")
  @@index([receive_business_id], map: "idx-receive_business_id")
  @@index([serial_no], map: "idx-serial_no")
  @@index([receive_code], map: "receive_code")
  @@index([ampur_id], map: "tm_receive_serials_ampur_id_IDX")
  @@index([from_warehouse_id], map: "tm_receive_serials_from_warehouse_id_IDX")
  @@index([is_returned], map: "tm_receive_serials_is_returned_IDX")
  @@index([item_is_deleted], map: "tm_receive_serials_item_is_deleted_IDX")
  @@index([package_id], map: "tm_receive_serials_package_id_IDX")
  @@index([payment_type_id], map: "tm_receive_serials_payment_type_id_IDX")
  @@index([province_id], map: "tm_receive_serials_province_id_IDX")
  @@index([receive_date], map: "tm_receive_serials_receive_date_IDX")
  @@index([recipient_code], map: "tm_receive_serials_recipient_code_IDX")
  @@index([recipient_detail_id], map: "tm_receive_serials_recipient_detail_id_IDX")
  @@index([recipient_id], map: "tm_receive_serials_recipient_id_IDX")
  @@index([serial_id], map: "tm_receive_serials_serial_id_IDX")
  @@index([shipper_id], map: "tm_receive_serials_shipper_id_IDX")
  @@index([tambon_id], map: "tm_receive_serials_tambon_id_IDX")
  @@index([to_warehouse_id], map: "tm_receive_serials_to_warehouse_id_IDX")
}

model tmp_receive_code_logs {
  receive_code String @id @db.VarChar(255)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model tmp_remove_receive_walkin_id {
  id                   Int  @default(autoincrement())
  receive_walkin_id    Int? @unique(map: "receive_walkin_id")
  tm_receive_status_id Int?

  @@index([id], map: "id")
  @@ignore
}

model tmp_replace_note_tm_product_transections {
  id                  Int       @id @default(autoincrement())
  transection_id      Int?
  truck_license_plate String?   @db.VarChar(255)
  note_old            String?   @db.VarChar(255)
  datetime            DateTime? @db.DateTime(0)
  note                String?   @db.VarChar(255)

  @@index([id], map: "id")
  @@index([transection_id], map: "transection_id")
}

model tmp_serial_no_logs {
  serial_no String @id @db.VarChar(255)
}

model tmp_tm_delivery_note_details_is_delete {
  receive_id Int?
  id         Int  @id @default(autoincrement())
}

model tmp_tm_delivery_note_serial_is_delete {
  serial_id String? @db.VarChar(200)
  id        Int     @id @default(autoincrement())

  @@index([serial_id], map: "serail_id")
}

model tmp_tm_product_activate {
  id           Int       @id @default(autoincrement())
  serial_id    String?   @db.VarChar(64)
  serial_no    String?   @db.VarChar(100)
  created_date DateTime? @db.DateTime(0)

  @@index([id], map: "id")
  @@index([serial_id], map: "serial_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tmp_tm_product_transactions {
  id                         Int     @default(autoincrement())
  tm_product_transections_id Int?
  serial_id                  String  @db.VarChar(64)
  receive_business_id        Int?
  receive_walkin_id          Int?
  receive_code               String? @db.VarChar(50)
  serial_no                  String? @db.VarChar(50)

  @@id([id, serial_id])
  @@index([receive_business_id], map: "idx-receive-business-id")
  @@index([receive_walkin_id], map: "idx-receive-id")
  @@index([serial_id], map: "idx-serial_id")
  @@index([receive_code], map: "tm_product_transactions_receive_code_IDX")
  @@index([serial_no], map: "tm_product_transactions_serial_no_IDX")
  @@index([tm_product_transections_id], map: "tm_product_transections_id")
}

model tmp_tm_receive_delivery_date {
  receive_id        Int
  receive_date      DateTime? @db.DateTime(0)
  customer_id       Int?
  arrival_date      DateTime? @db.DateTime(0)
  recipient_name    String?   @db.VarChar(255)
  delivery_date     DateTime? @db.Date
  receive_code      String?   @db.VarChar(100)
  from_warehouse_id Int?
  to_warehouse_id   Int?
  id                Int       @default(autoincrement()) @db.UnsignedInt

  @@id([id, receive_id])
  @@index([receive_id], map: "`receive_id`")
  @@index([id], map: "id")
  @@index([receive_code], map: "receive_code")
}

model tmp_tm_receive_status {
  id_tmp       Int       @unique(map: "id_tmp") @default(autoincrement())
  receive_code String?   @unique(map: "receive_code") @db.VarChar(50)
  datetime     DateTime? @db.DateTime(0)
  status       String?   @db.VarChar(255)
  status_id    Int?
  id           Int

  @@id([id, id_tmp])
  @@index([id], map: "id")
}

model tmp_tp_status11 {
  id            Int       @id @default(autoincrement())
  tp_id         Int?
  receive_code  String?   @db.VarChar(50)
  serial_no     String?   @db.VarChar(50)
  serial_id     String?   @db.VarChar(64)
  status_id     Int?
  note          String?   @db.VarChar(255)
  resend_date   String?   @db.VarChar(255)
  user_truck_id Int?
  user_id       Int?
  create_date   DateTime? @db.DateTime(0)
  timestamp     DateTime? @db.Timestamp(0)
  resend_date1  DateTime? @db.Date

  @@index([id], map: "id")
  @@index([receive_code], map: "receive_code")
  @@index([tp_id], map: "tp_id")
}

model tmp_truck_count {
  id              Int             @id @default(autoincrement())
  truck_load_id   Int
  count           Int
  timestamp       DateTime?       @default(now()) @db.DateTime(0)
  tmp_trucks_data tmp_trucks_data @relation(fields: [truck_load_id], references: [truck_load_id], onDelete: Cascade, map: "fk_tmp_count_truck_load_id")

  @@index([timestamp], map: "idx_timestamp")
  @@index([truck_load_id], map: "idx_truck_load_id")
}

model tmp_trucks_data {
  create_date      DateTime?         @db.DateTime(0)
  truck_load_id    Int               @id
  license_plate    String?           @db.VarChar(50)
  fleet_card       String?           @db.VarChar(50)
  warehouse_id     Int?
  warehouse_name   String?           @db.VarChar(255)
  to_warehouse     String?           @db.VarChar(255)
  truck_code       String?           @db.VarChar(50)
  is_close         Boolean?
  close_datetime   DateTime?         @db.DateTime(0)
  is_go            Boolean?
  go_datetime      DateTime?         @db.DateTime(0)
  is_arrived       Boolean?
  arrived_datetime DateTime?         @db.DateTime(0)
  is_go_app        Boolean?
  go_app_datetime  DateTime?         @db.DateTime(0)
  go_app_by        String?           @db.VarChar(50)
  user_truck_id    Int?
  truck_id         Int?
  truck_number     String?           @db.VarChar(50)
  status           String?           @db.VarChar(50)
  is_deleted       Boolean?
  tmp_truck_count  tmp_truck_count[]

  @@index([create_date], map: "idx_create_date")
  @@index([is_arrived], map: "idx_is_arrived")
  @@index([is_go], map: "idx_is_go")
  @@index([license_plate], map: "idx_license_plate")
  @@index([status], map: "idx_status")
  @@index([warehouse_id], map: "idx_warehouse_id")
  @@index([warehouse_id, status], map: "idx_warehouse_status")
  @@index([truck_code], map: "truck_code")
}

model tmp_update_dc2dc {
  id                  Int       @id @default(autoincrement())
  receive_business_id Int?
  receive_walkin_id   Int?
  receive_code        String?   @db.VarChar(50)
  serial_id           String?   @unique(map: "serial_id") @db.VarChar(64)
  status_id           Int?
  timestamp           DateTime? @db.DateTime(0)

  @@index([id], map: "id")
}

model tmp_update_q_bank {
  id                Int    @default(autoincrement())
  receive_id        Int
  package_detail_id Int?
  size_max          Int?
  weight_max        Float?
  receive_detail_id Int    @unique(map: "receive_detail_id")

  @@id([id, receive_id, receive_detail_id])
  @@index([id], map: "id")
  @@index([receive_id], map: "receive_id")
}

model tmp_update_serial_no {
  id             Int       @id @default(autoincrement())
  serial_id      String    @db.VarChar(64)
  deleted_date   DateTime? @db.Date
  receive_date   DateTime? @db.Date
  serial_id_date String?   @db.VarChar(100)

  @@index([id], map: "id")
  @@index([serial_id], map: "serial_id")
}

model tmp_update_staus9 {
  id             Int     @default(autoincrement())
  serial_id      String  @db.VarChar(64)
  status_id      String? @db.VarChar(2)
  note           String? @db.VarChar(255)
  transection_id String? @default("") @db.VarChar(255)

  @@id([id, serial_id])
  @@index([id], map: "id")
  @@index([serial_id], map: "serial_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tmp_update_tm_resends {
  tmp_update_resends Int     @default(autoincrement())
  resend_id          String  @id @unique(map: "unq") @db.VarChar(20)
  status_id          String  @db.VarChar(2)
  serial_id          String? @db.VarChar(60)
  success_18_14      String? @default("Y") @db.VarChar(1)

  @@index([tmp_update_resends], map: "id")
  @@index([serial_id], map: "serial_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tmp_update_tm_tran_status11 {
  id                  Int
  receive_business_id String?   @db.VarChar(10)
  receive_walkin_id   String?   @db.VarChar(10)
  receive_code        String    @db.VarChar(50)
  serial_id           String?   @db.VarChar(64)
  serial_no           String?   @db.VarChar(50)
  status_id           Int?
  note                String?   @db.VarChar(255)
  resend_date1        DateTime? @db.Date
  resend_date         String?   @db.VarChar(255)
  user_truck_id       Bytes?    @db.VarBinary(10)
  frist_name          String?   @db.VarChar(255)
  last_name           String?   @db.VarChar(255)
  license_plate       String?   @db.VarChar(255)
  datetime            DateTime? @db.DateTime(0)

  @@id([receive_code, id])
  @@index([id], map: "id")
  @@index([receive_code], map: "tm_product_transactions_receive_code_IDX")
  @@index([serial_no], map: "tm_product_transactions_serial_no_IDX")
}

model tmp_update_transections {
  id                  Int       @id @default(autoincrement())
  receive_business_id Int?
  receive_walkin_id   Int?
  receive_code        String?   @db.VarChar(50)
  serial_id           String?   @unique(map: "serial_id") @db.VarChar(64)
  status_id           Int?
  timestamp           DateTime? @db.DateTime(0)

  @@index([id], map: "id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tp_product_transaction {
  id                  Int     @default(autoincrement())
  tp_transections_id  Int?
  serial_id           String  @db.VarChar(64)
  receive_business_id Int?
  receive_walkin_id   Int?
  receive_code        String? @db.VarChar(50)
  status_id           Int?

  @@id([id, serial_id])
  @@index([receive_business_id], map: "idx-receive-business-id")
  @@index([receive_walkin_id], map: "idx-receive-id")
  @@index([serial_id], map: "idx-serial_id")
  @@index([receive_code], map: "tm_product_transactions_receive_code_IDX")
  @@index([tp_transections_id], map: "tm_product_transections_id")
}

model trigger_log_receive_status {
  id   Int       @id @default(autoincrement())
  time DateTime? @db.DateTime(0)

  @@index([id], map: "id")
}

model tt_start1 {
  id                   Int                           @default(autoincrement())
  date_procress        DateTime?                     @db.DateTime(0)
  transactions_last_id Int?
  receive_business_id  Int?
  receive_walkin_id    Int?
  receive_code         String?                       @db.VarChar(50)
  serial_id            String                        @unique(map: "sn_id") @db.VarChar(64)
  serial_no            String                        @db.VarChar(50)
  reference_no         String?                       @db.VarChar(200)
  customer_id          Int?
  customer_code        String?                       @db.VarChar(50)
  customer_name        String?                       @db.VarChar(255)
  is_deleted           tt_start1_is_deleted?         @default(N)
  is_pickup_customer   tt_start1_is_pickup_customer? @default(N)
  is_pickup_shipper    tt_start1_is_pickup_shipper?  @default(N)
  cost                 Decimal?                      @db.Decimal(10, 2)
  from_warehouse_id    Int?
  warehouse_name       String?                       @db.VarChar(200)
  to_warehouse_id      Int?
  to_warehouse_name    String?                       @db.VarChar(200)
  recipient_name       String?                       @db.VarChar(255)
  address              String?                       @db.VarChar(1000)
  tambon_id            Int?
  tambon_name          String?                       @db.VarChar(150)
  ampur_id             Int?
  ampur_name           String?                       @db.VarChar(150)
  province_id          Int?
  province_name        String?                       @db.VarChar(150)
  zip_code             String?                       @db.VarChar(5)
  tel                  String?                       @db.VarChar(20)
  item_is_deleted      String?                       @db.VarChar(1)
  remark               String?                       @db.MediumText
  weight               Decimal?                      @db.Decimal(32, 2)
  width                Decimal?                      @db.Decimal(32, 2)
  cod                  Decimal?                      @db.Decimal(32, 2)
  length               Decimal?                      @db.Decimal(32, 2)
  height               Decimal?                      @db.Decimal(32, 2)
  q                    Decimal?                      @db.Decimal(32, 2)
  status_message       String?                       @db.VarChar(200)
  status_id            Int?
  datetime             DateTime?                     @db.DateTime(0)

  @@id([id, serial_id, serial_no])
  @@index([id], map: "id")
  @@index([serial_no], map: "sn")
  @@index([transactions_last_id], map: "transection_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tt_tokens {
  token_id         Int       @id @default(autoincrement())
  transaction_code String    @db.VarChar(50)
  token            String?   @db.Text
  created          DateTime? @default(now()) @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tt_tracking_processing_summary {
  id             Int       @id @default(autoincrement())
  warehouse_id   Int?
  process_date   DateTime? @db.Date
  created_at     DateTime? @db.DateTime(0)
  transaction_id String?   @db.VarChar(128)
  user_id        Int?
  oil_price      Float?
  oil_date       DateTime? @db.Date

  @@index([transaction_id], map: "tt_tracking_proces_idx_transaction_id")
  @@index([oil_date], map: "tt_tracking_processing_summary_oil_date_IDX")
  @@index([transaction_id], map: "tt_tracking_processing_summary_transaction_id_IDX")
  @@index([user_id], map: "tt_tracking_processing_summary_user_id_IDX")
}

model tt_tracking_transaction_logs {
  transaction_id String?   @db.VarChar(100)
  serial_id      String?   @db.VarChar(100)
  serial_no      String?   @db.VarChar(100)
  latitude       Float?
  longitude      Float?
  user_truck_id  Int?
  truck_load_id  String?   @db.VarChar(100)
  create_date    DateTime? @db.Timestamp(0)
  is_receive     String?   @db.Char(1)
  receive_by     Int?
  receive_date   DateTime? @db.Timestamp(0)
  id             Int       @id @default(autoincrement())
  cost           Float?    @default(0.000)
  receive_code   String?   @db.VarChar(100)

  @@index([create_date], map: "tt_tracking_transaction_logs_create_date_IDX")
  @@index([receive_code], map: "tt_tracking_transaction_logs_receive_code_IDX")
  @@index([serial_id], map: "tt_tracking_transaction_logs_serial_id_IDX")
  @@index([transaction_id], map: "tt_tracking_transaction_logs_transaction_id_IDX")
  @@index([truck_load_id], map: "tt_tracking_transaction_logs_truck_load_id_IDX")
  @@index([user_truck_id], map: "tt_tracking_transaction_logs_user_truck_id_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tt_tracking_transactions {
  id                 Int       @id @default(autoincrement())
  alat               Float?
  alng               Float?
  blat               Float?
  blng               Float?
  receive_code       String?   @db.VarChar(50)
  recipient_name     String?   @db.VarChar(150)
  address            String?   @db.VarChar(255)
  tambon_name        String?   @db.VarChar(50)
  ampur_name         String?   @db.VarChar(50)
  province_name      String?   @db.VarChar(50)
  to_warehouse_id    Int?
  zip_code           String?   @db.VarChar(10)
  status_id          Int?
  status_name        String?   @db.VarChar(50)
  send_date          DateTime? @db.DateTime(0)
  user_truck_id      Int?
  distance           Float?
  duration           Float?
  sum_distance       Float?
  transaction_id     String?   @db.VarChar(128)
  route_id           Int?
  route_code         String?   @db.VarChar(10)
  route_oil_distance Float?
  truck_code         String?   @db.VarChar(20)
  tx_idx             Int?
  payment_type_id    Int?
  cod                Float?    @default(0.000)
  cost               Float?    @default(0.000)
  is_receive         String?   @db.Char(1)
  receive_by         Int?
  receive_date       DateTime? @db.Timestamp(0)
  total_serial       Int?      @default(0)

  @@index([transaction_id, user_truck_id, route_id], map: "tt_tracking_transa_idx_transactio_user_id_route_id")
  @@index([user_truck_id, transaction_id], map: "tt_tracking_transa_idx_user_id_transaction_id")
  @@index([user_truck_id, truck_code], map: "tt_tracking_transa_idx_user_id_truck_code")
  @@index([is_receive], map: "tt_tracking_transactions_is_receive_IDX")
  @@index([payment_type_id], map: "tt_tracking_transactions_payment_type_id_IDX")
  @@index([receive_code], map: "tt_tracking_transactions_receive_code_IDX")
  @@index([recipient_name], map: "tt_tracking_transactions_recipient_name_IDX")
  @@index([route_code], map: "tt_tracking_transactions_route_code_IDX")
  @@index([route_id], map: "tt_tracking_transactions_route_id_IDX")
  @@index([send_date], map: "tt_tracking_transactions_send_date_IDX")
  @@index([status_id], map: "tt_tracking_transactions_status_id_IDX")
  @@index([to_warehouse_id], map: "tt_tracking_transactions_to_warehouse_id_IDX")
  @@index([transaction_id], map: "tt_tracking_transactions_transaction_id_IDX")
  @@index([truck_code], map: "tt_tracking_transactions_truck_code_IDX")
  @@index([tx_idx], map: "tt_tracking_transactions_tx_idx_IDX")
  @@index([user_truck_id], map: "tt_tracking_transactions_user_truck_id_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tt_trackings {
  tracking_id    Int     @id @default(autoincrement())
  latitude       Float?
  longitude      Float?
  user_truck_id  Int?
  device_model   String? @db.VarChar(50)
  imei           String? @db.VarChar(255)
  time           BigInt?
  accuracy       Float?
  altitude       Float?
  speed          Float?
  speed_accuracy Float?
  heading        Float?
  server_time    BigInt?

  @@index([user_truck_id], map: "tt_trackings_user_truck_id_IDX")
}

model tt_vrp_jobs {
  vrp_id         String   @db.VarChar(100)
  receive_code   String?  @db.Text
  vrp_truck_id   Decimal? @db.Decimal(10, 0)
  lat            Float?
  lng            Float?
  province_name  String?  @db.VarChar(200)
  recipient_code String?  @db.VarChar(100)
  recipient_name String?  @db.VarChar(200)
  tambon_id      Decimal? @db.Decimal(10, 0)
  tambon_name    String?  @db.VarChar(200)
  total_bill     Decimal? @default(0) @db.Decimal(10, 0)
  total_serials  Decimal? @default(0) @db.Decimal(10, 0)
  total_volume   Decimal? @default(0) @db.Decimal(10, 0)
  ampur_name     String?  @db.VarChar(200)
  created_at     DateTime @default(now()) @db.DateTime(0)
  vrp_job_id     Int      @id @default(autoincrement())
  q              Decimal? @db.Decimal(10, 0)
  tel            String?  @db.VarChar(100)
  address        String?  @db.VarChar(250)

  @@index([tambon_id], map: "tt_vrp_jobs_tambon_id_IDX")
  @@index([vrp_id], map: "tt_vrp_jobs_vrp_id_IDX")
}

model tt_vrp_summary {
  vrp_id       String    @id @db.VarChar(100)
  amount       Decimal?  @default(0) @db.Decimal(10, 0)
  delivery     Decimal?  @default(0) @db.Decimal(10, 0)
  routes       Decimal?  @default(0) @db.Decimal(10, 0)
  unassigned   Decimal?  @default(0) @db.Decimal(10, 0)
  created_at   DateTime? @default(now()) @db.DateTime(0)
  warehouse_id Int?
  start_date   DateTime? @db.Date
  end_date     DateTime? @db.Date

  @@index([warehouse_id], map: "tt_vrp_summary_warehouse_id_IDX")
}

model tt_vrp_trucks {
  vrp_id                 String    @db.VarChar(100)
  id                     Decimal   @db.Decimal(10, 0)
  license_plate          String    @db.VarChar(100)
  maximum_loading_volume Decimal?  @db.Decimal(10, 0)
  created_at             DateTime? @default(now()) @db.DateTime(0)
  vrp_truck_id           Int       @id @default(autoincrement())

  @@index([id], map: "tt_vrp_trucks_id_IDX")
  @@index([license_plate], map: "tt_vrp_trucks_license_plate_IDX")
}

model tt_vrp_unassigned {
  vrp_id            String   @db.VarChar(100)
  receive_code      String?  @db.Text
  lat               Float?
  lng               Float?
  province_name     String?  @db.VarChar(200)
  recipient_code    String?  @db.VarChar(100)
  recipient_name    String?  @db.VarChar(200)
  tambon_id         Decimal? @db.Decimal(10, 0)
  tambon_name       String?  @db.VarChar(200)
  total_bill        Decimal? @default(0) @db.Decimal(10, 0)
  total_serials     Decimal? @default(0) @db.Decimal(10, 0)
  total_volume      Decimal? @default(0) @db.Decimal(10, 0)
  ampur_name        String?  @db.VarChar(200)
  created_at        DateTime @default(now()) @db.DateTime(0)
  vrp_unassigned_id Int      @id @default(autoincrement())
  tel               String?  @db.VarChar(100)
  address           String?  @db.VarChar(250)

  @@index([tambon_id], map: "tt_vrp_unassigned_tambon_id_IDX")
  @@index([vrp_id], map: "tt_vrp_unassigned_vrp_id_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_customer_cod {
  id              Int                         @id @default(autoincrement())
  cid             String?                     @db.VarChar(13)
  account_name    String?                     @db.VarChar(250)
  account_number  String?                     @db.VarChar(20)
  account_branch  String?                     @db.VarChar(200)
  account_bank_id Int?
  account_type_id Int?
  is_actived      um_customer_cod_is_actived? @default(Y)
  is_deleted      um_customer_cod_is_deleted? @default(N)
  first_name      String?                     @db.VarChar(255)
  last_name       String?                     @db.VarChar(255)
  title_id        Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_customer_credit_transactions {
  customer_credit_log_id Int      @id @default(autoincrement())
  customer_id            Int?
  credit_in              Decimal? @db.Decimal(32, 16)
  credit_out             Decimal? @db.Decimal(32, 16)
  credit_balance         Decimal? @db.Decimal(32, 16)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_customer_credits {
  customer_credit_id Int      @id @default(autoincrement())
  customer_id        Int?
  credit             Decimal? @db.Decimal(32, 16)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_customer_payment_types {
  customer_id     Int?
  payment_type_id Int?
  id              Int  @id @default(autoincrement())
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_customer_topup {
  id               Int                       @id @default(autoincrement())
  datetime         DateTime?                 @db.DateTime(0)
  account_id       Int?
  amount           Decimal?                  @db.Decimal(32, 16)
  image_url        String?                   @db.VarChar(200)
  customer_id      Int?
  status           um_customer_topup_status?
  create_date      DateTime?                 @db.DateTime(0)
  user_id_approved Int?
  date_approved    DateTime?                 @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_customer_types {
  id        Int                          @id
  name      String                       @db.VarChar(255)
  is_delete um_customer_types_is_delete? @default(N)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_customer_wallet_transactions {
  customer_wallet_log_id Int       @id @default(autoincrement())
  customer_id            Int?
  wallet_in              Decimal?  @db.Decimal(32, 16)
  wallet_out             Decimal?  @db.Decimal(32, 16)
  wallet_balance         Decimal?  @db.Decimal(32, 16)
  transection_type       String?   @db.VarChar(50)
  document_ref_id        Int?
  document_ref_no        String?   @db.VarChar(50)
  create_date            DateTime? @default(now()) @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_customer_wallets {
  customer_credit_id Int      @id @default(autoincrement())
  customer_id        Int?
  wallet             Decimal? @db.Decimal(32, 16)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_customers {
  customer_id        Int                          @id @default(autoincrement())
  customer_code      String?                      @db.VarChar(50)
  customer_name      String?                      @db.VarChar(255)
  address            String?                      @db.VarChar(500)
  tambon_id          Int?
  ampur_id           Int?
  province_id        Int?
  zip_code           String?                      @db.VarChar(255)
  tel_1              String?                      @db.VarChar(255)
  is_deleted         um_customers_is_deleted?     @default(N)
  prefix             String?                      @db.VarChar(20)
  contact_name       String?                      @db.VarChar(100)
  contact_tel        String?                      @db.VarChar(100)
  cod                Int?
  credit             Decimal?                     @default(0.0000000000000000) @db.Decimal(32, 16)
  customer_types     um_customers_customer_types? @default(NORMAL_PERSON)
  company_id         Int?
  customer_cid       String?                      @db.VarChar(13)
  customer_name_en   String?                      @db.VarChar(255)
  tax                String?                      @db.VarChar(255)
  customer_nick_name String?                      @db.VarChar(255)
  industry_type_id   Int?
  tel_2              String?                      @db.VarChar(255)
  tel_mobile         String?                      @db.VarChar(255)
  latitude           String?                      @db.VarChar(255)
  longitude          String?                      @db.VarChar(255)
  line               String?                      @db.VarChar(255)
  email              String?                      @db.VarChar(255)
  cal_weight         String?                      @default("PLUS") @db.VarChar(20)
  customer_type      um_customers_customer_type?
  location_name      String?                      @db.VarChar(255)
  room_no            String?                      @db.VarChar(255)
  floor              String?                      @db.VarChar(255)
  no                 String?                      @db.VarChar(255)
  village            String?                      @db.VarChar(255)
  moo                String?                      @db.VarChar(255)
  soi                String?                      @db.VarChar(255)
  road               String?                      @db.VarChar(255)
  warehouse_id       Int?
  is_cod             um_customers_is_cod?         @default(Y)
  type               um_customers_type?
  group              String?                      @db.VarChar(255)
  corporate_group    String?                      @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_departments {
  id         Int                        @id @default(autoincrement())
  name       String?                    @db.VarChar(200)
  is_deleted um_departments_is_deleted? @default(N)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_employees {
  employee_id    Int                      @id @default(autoincrement())
  employee_code  String?                  @db.VarChar(50)
  cid            String?                  @db.VarChar(13)
  title_id       Int?
  first_name     String?                  @db.VarChar(200)
  last_name      String?                  @db.VarChar(200)
  nick_name      String?                  @db.VarChar(200)
  gender         String?                  @db.VarChar(50)
  status         um_employees_status?
  birthday       DateTime?                @db.Date
  email          String?                  @db.VarChar(200)
  telephone      String?                  @db.VarChar(20)
  driver_license String?                  @db.VarChar(255)
  position_id    Int?
  department_id  Int?
  image_employee String?                  @db.VarChar(255)
  is_deleted     um_employees_is_deleted? @default(N)
  warehouse_id   Int?
  address        String?                  @db.VarChar(255)
  black_list     um_employees_black_list? @default(N)
  work_start     DateTime?                @db.Date
  recipient      String?                  @db.VarChar(255)
  document1      String?                  @db.VarChar(255)
  document2      String?                  @db.VarChar(255)
  document3      String?                  @db.VarChar(255)
  tambon_id      Int?
  ampur_id       Int?
  province_id    Int?
  zip_code       String?                  @db.VarChar(255)
  head_quarters  String?                  @db.VarChar(1)
  remark         String?                  @db.VarChar(255)
  datetimestamp  DateTime?                @db.Timestamp(0)
  create_by_user String?                  @db.VarChar(255)
  lineid         String?                  @db.VarChar(20)
  levelid        um_employees_levelid?    @default(dbgenerated("0"))
  work_end       DateTime?                @db.Date

  @@index([ampur_id], map: "um_employees_ampur_id_IDX")
  @@index([employee_id], map: "um_employees_idx_employee_id")
  @@index([is_deleted], map: "um_employees_is_deleted_IDX")
  @@index([position_id], map: "um_employees_position_id_IDX")
  @@index([province_id], map: "um_employees_province_id_IDX")
  @@index([tambon_id], map: "um_employees_tambon_id_IDX")
  @@index([title_id], map: "um_employees_title_id_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_group_rights {
  group_id        Int
  right_detail_id Int

  @@id([group_id, right_detail_id])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_groups {
  group_id     Int                   @id @default(autoincrement())
  group_name   String?               @db.VarChar(255)
  user_type_id Int?
  is_actived   um_groups_is_actived? @default(Y)
  is_deleted   um_groups_is_deleted? @default(N)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_industry_types {
  id         Int                          @id @default(autoincrement())
  name       String                       @db.VarChar(255)
  is_deleted um_industry_types_is_deleted @default(N)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_peoples {
  people_id   Int                    @id @default(autoincrement())
  title_id    Int?
  first_name  String?                @db.VarChar(100)
  last_name   String?                @db.VarChar(100)
  create_date DateTime?              @default(now()) @db.DateTime(0)
  is_actived  um_peoples_is_actived? @default(Y)
  is_deleted  um_peoples_is_deleted? @default(N)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_positions {
  id         Int                      @id @default(autoincrement())
  name       String?                  @db.VarChar(200)
  is_deleted um_positions_is_deleted? @default(N)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_right_details {
  right_detail_id   Int     @id @default(autoincrement())
  right_id          Int?
  right_detail_code String? @db.VarChar(100)
  right_detail_name String? @db.VarChar(200)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_rights {
  right_id   Int     @id @default(autoincrement())
  right_name String? @db.VarChar(200)
  right_code String? @db.VarChar(100)
  right_type String? @db.VarChar(100)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_rights_copy2 {
  right_id          Int     @id @default(autoincrement())
  right_name        String? @db.VarChar(200)
  right_code        String? @db.VarChar(100)
  right_type        String? @db.VarChar(100)
  right_sub_type    String? @db.VarChar(100)
  right_name_detail String? @db.VarChar(200)
  group_id          Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_titles {
  id         Int                   @id @default(autoincrement())
  name       String?               @db.VarChar(200)
  is_actived um_titles_is_actived? @default(Y)
  is_deleted um_titles_is_deleted? @default(N)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_user_credits {
  user_credit_id Int      @id @default(autoincrement())
  user_id        Int?
  credit         Decimal? @db.Decimal(32, 16)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_user_rights {
  user_id         Int
  right_detail_id Int
  expired_date    DateTime?                  @db.Date
  is_expired      um_user_rights_is_expired? @default(N)

  @@id([user_id, right_detail_id])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_user_topup {
  id               Int                   @id @default(autoincrement())
  datetime         DateTime?             @db.DateTime(0)
  account_id       Int?
  amount           Decimal?              @db.Decimal(32, 16)
  image_url        String?               @db.VarChar(200)
  status           um_user_topup_status?
  create_date      DateTime?             @default(now()) @db.DateTime(0)
  user_id_approved Int?
  date_approved    DateTime?             @db.DateTime(0)
  user_id          Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_user_trucks {
  user_truck_id Int                        @id @default(autoincrement())
  truck_id      Int
  user_id       Int
  is_deleted    um_user_trucks_is_deleted? @default(N)
  create_date   DateTime?                  @default(now()) @db.DateTime(0)
  update_date   DateTime?                  @db.DateTime(0)

  @@index([user_truck_id], map: "um_user_trucks_idx_user_id")
  @@index([truck_id], map: "um_user_trucks_truck_id_IDX")
  @@index([user_id], map: "um_user_trucks_user_id_IDX")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_user_types {
  id       Int     @id @default(autoincrement())
  name     String? @db.VarChar(255)
  namethai String? @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_user_wallet_transactions {
  wallet_log_id    Int       @id @default(autoincrement())
  user_id          Int?
  wallet_in        Decimal?  @db.Decimal(32, 16)
  wallet_out       Decimal?  @db.Decimal(32, 16)
  wallet_balance   Decimal?  @db.Decimal(32, 16)
  transection_type String?   @db.VarChar(50)
  document_ref_id  Int?
  document_ref_no  String?   @db.VarChar(50)
  create_date      DateTime? @default(now()) @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_user_wallets {
  wallet_id Int      @id @default(autoincrement())
  user_id   Int?
  wallet    Decimal? @db.Decimal(32, 16)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_user_warehouse_rights {
  user_warehouse_id Int
  right_detail_id   Int
  expired_date      DateTime?                            @db.Date
  is_expired        um_user_warehouse_rights_is_expired? @default(N)

  @@id([user_warehouse_id, right_detail_id])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_user_warehouses {
  user_warehouse_id Int  @id @default(autoincrement())
  user_id           Int?
  warehouse_id      Int?
  group_id          Int?
  warehouse_sub_id  Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model um_users {
  user_id        Int                  @id @default(autoincrement())
  username       String?              @db.VarChar(50)
  password       String?              @db.VarChar(64)
  people_id      Int?
  group_id       Int?
  user_type      um_users_user_type?
  customer_id    Int?
  is_actived     um_users_is_actived? @default(Y)
  is_deleted     um_users_is_deleted? @default(N)
  create_date    DateTime?            @default(now()) @db.DateTime(0)
  create_user_id Int?
  updat_date     DateTime?            @db.DateTime(0)
  update_user_id Int?
  credit         Decimal?             @db.Decimal(32, 16)
  end_date       DateTime?            @db.Date
  warehouse_id   Int?
  timestamp      DateTime?            @db.Timestamp(0)
  note           String?              @db.VarChar(255)
  employee_id    Int?

  @@index([employee_id], map: "employee_id")
  @@index([people_id], map: "idx-people_id")
  @@index([user_id], map: "um_users_idx_user_id")
  @@index([is_actived], map: "um_users_is_actived_IDX")
  @@index([is_deleted], map: "um_users_is_deleted_IDX")
  @@index([password], map: "um_users_password_IDX")
  @@index([username], map: "um_users_username_IDX")
  @@index([warehouse_id], map: "um_users_warehouse_id_IDX")
}

model update_reference_log {
  log_id             Int       @id @default(autoincrement())
  start_time         DateTime? @db.DateTime(0)
  end_time           DateTime? @db.DateTime(0)
  total_updated_rows Int?
  log_message        String?   @db.Text
  ip_address         String?   @db.VarChar(45)
  run_by_user        String?   @db.VarChar(100)

  @@index([start_time], map: "idx_start_time")
  @@index([run_by_user, ip_address], map: "idx_user_ip")
}

model web_count {
  id                  Int                            @default(autoincrement())
  receive_date        DateTime                       @db.Date
  delivery_date       DateTime                       @db.Date
  datediff            Int?
  month_year          String                         @db.VarChar(6)
  serial_id           String                         @unique(map: "serial_id") @db.VarChar(64)
  customer_id         Int?
  from_warehouse_id   Int?
  to_warehouse_id     Int?
  cost                Decimal                        @db.Decimal(10, 2)
  item_is_deleted     String?                        @db.VarChar(1)
  is_returned         String?                        @db.VarChar(1)
  payment_type_id     Int?
  package_name        String?                        @db.VarChar(255)
  package_detail_name String?                        @db.VarChar(255)
  province_id         Int
  shipper_id          Int?
  weight              Float?
  width               Float?
  length              Float?
  height              Float?
  q                   String?                        @db.VarChar(255)
  count               Decimal?                       @db.Decimal(32, 2)
  deleted_date        DateTime?                      @db.DateTime(0)
  deleted_by_user     String?                        @db.VarChar(70)
  remark              String?                        @db.LongText
  web_count_is_delete web_count_web_count_is_delete? @default(N)
  dalete_date         DateTime?                      @db.DateTime(0)

  @@id([id, serial_id, receive_date, delivery_date, province_id, cost, month_year])
  @@index([customer_id], map: "customer_id")
  @@index([id], map: "id")
  @@index([shipper_id], map: "shipper_id")
  @@index([from_warehouse_id, to_warehouse_id], map: "warehouse_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model wm_receives_details {
  id            Int  @id @default(autoincrement())
  wm_receive_id Int?
  generic_id    Int?
  qty           Int?
}

enum mm_ispickup_customer_is_pickup_customer {
  Y
  N
}

enum mm_ispickup_shipper_is_pickup_shipper {
  Y
  N
}

enum at_creditors_is_actived {
  Y
  N
}

enum at_receipt_co_payments_type {
  MONEY
  CHEQUE
  TAX_WITH_HOLDING
  FEE
  TRANFER
}

enum at_receipt_payments_type {
  MONEY
  CHEQUE
  TAX_WITH_HOLDING
  FEE
  TRANFER
}

enum mm_accident_types_is_deleted {
  Y
  N
}

enum mm_banks_account_types_is_deleted {
  Y
  N
}

enum mm_complaint_types_is_deleted {
  Y
  N
}

enum mm_cost_other_is_deleted {
  Y
  N
}

enum mm_delivery_types_is_deleted {
  Y
  N
}

enum mm_driver_truck_is_deleted {
  Y
  N
}

enum mm_ex_curier_reason_is_delete {
  Y
  N
}

enum mm_geographies_is_actived {
  Y
  N
}

enum mm_payments_is_actived {
  Y
  N
}

enum mm_reason_sends_is_deleted {
  Y
  N
}

enum mm_receive_types_is_deleted {
  Y
  N
}

enum mm_recipient_types_is_deleted {
  Y
  N
}

enum mm_routes_dc_to_dc_is_deleted {
  Y
  N
}

enum mm_shipper_types_is_deleted {
  Y
  N
}

enum mm_truck_types_is_deleted {
  Y
  N
}

enum mm_units_is_deleted {
  Y
  N
}

enum sys_token_is_actived {
  Y
  N
}

enum tm_palettes_is_actived {
  N
  Y
}

enum tm_receive_imports_status {
  PENDING
  SUCCESS
}

enum tm_screens_is_deleted {
  Y
  N
}

enum um_customer_types_is_delete {
  Y
  N
}

enum um_departments_is_deleted {
  Y
  N
}

enum um_industry_types_is_deleted {
  Y
  N
}

enum um_positions_is_deleted {
  Y
  N
}

enum um_titles_is_actived {
  Y
  N
}

enum at_costs_is_actived {
  Y
  N
}

enum at_creditors_is_deleted {
  Y
  N
}

enum mm_accidents_is_check {
  Y
  N
}

enum mm_banks_is_deleted {
  Y
  N
}

enum mm_banks_account_types_is_actived {
  Y
  N
}

enum mm_ex_curier_reason_is_active {
  Y
  N
}

enum mm_geographies_is_deleted {
  Y
  N
}

enum mm_image_document_return_details_is_deleted {
  Y
  N
}

enum mm_image_document_returns_is_deleted {
  Y
  N
}

enum mm_l_edit_table_type_is_actived {
  Y
  N
}

enum mm_payments_is_deleted {
  Y
  N
}

enum mm_price_fast12_is_actived {
  Y
  N
}

enum mm_routes_is_deleted {
  Y
  N
}

enum mm_routes_2024_11_19_is_deleted {
  Y
  N

  @@map("mm_routes_2024-11-19_is_deleted")
}

enum mm_routes_old_is_deleted {
  Y
  N
}

enum mm_warehouse_mapping_is_deleted {
  Y
  N
}

enum mm_warehouse_mapping_old_is_deleted {
  Y
  N
}

enum mm_warehouse_subs_is_deleted {
  Y
  N
}

enum nm_gasolines_is_deleted {
  Y
  N
}

enum tm_bill_details_is_pay {
  Y
  N
}

enum tm_delivery_note_express_serials_is_receive {
  Y
  N
}

enum tm_delivery_note_serials_is_receive {
  Y
  N
}

enum tm_deliverys_is_close {
  Y
  N
}

enum tm_palettes_is_deleted {
  Y
  N
}

enum tm_receive_imports_is_deleted {
  Y
  N
}

enum tm_screen_details_is_receive {
  Y
  N
}

enum tm_trucks_is_close {
  Y
  N
}

enum um_groups_is_actived {
  Y
  N
}

enum um_titles_is_deleted {
  Y
  N
}

enum um_user_rights_is_expired {
  Y
  N
}

enum um_user_trucks_is_deleted {
  Y
  N
}

enum um_user_warehouse_rights_is_expired {
  Y
  N
}

enum at_costs_is_deleted {
  Y
  N
}

enum mm_banks_is_actived {
  Y
  N
}

enum mm_ex_curier_is_delete {
  Y
  N
}

enum mm_image_document_return_details_status {
  SEND
  RETURN
  BLANK
}

enum mm_l_edit_table_type_is_deleted {
  Y
  N
}

enum mm_line_id_is_actived {
  Y
  N
}

enum mm_line_message_is_actived {
  Y
  N
}

enum mm_package_business_is_deleted {
  Y
  N
}

enum mm_package_businessxxx_is_deleted {
  Y
  N
}

enum mm_package_express_is_deleted {
  Y
  N
}

enum mm_package_express_settings_is_deleted {
  Y
  N
}

enum mm_packages_is_deleted {
  Y
  N
}

enum mm_payments_is_business {
  Y
  N
}

enum mm_price_fast12_is_deleted {
  Y
  N
}

enum mm_report_customers_is_deleted {
  Y
  N
}

enum mm_warehouse_num_days_is_deleted {
  N
  Y
}

enum nm_gasolines_is_approved {
  Y
  N
}

enum sys_login_logs_status {
  CUSTOMER_SUCCESS @map("CUSTOMER-SUCCESS")
  CUSTOMER_WRONG   @map("CUSTOMER-WRONG")
  CUSTOMER_ERROR   @map("CUSTOMER-ERROR")
  DC_SUCCESS       @map("DC-SUCCESS")
  DC_WRONG         @map("DC-WRONG")
  DC_ERROR         @map("DC-ERROR")
  SUCCESS
  WRONG
  ERROR
}

enum tm_bills_is_completed {
  Y
  N
}

enum tm_book_details_status {
  PENDING
  COMPLETE
}

enum tm_deliverys_is_approved {
  Y
  N
}

enum tm_product_trucks_status {
  PENDDING
  RESEND
  SENDBACK
}

enum tm_product_trucks_temp_status {
  PENDDING
  RESEND
  SENDBACK
}

enum tm_receive_detail_items_is_deleted {
  N
  Y
}

enum tm_receive_import_detail_items_is_deleted {
  N
  Y
}

enum tm_truck_details_is_receive {
  Y
  N
}

enum tm_trucks_is_completed {
  Y
  N
}

enum um_groups_is_deleted {
  Y
  N
}

enum at_accounts_is_pay {
  Y
  N
}

enum mm_ampur_is_deleted {
  Y
  N
}

enum mm_ex_curier_is_active {
  Y
  N
}

enum mm_l_edit_table_type_is_business {
  Y
  N
}

enum mm_line_id_is_deleted {
  Y
  N
}

enum mm_line_message_is_deleted {
  Y
  N
}

enum mm_payments_is_express {
  Y
  N
}

enum mm_province_is_deleted {
  Y
  N
}

enum mm_province_zone_is_deleted {
  Y
  N
}

enum mm_recipients_is_deleted {
  Y
  N
}

enum mm_tambon_is_deleted {
  Y
  N
}

enum mm_tambon_days_count {
PLACEHOLDER
}

enum mm_tambon_days_2024_11_07_count {

PLACEHOLDER
  @@map("mm_tambon_days_2024-11-07_count")
}

enum mm_tambon_days_old_2024_08_06__count {

PLACEHOLDER
  @@map("mm_tambon_days_old(2024-08-06)_count")
}

enum mm_warehouses_is_actived {
  Y
  N
}

enum mm_warehouses_to_is_actived {
  Y
  N
}

enum mm_warehouses_too_is_actived {
  Y
  N
}

enum nm_damage_products_is_deleted {
  Y
  N
}

enum sys_login_logs_is_mobile {
  Y
  N
}

enum tm_bills_is_deleted {
  Y
  N
}

enum tm_palettes_is_success {
  N
  Y
}

enum um_peoples_is_actived {
  Y
  N
}

enum um_user_topup_status {
  WAIT
  APPROVE
  CANCEL
}

enum um_users_user_type {
  ADMIN
  DC
  CUSTOMER
  TRUCK
}

enum at_accounts_is_receipt {
  Y
  N
}

enum mm_ampur_is_actived {
  Y
  N
}

enum mm_drivers_is_deleted {
  Y
  N
}

enum mm_holiday_is_deleted {
  Y
  N
}

enum mm_l_edit_table_type_is_express {
  Y
  N
}

enum mm_province_is_actived {
  Y
  N
}

enum mm_province_zone_is_actived {
  Y
  N
}

enum mm_tambon_is_actived {
  Y
  N
}

enum mm_warehouses_is_deleted {
  Y
  N
}

enum mm_warehouses_to_is_deleted {
  Y
  N
}

enum mm_warehouses_too_is_deleted {
  Y
  N
}

enum sys_login_logs_is_tablet {
  Y
  N
}

enum tm_budget_transaction_customers_is_receive {
  Y
  N
}

enum tm_import_receives_is_cod {
  Y
  N
}

enum tm_truck_checks_status {
  CHECK_IN
  CHECK_OUT
}

enum um_customer_topup_status {
  WAIT
  APPROVE
  CANCEL
}

enum um_peoples_is_deleted {
  Y
  N
}

enum at_accounts_is_deleted {
  Y
  N
}

enum at_billings_is_completed {
  Y
  N
}

enum at_invoices_is_billing {
  Y
  N
}

enum audit_report_tk_status_message {
  CUSTOMER_TRUCK
  DC_TRUCK_DC
  DC_TRUCK
  SHIPPER_TRUCK
  RECIPIENT_TRUCK
}

enum l_edit_mobile_update2_l_edit_table {
  Y
  N
}

enum mm_accidents_is_deleted {
  N
  Y
}

enum mm_drivers_is_actived {
  Y
  N
}

enum mm_holiday_is_actived {
  Y
  N
}

enum mm_tambon_tambon_paid {
  Y
  N
}

enum nm_lost_product_is_deleted {
  Y
  N
}

enum pm_posts_user_type {
  CUSTOMER
  ADMIN
  CHATBOT
}

enum sys_login_logs_is_desktop {
  Y
  N
}

enum tm_budget_transaction_customers_is_pay {
  Y
  N
}

enum tm_do_not_sends_is_send {
  N
  Y
}

enum tm_job_dc_to_dc_is_completed {
  Y
  N
}

enum tm_receive_invoice_is_complete {
  Y
  N
}

enum tm_receive_walkin_details_is_cod {
  Y
  N
}

enum tm_truck_details_manul_recive {
  Y
  N
}

enum tm_trucks_status {
  CUSTOMER_TRUCK
  DC_TRUCK_DC
  DC_TRUCK
  SHIPPER_TRUCK
  RECIPIENT_TRUCK
}

enum um_customer_cod_is_actived {
  Y
  N
}

enum um_users_is_actived {
  Y
  N
}

enum at_accounts_is_actived {
  Y
  N
}

enum at_billings_is_vat {
  Y
  N
}

enum at_invoices_is_vat {
  Y
  N
}

enum tm_books_is_deleted {
  Y
  N
}

enum tm_delivery_note_express_is_deleted {
  Y
  N
}

enum tm_delivery_notes_is_deleted {
  Y
  N
}

enum tm_import_receives_is_document_return {
  Y
  N
}

enum tm_job_dc_to_dc_is_deleted {
  N
  Y
}

enum tm_jobs_status {
  CUSTOMER_DC
  DC_CUSTOMER
  DC_DC
}

enum tm_screens_is_move {
  Y
  N
}

enum um_customer_cod_is_deleted {
  Y
  N
}

enum um_employees_status {
  single
  marry
  divorce
}

enum um_users_is_deleted {
  Y
  N
}

enum at_receipt_cos_is_crash {
  Y
  N
}

enum mm_package_details_is_deleted {
  Y
  N
}

enum mm_packages_is_document_return {
  Y
  N
}

enum mm_tambon_days_is_deleted {
  Y
  N
}

enum mm_tambon_days_2024_11_07_is_deleted {
  Y
  N

  @@map("mm_tambon_days_2024-11-07_is_deleted")
}

enum mm_tambon_days_old_2024_08_06__is_deleted {
  Y
  N

  @@map("mm_tambon_days_old(2024-08-06)_is_deleted")
}

enum mm_trucks_is_deleted {
  Y
  N
}

enum tm_books_is_approved {
  Y
  N
}

enum tm_delivery_note_express_is_approved {
  Y
  N
}

enum tm_delivery_notes_is_approved {
  Y
  N
}

enum tm_job_dc_to_cus_is_completed {
  Y
  N
}

enum tm_screens_is_go {
  Y
  N
}

enum um_customers_is_deleted {
  Y
  N
}

enum at_creditors_tax_type {
  HEAD
  SUB
  INDIVIDUAL
}

enum mm_package_business_is_actived {
  Y
  N
}

enum mm_package_businessxxx_is_actived {
  Y
  N
}

enum mm_package_express_is_actived {
  Y
  N
}

enum mm_package_express_settings_is_actived {
  Y
  N
}

enum mm_packages_type {
  EXPRESS
  BUSINESS
}

enum mm_tambon_days_is_actived {
  Y
  N
}

enum mm_tambon_days_2024_11_07_is_actived {
  Y
  N

  @@map("mm_tambon_days_2024-11-07_is_actived")
}

enum mm_tambon_days_old_2024_08_06__is_actived {
  Y
  N

  @@map("mm_tambon_days_old(2024-08-06)_is_actived")
}

enum mm_trucks_is_actived {
  Y
  N
}

enum tm_job_cus_to_dc_is_completed {
  Y
  N
}

enum tm_job_dc_to_cus_is_deleted {
  N
  Y
}

enum tm_line_transactions_type {
  SENT
  RECEIVE
}

enum tm_product_transactions_type {
  PUBLIC
  PRIVATE
}

enum tm_product_transactions_cencel_type {
  PUBLIC
  PRIVATE
}

enum tm_product_transactions_ex_type {
  PUBLIC
  PRIVATE
}

enum tm_product_transactions_last_type {
  PUBLIC
  PRIVATE
}

enum tm_product_transactions_status8_type {
  PUBLIC
  PRIVATE
}

enum tm_resends_not_in_14_18_success_18_14 {
  Y
  N
}

enum tmp_product_transactions_type {
  PUBLIC
  PRIVATE
}

enum tmp_product_transactions_ro_type {
  PUBLIC
  PRIVATE
}

enum mm_packages_is_actived {
  Y
  N
}

enum mm_trucks_type {
  TRANSPORT
  DELIVERY
}

enum tm_import_receives_is_deleted {
  Y
  N
}

enum tm_job_cus_to_dc_is_deleted {
  N
  Y
}

enum tm_job_shipper_to_dc_is_completed {
  Y
  N
}

enum tm_trucks_is_go {
  Y
  N
}

enum mm_recipient_details_is_deleted {
  Y
  N
}

enum mm_shippers_is_deleted {
  Y
  N
}

enum tm_books_book_status {
  READY
  PENDING
  COMPLETE
}

enum tm_job_cus_to_dc_is_pickup {
  CUSTOMER
  SHIPPER
}

enum tm_job_shipper_to_dc_is_deleted {
  N
  Y
}

enum tm_jobs_is_completed {
  Y
  N
}

enum tt_start1_is_deleted {
  Y
  N
}

enum mm_recipient_details_is_default {
  Y
  N
}

enum tm_job_shipper_to_dc_is_pickup {
  CUSTOMER
  SHIPPER
}

enum tm_jobs_is_deleted {
  N
  Y
}

enum tt_start1_is_pickup_customer {
  Y
  N
}

enum at_cos_is_deleted {
  Y
  N
}

enum mm_package_details_is_document_return {
  Y
  N
}

enum tm_job_shipper_to_dc_type {
  document_return
  shipper
}

enum tt_start1_is_pickup_shipper {
  Y
  N
}

enum at_cos_is_completed {
  Y
  N
}

enum tm_resends_not_in_14_18_is_deleted {
  Y
  N
}

enum um_customers_customer_types {
  NORMAL_PERSON
  JURISTIC_PERSON
}

enum mm_package_business_is_document_return {
  Y
  N
}

enum mm_package_businessxxx_is_document_return {
  Y
  N
}

enum tm_log_activity_tel_call_type {
 PLACEHOLDER
}

enum tm_trucks_is_arrived {
  N
  Y
}

enum um_employees_is_deleted {
  Y
  N
}

enum mm_package_details_is_weight_fix {
  Y
  N
}

enum tm_receive_details_size_type {
  Q
  SIZE
}

enum tm_receive_import_details_size_type {
  Q
  SIZE
}

enum tm_receive_walkins_is_deleted {
  Y
  N
}

enum mm_package_details_is_vat {
  Y
  N
}

enum tm_log_activity_type {
  PUBLIC
  PRIVATE
  no_show
}

enum tm_log_activity_transection_type {
  PUBLIC
  PRIVATE
  no_show
}

enum tm_receive_import_details_is_document_return {
  Y
  N
}

enum tm_receive_walkins_is_approved {
  Y
  N
}

enum mm_package_business_is_weight_fix {
  Y
  N
}

enum mm_package_businessxxx_is_weight_fix {
  Y
  N
}

enum um_employees_black_list {
  Y
  N
}

enum mm_package_business_is_vat {
  Y
  N
}

enum mm_package_businessxxx_is_vat {
  Y
  N
}

enum tm_books_is_pickup_customer {
  N
  Y
}

enum tm_receive_import_head_is_cod {
  Y
  N
}

enum tm_receives_is_cod {
  Y
  N
}

enum tm_receives_backup_is_cod {
  Y
  N
}

enum tm_receives_copy2_is_cod {
  Y
  N
}

enum tm_books_is_pickup_shipper {
  N
  Y
}

enum tm_trucks_is_deleted {
  Y
  N
}

enum tm_books_is_invoices {
  Y
  N
}

enum tm_receive_import_head_is_document_return {
  Y
  N
}

enum tm_receives_is_document_return {
  Y
  N
}

enum tm_receives_backup_is_document_return {
  Y
  N
}

enum tm_receives_copy2_is_document_return {
  Y
  N
}

enum acc_income_daily_del_retail {
  Y
  N
}

enum tm_trucks_is_go_app {
  Y
  N
}

enum acc_income_daily_del_bill {
  Y
  N
}

enum tm_log_activity_tel_type {
  PUBLIC
  PRIVATE
  no_show
}

enum acc_income_daily_payment_type_id {
PLACEHOLDER
}

enum tm_receive_walkins_is_invoices {
  Y
  N
}

enum tm_receive_import_head_is_deleted {
  Y
  N
}

enum tm_receives_is_deleted {
  Y
  N
}

enum tm_receives_backup_is_deleted {
  Y
  N
}

enum tm_receives_copy2_is_deleted {
  Y
  N
}

enum web_count_web_count_is_delete {
  Y
  N
}

enum acc_income_daily_is_cod {
  Y
  N
}

enum tm_receive_import_head_is_approved {
  Y
  N
}

enum tm_receive_walkin_details_size_type {
  Q
  SIZE
}

enum tm_receives_is_approved {
  Y
  N
}

enum tm_receives_backup_is_approved {
  Y
  N
}

enum tm_receives_copy2_is_approved {
  Y
  N
}

enum tm_receive_walkin_details_item_is_deleted {
  Y
  N
}

enum um_customers_customer_type {
  EXPRESS
  BUSINESS
}

enum tm_ex_transit_is_cod {
  Y
  N
}

enum tm_receive_advice_fast12_count {
  PLACEHOLDER
}

enum tm_ex_transit_is_pickup_customer {
  Y
  N
}

enum tm_ex_transit_is_pickup_shipper {
  Y
  N
}

enum tm_receive_import_head_is_pickup_customer {
  Y
  N
}

enum tm_receives_is_pickup_customer {
  Y
  N
}

enum tm_receives_backup_is_pickup_customer {
  Y
  N
}

enum tm_receives_copy2_is_pickup_customer {
  Y
  N
}

enum um_employees_levelid {
PLACEHOLDER
}

enum tm_receive_import_head_is_pickup_shipper {
  Y
  N
}

enum tm_receives_is_pickup_shipper {
  Y
  N
}

enum tm_receives_backup_is_pickup_shipper {
  Y
  N
}

enum tm_receives_copy2_is_pickup_shipper {
  Y
  N
}

enum tm_receive_import_head_is_invoices {
  Y
  N
}

enum tm_receives_is_invoices {
  Y
  N
}

enum tm_receives_backup_is_invoices {
  Y
  N
}

enum tm_receives_copy2_is_invoices {
  Y
  N
}

enum acc_income_daily_receives_money {
  Y
  N
}

enum tm_receive_import_head_app_create {
  WEB
  API
  IMPORT
}

enum tm_receives_app_create {
  WEB
  API
  IMPORT
}

enum tm_receives_backup_app_create {
  WEB
  API
  IMPORT
}

enum tm_receives_copy2_app_create {
  WEB
  API
  IMPORT
}

enum tm_receive_import_head_is_returned {
  N
  Y
}

enum tm_receives_is_returned {
  N
  Y
}

enum tm_receives_backup_is_returned {
  N
  Y
}

enum tm_receives_copy2_is_returned {
  N
  Y
}

enum um_customers_is_cod {
  N
  Y
}

enum um_customers_type {
  EXPRESS
  BUSINESS
}

enum tm_receive_serials_size_type {
  Q
  SIZE
}

enum tmp_receive_cod_size_type {
  Q
  SIZE
}
